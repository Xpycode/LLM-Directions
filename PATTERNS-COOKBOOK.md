# Swift/SwiftUI Patterns Cookbook

**Extracted from working production code across 15+ projects.**
**Last updated: 2026-07-02 (#154).** Change history lives in git — this file is a *router*: grep a keyword or scan the filenames, then read only the matching `cookbook/NNN-*.md`. Never load the whole index.

---

> **MANDATORY STANDARD — READ FIRST**
>
> Every macOS app MUST use the **App Shell Standard** below. This means:
> - `HSplitView` for panes (NOT `NavigationSplitView` — no Tahoe frosted sidebars)
> - `FCPToolbarButtonStyle` for toolbar buttons (NOT default round/capsule buttons)
> - `.windowStyle(.hiddenTitleBar)` + `.preferredColorScheme(.dark)` + `.toolbarRole(.editor)`
> - Custom dark `Theme` struct for consistent colors
>
> **Existing apps not using this pattern should be migrated.** When starting work on
> any macOS app, check whether it follows the App Shell Standard. If it doesn't,
> migrating to this standard is a prerequisite before adding new features.
>
> Reference implementation: `1-macOS/Penumbra/` (pre-Tahoe SDK toolbar)
> Titlebar injection reference: `1-macOS/VAM/` (macOS 26 SDK — no system chrome)

---

## Patterns Index

Each pattern lives in `cookbook/`. Read the relevant file when a pattern is needed.

| # | File | Pattern (grep a keyword here or the file's Tags line; read the file for detail) |
|---|------|---------|
| 0 | [00-app-shell.md](cookbook/00-app-shell.md) | MANDATORY app-shell baseline: entry point, Theme struct, FCPToolbarButtonStyle, HSplitView panes, plus migration checklist for every macOS app. |
| 1 | [01-window-layouts.md](cookbook/01-window-layouts.md) | Window layout variants: NavigationSplitView vs HSplitView, multi-window, autosaved divider positions, NSTableView embedding. |
| 2 | [02-layout-templates.md](cookbook/02-layout-templates.md) | Five reusable layout archetypes: Browser, Editor, Organizer, Dual Viewer, Workspace. |
| 3 | [03-appkit-controls.md](cookbook/03-appkit-controls.md) | SwiftUI wrappers for AppKit controls: NSButton, NSCheckbox, NSPopUpButton, NSSegmentedControl, NSSlider, NSTextField. |
| 4 | [04-swiftui-performance.md](cookbook/04-swiftui-performance.md) | SwiftUI perf: view diffing checkpoints, @ViewBuilder anti-pattern, .equatable(), fixing image-cache flash on redraw. |
| 5 | [05-export-file-dialogs.md](cookbook/05-export-file-dialogs.md) | File dialogs: NSSavePanel/NSOpenPanel, async panels, progress tracking, security-scoped bookmarks, .fileImporter. |
| 6 | [06-app-lifecycle.md](cookbook/06-app-lifecycle.md) | App lifecycle and init order: @main entry, .task init sequencing, scenePhase, Manager.configure(), FolderManager. |
| 7 | [07-timecode-typography.md](cookbook/07-timecode-typography.md) | Timecode displays with SF Pro `.monospacedDigit()` and a weight hierarchy for stable, readable readouts. |
| 8 | [08-keyboard-shortcuts.md](cookbook/08-keyboard-shortcuts.md) | Four keyboard-shortcut tiers: SwiftUI Commands, .onKeyPress, NSEvent monitor, custom manager. |
| 9 | [09-context-menus.md](cookbook/09-context-menus.md) | Context menus: basic, conditional, extracted @ViewBuilder, and NSMenuDelegate for NSTableView rows. |
| 10 | [10-selection-models.md](cookbook/10-selection-models.md) | Selection models: single, multi Set<ID>, grid, NSTableView sync, cross-pane, two-level hierarchical. |
| 11 | [11-drag-drop.md](cookbook/11-drag-drop.md) | Drag and drop: .onDrop typed handlers, concurrent TaskGroup imports, internal reorder, NSTableView/NSView drops. |
| 12 | [12-activity-progress.md](cookbook/12-activity-progress.md) | Progress UI: status bar, inline, determinate+cancel, multi-level, metrics panel, floating, phase readouts. |
| 13 | [13-workspace-switching.md](cookbook/13-workspace-switching.md) | Workspace/view-mode switching: mode toggle, tool picker, sidebar-driven, @AppStorage persistence, nested sub-modes. |
| 14 | [14-subprocess-url.md](cookbook/14-subprocess-url.md) | Subprocess file paths: `URL.path()` pitfall and keeping security-scoped access alive across async pipelines. |
| 15 | [15-native-video-analysis.md](cookbook/15-native-video-analysis.md) | Native video analysis: shot/scene detection via Y-plane histograms and motion scoring via frame differencing. |
| 16 | [16-sparkle-auto-updates.md](cookbook/16-sparkle-auto-updates.md) | Sparkle auto-update integration checklist: INFOPLIST_KEY_ gotcha, empty-appcast fix, minimal updater setup. |
| 17 | [17-thread-safe-rendering.md](cookbook/17-thread-safe-rendering.md) | Thread-safe offscreen rendering: use NSBitmapImageRep inside TaskGroup for concurrent image generation. |
| 18 | [18-pipeline-extraction.md](cookbook/18-pipeline-extraction.md) | Extract shared processing logic into a reusable pipeline with caller-owned I/O. |
| 19 | [19-swift6-concurrency.md](cookbook/19-swift6-concurrency.md) | Swift 6 concurrency: pair @MainActor with @Observable to enforce main-thread mutation at the class level. |
| 20 | [20-actor-reentrancy.md](cookbook/20-actor-reentrancy.md) | When TOCTOU is NOT possible: a synchronous, uninterrupted sequence can't race, so no re-check is needed; contrast with actor await suspension points. |
| 21 | [21-anti-patterns.md](cookbook/21-anti-patterns.md) | Catalog of common Swift/SwiftUI mistakes to avoid. |
| 22 | [22-debounced-cifilter.md](cookbook/22-debounced-cifilter.md) | Live CIFilter preview with debounced updates and a SwiftUI fallback cache to keep the last good image while recomputing. |
| 23 | [23-z-order-overlay.md](cookbook/23-z-order-overlay.md) | Out-of-bounds visual feedback via z-order overlays instead of badges. |
| 24 | [24-web-dev-patterns.md](cookbook/24-web-dev-patterns.md) | Web patterns: Jinja2 data injection, ES-module dependency injection, and a shared state module. |
| 25 | [25-extension-file-splitting.md](cookbook/25-extension-file-splitting.md) | Split large Swift files via extensions; handle access-level fixes and pick a splitting strategy per file type. |
| 26 | [26-launchd-node-service.md](cookbook/26-launchd-node-service.md) | Run a Node service under launchd with KeepAlive and scheduled tasks; install/uninstall and the Apple Silicon PATH gotcha. |
| 27 | [27-timelineview-elapsed.md](cookbook/27-timelineview-elapsed.md) | TimelineView(.periodic) for elapsed/remaining readouts, replacing Timer + objectWillChange. |
| 28 | [28-commandgroup-observation.md](cookbook/28-commandgroup-observation.md) | Wrap a Commands struct with @ObservedObject so menu items update live from @Published state. |
| 29 | [29-disk-space-preflight.md](cookbook/29-disk-space-preflight.md) | Preflight free-space check via URLResourceKey volume APIs, with same-volume detection and named-volume errors. |
| 30 | [30-volume-custom-icons.md](cookbook/30-volume-custom-icons.md) | Set a volume-root icon with a two-step write (`.VolumeIcon.icns` + `com.apple.FinderInfo` xattr + `utimes`) because `NSWorkspace.setIcon` is broken on volume roots since macOS 13.1. |
| 31 | [31-volume-enumeration.md](cookbook/31-volume-enumeration.md) | External-drive heuristic: `volumeIsRemovableKey` is misleading; correct filter is `!isInternal && !isRootFileSystem && !isLikelyDiskImage`. |
| 32 | [32-nsworkspace-asyncstream.md](cookbook/32-nsworkspace-asyncstream.md) | Bridge `NSWorkspace` mount/unmount notifications to `AsyncStream<MountEvent>` in an actor; handle observer ownership and termination cleanup. |
| 33 | [33-managed-developer-id.md](cookbook/33-managed-developer-id.md) | Xcode Archive to Direct Distribution with a server-side managed Developer ID cert; CLI `notarytool` pipeline as appendix. |
| 34 | [34-xcodeproj-clone-rename.md](cookbook/34-xcodeproj-clone-rename.md) | Clone an `.xcodeproj` to reuse toolchain bundling/entitlements/build phases via `cp -R` + sed; covers macOS sed, display names with spaces, xcuserdata gotchas. |
| 35 | [35-asyncstream-bounded-fanout.md](cookbook/35-asyncstream-bounded-fanout.md) | `withTaskGroup` drain-and-refill: stream results from thousands of async ops under a bounded concurrency cap; why naive `for url in files { group.addTask }` is wrong. |
| 36 | [36-fast-preview-heavy-commit.md](cookbook/36-fast-preview-heavy-commit.md) | Split render into a sync in-memory `preview() -> NSImage` (~15ms) vs async `render() -> Data` full pipeline (~300ms); sync `.onChange` for live sliders, no Task/cancel gymnastics. |
| 37 | [37-effective-source-fallback.md](cookbook/37-effective-source-fallback.md) | `pending ?? cached` editor binding lets a saved asset be re-tweaked without re-importing; includes dirty-detection commit gate and self-healing for vanished cache files. |
| 38 | [38-destructive-copy-guard.md](cookbook/38-destructive-copy-guard.md) | Early-return when `sourceURL.standardizedFileURL == destURL.standardizedFileURL` before `removeItem → copyItem`, so `src == dest` doesn't delete the file you're about to read. |
| 39 | [39-design-tokens.md](cookbook/39-design-tokens.md) | App-wide visual tokens: semantic modular type scale, 8pt spacing grid with internal≤external rule, SF Symbol weight/scale, corner radii, and CSS `clamp()` fluid type for web. |
| 40 | [40-spaces-plist-backend.md](cookbook/40-spaces-plist-backend.md) | Public Spaces backend by parsing `com.apple.spaces` plist for real Space UUIDs + current-Space per monitor without private CGS SPI; handles `spans-displays=0/1`, Swift 6 reader, AsyncStream 120ms settle. |
| 41 | [41-web-hero-floating-icons.md](cookbook/41-web-hero-floating-icons.md) | Fill an empty web hero by scattering app icons around the headline: absolute `.float-icon`, rotation, coprime drift animations; use `filter: drop-shadow` not box-shadow; `aria-hidden`/`alt=""`. |
| 42 | [42-web-native-dialog-lightbox.md](cookbook/42-web-native-dialog-lightbox.md) | Image lightbox via a single shared native `<dialog>`; JS swaps `img.src`; free focus trap, ESC-close, `::backdrop` blur; a11y attrs added at runtime, not in HTML. |
| 43 | [43-subprocess-fire-and-collect.md](cookbook/43-subprocess-fire-and-collect.md) | Short-lived subprocess to one stdout blob via `waitUntilExit()` + `readDataToEndOfFile()` in `Task.detached`, avoiding the tail-byte race of `readabilityHandler`; always drain stderr. |
| 44 | [44-inherited-project-dead-code-sweep.md](cookbook/44-inherited-project-dead-code-sweep.md) | Bulk-remove dead Swift from a forked project: `sed -i '' "/file.swift/d" project.pbxproj` drops all 4 refs at once (filename in every comment); `rm` on disk, close Xcode, escape regex metachars. |
| 45 | [45-macos-firmlink-canonical-path.md](cookbook/45-macos-firmlink-canonical-path.md) | `/var` vs `/private/var` URL-equality gotcha: `resolvingSymlinksInPath()` is a no-op on APFS firmlinks but FileManager returns resolved URLs; fix via `URLResourceValues.canonicalPath` (path must exist). |
| 46 | [46-web-php-download-counter.md](cookbook/46-web-php-download-counter.md) | Backendless download counter: `dl.php?app=` regex-validated, increments `counts.json` under `flock(LOCK_EX)`, then 302s to the real file; GET-only counting defeats prefetch/`curl -I`, bot UA filter. |
| 47 | [47-xcodegen-swiftterm-setup.md](cookbook/47-xcodegen-swiftterm-setup.md) | xcodegen + SwiftTerm gotchas: TTF fonts need postBuildScripts + ATSApplicationFontsPath; put UIDesignRequiresCompatibility in info.properties; disambiguate SwiftUI.Color vs SwiftTerm.Color; re-run xcodegen after adding files. |
| 48 | [48-swiftterm-output-monitor.md](cookbook/48-swiftterm-output-monitor.md) | SwiftTerm output monitoring: override `dataReceived(slice:)` in a `LocalProcessTerminalView` subclass (delegate interception breaks internals); closure bridge for PTY writes, rolling 512-char buffer for split patterns, dispatch to main before @Observable mutation. |
| 49 | [49-web-php-feedback-form-with-admin.md](cookbook/49-web-php-feedback-form-with-admin.md) | PHP flat-file feedback form with public/private PII split (`public.json` vs `.htaccess`-denied log), join-at-read, `textContent` XSS discipline, honeypot + salted-IP rate limit, Basic-Auth admin needing same-origin CSRF check. |
| 50 | [50-detachable-windows.md](cookbook/50-detachable-windows.md) | Detachable windows via `WindowGroup(for: UUID.self)` + a `[UUID: Manager]` `WindowManager`; `detachSession` moves the item and `openWindow(value:)` opens the typed scene; cross-window drag via `Transferable`/UTType/`.dropDestination`. |
| 51 | [51-swiftui-recursive-split-pane.md](cookbook/51-swiftui-recursive-split-pane.md) | Recursive split panes: `indirect enum SplitNode` tree with value-semantic insert/remove, recursing via `HSplitView`/`VSplitView`; delegate `switch` to a `@ViewBuilder func` since switch breaks the result builder; 1px active-pane border. |
| 52 | [52-appendingpathcomponent-fs-probe.md](cookbook/52-appendingpathcomponent-fs-probe.md) | Single-arg `appendingPathComponent(_:)` fs-probes to set `hasDirectoryPath`, so a path resolved before vs after the dir exists compares unequal (breaks Set<URL>/caches); always pass `isDirectory: true`. |
| 53 | [53-web-grid-masonry-jsspan.md](cookbook/53-web-grid-masonry-jsspan.md) | Masonry with row-first order via CSS Grid + JS `grid-row-end: span N` (small `grid-auto-rows: 8px`, span from aspect ratio on img load), unlike `column-count` which fills columns first; resize debounced 150ms. |
| 54 | [54-web-portrait-aware-uniform-grid.md](cookbook/54-web-portrait-aware-uniform-grid.md) | Portrait-aware uniform gallery: default `aspect-ratio: 3/2`, JS tags `.portrait` when naturalHeight>naturalWidth to `grid-row: span 2` + `2/3`, `grid-auto-flow: dense` fills gaps; aspect-ratio must sit on the link not `<picture>`. |
| 55 | [55-web-preview-suffix-deploy.md](cookbook/55-web-preview-suffix-deploy.md) | Staging-less preview on shared hosting: deploy each page as `*-new.html` beside the live one with preview-internal hrefs; ship by renaming to `*.html` + sitewide sed to strip `-new`; same origin shares assets. |
| 56 | [56-web-static-admin-template-roundtrip.md](cookbook/56-web-static-admin-template-roundtrip.md) | Prove `parse(leaf) → render(manifest) == leaf` byte-for-byte before writing any admin mutation endpoint: `{{PLACEHOLDER}}` template, pure `strtr` renderer, regex parser, CLI-only diff harness as permanent regression guard. |
| 57 | [57-commandgroup-saveitem-cmdw-override.md](cookbook/57-commandgroup-saveitem-cmdw-override.md) | Override ⌘W in multi-tab SwiftUI via `CommandGroup(replacing: .saveItem)` (only placement beating File > Close Window) with a never-`.disabled` button; a greyed `CommandMenu` falls through to close-window; confirm via `.confirmationDialog`. |
| 58 | [58-xcodegen-postbuildscripts-sandbox.md](cookbook/58-xcodegen-postbuildscripts-sandbox.md) | xcodegen resource-copy script phase needs both `inputFiles:` (sandbox `file-read-data` grant) and `outputFiles:` (incremental skip); use `$(VAR)` not `${VAR}`, list concrete paths (no globs), switch to `.xcfilelist` past 10 files. |
| 59 | [59-decodable-only-codingkeys.md](cookbook/59-decodable-only-codingkeys.md) | A `Decodable`-only type with a hand-rolled `init(from:)` synthesizes no `CodingKeys` (SE-0166 only generates them alongside a synthesized init/encode), failing "cannot find CodingKeys"; declare the enum explicitly. |
| 60 | [60-closure-bridged-appkit.md](cookbook/60-closure-bridged-appkit.md) | Trigger AppKit (window close, NSWorkspace deep-link, NSAlert) from a model-layer `@MainActor` ObservableObject by injecting closures (`dismissAction`, `openSettingsAction`) from the window controller instead of holding `weak var window`, keeping AppKit out of model files. |
| 61 | [61-probe-classify-not-catch-all.md](cookbook/61-probe-classify-not-catch-all.md) | TCC/FDA pre-flight without false positives: `enum AccessProbeResult` driven by `fileExists(atPath:isDirectory:)` then typed `CocoaError.fileReadNoPermission`, only `.permissionDenied` gates FDA; arbitrary `~/` paths aren't TCC-protected. |
| 62 | [62-web-prefers-color-scheme-token-theme.md](cookbook/62-web-prefers-color-scheme-token-theme.md) | Web auto dark theme by overriding ~20 design tokens in one `@media (prefers-color-scheme: dark)` block plus `color-scheme: light dark`; grep for hardcoded non-token colors, and lightbox JS must read `img.currentSrc` not `.src`. |
| 63 | [63-macos-favicon-quicklook-glyph-ico.md](cookbook/63-macos-favicon-quicklook-glyph-ico.md) | macOS favicon gen: ImageMagick silently emits near-blank PNGs for SVGs with clipPath/gradients (missing rsvg), so render master with `qlmanage -t -s 1024`; author separate glyph-only 16px SVG, pack multi-size `.ico`, flatten apple-touch-icon on opaque bg. |
| 64 | [64-carbon-global-hotkey-toggle.md](cookbook/64-carbon-global-hotkey-toggle.md) | Permission-free system-wide hotkey via Carbon `RegisterEventHotKey` (no Accessibility/Input-Monitoring, catches synthesized keys); pass `self` through `userData` since the C callback can't capture, hop to `@MainActor`, use Carbon keycodes/modifier masks, tear down explicitly. |
| 65 | [65-cursor-anchored-nspanel-hud.md](cookbook/65-cursor-anchored-nspanel-hud.md) | Cursor-anchored non-activating `NSPanel` HUD: `[.borderless,.nonactivatingPanel]` + `orderFrontRegardless()`, `acceptsFirstMouse` subclass, clamp to cursor screen `visibleFrame`, dismiss via hotkey re-press + permission-free global mouse monitor; growing panel computes deterministic height and anchors top edge. |
| 66 | [66-macos-permission-free-disk-stats.md](cookbook/66-macos-permission-free-disk-stats.md) | Permission-free disk stats without `fs_usage` (needs root/FDA): capacity via `statfs("/")` (`f_bavail*f_bsize`), throughput by summing each `IOBlockStorageDriver` `Statistics` `Bytes (Read/Write)` as per-tick deltas; `IOObjectRelease` everything, set `allowsNonnumericFormatting=false`. |
| 67 | [67-swiftui-jitter-free-numeric-readout.md](cookbook/67-swiftui-jitter-free-numeric-readout.md) | Stop a live number from resizing its container: `.monospacedDigit()` equalizes digit width but not count, so stack a hidden `Text(widestValue)` behind the real value in a `ZStack` to reserve worst-case width with no measurement. |
| 68 | [68-macos-permission-free-network-throughput.md](cookbook/68-macos-permission-free-network-throughput.md) | Permission-free network throughput via `getifaddrs` (no BPF): cumulative counters live only on `AF_LINK` records' `ifa_data` (`if_data.ifi_ibytes/ifi_obytes`) — skip IP records or you double-count; report per-tick delta, `freeifaddrs` via defer, make interface filter a pluggable predicate testing flag bits. |
| 69 | [69-macos-iokit-power-sources-battery.md](cookbook/69-macos-iokit-power-sources-battery.md) | Permission-free battery via `IOKit.ps`: ownership follows prefix — `IOPSCopy…` uses `.takeRetainedValue()`, `IOPSGetPowerSourceDescription` uses `.takeUnretainedValue()`; `-1` time = calculating (nil), desktop Macs report no source so hide via `isPresent`. |
| 70 | [70-swiftui-data-driven-tile-strip.md](cookbook/70-swiftui-data-driven-tile-strip.md) | Cure SwiftUI "unable to type-check this expression in reasonable time" from N hardcoded subviews by rendering a `ForEach` over a homogeneous `[Descriptor]` array; a `kind` enum gives stable identity/order and one `visibleStats` builder is the hook for stat-selection. |
| 71 | [71-lsuielement-self-managed-settings-window.md](cookbook/71-lsuielement-self-managed-settings-window.md) | In `LSUIElement` apps `showSettingsWindow:` silently no-ops; build your own `NSWindow(contentViewController:)` with `isReleasedWhenClosed=false` + `activate` + `makeKeyAndOrderFront`, and strip the auto ⌘, item via `CommandGroup(replacing: .appSettings)`. |
| 72 | [72-scoped-carbon-hotkey-esc-dismiss.md](cookbook/72-scoped-carbon-hotkey-esc-dismiss.md) | Permission-free Esc-to-dismiss for a non-key panel: register bare Escape as a Carbon hotkey only while visible; the second `HotKeyService` must filter by `EventHotKeyID` in the C callback (Carbon dispatches to all handlers), and drive register/unregister from the panel's own visibility signal so click-away doesn't leak. |
| 73 | [73-verify-hud-without-screen-recording.md](cookbook/73-verify-hud-without-screen-recording.md) | Verify a HUD without Screen-Recording: observe show/hide via `CGWindowListCopyWindowInfo` (geometry needs no perm), drive via `CGEventPost`, measure hotkey capture with a listen-only `cgAnnotatedSessionEventTap` (`delivered==0` ⇒ consumed); a `RegisterEventHotKey` conflict probe is useless cross-process. |
| 74 | [74-macos-permission-free-load-uptime-topproc.md](cookbook/74-macos-permission-free-load-uptime-topproc.md) | Permission-free load/uptime/top-process samplers: `getloadavg` normalized by `activeProcessorCount`, `sysctlbyname("kern.boottime")` not `ProcessInfo.systemUptime` (drops sleep); libproc `proc_pid_rusage` is `CHECK_SAME_USER` so foreign UIDs EPERM — ship user-only memory (`ri_phys_footprint`) via `withMemoryRebound`. |
| 75 | [75-macos-top-processes-by-app.md](cookbook/75-macos-top-processes-by-app.md) | Top processes by CPU/mem/disk grouped by app via two engines: in-process `rusage_info_v4` (user-only, `ri_user_time` is mach-time NOT nanoseconds — convert via `mach_timebase_info`) vs shelling to `/usr/bin/top` (system-inclusive, needs sandbox off); group by app sum FIRST then rank, `appGroupName` climbs to outermost `.app`. |
| 76 | [76-macos-appicon-coregraphics-generator.md](cookbook/76-macos-appicon-coregraphics-generator.md) | Generate `AppIcon.appiconset` (all 10 sizes + Contents.json) from a standalone `swift` Core Graphics script: convert `oklch`→sRGB by hand, render per-size not downscaled (radius = `0.2237×size`, CG y-axis bottom-up), emit both the appiconset and catalog-level Contents.json. |
| 77 | [77-jsx-prototype-to-png-webkit-playwright.md](cookbook/77-jsx-prototype-to-png-webkit-playwright.md) | Render a Babel-in-browser JSX design prototype to per-section PNGs without Chrome: a flat-render sibling harness mounts section components in normal flow (canvas wrappers guard ctx), then Playwright+WebKit with `deviceScaleFactor:2` waits on a `data-render-ready` flag and screenshots each `[data-flat-section]`; serve over http not `file://`. |
| 78 | [78-swiftui-state-aware-menu-pill.md](cookbook/78-swiftui-state-aware-menu-pill.md) | State-aware header pill that is both nav target and switcher: body tap navigates when off the surface but opens the picker when already on it, chevron always opens picker; one `tapTarget` expression drives label + tap + a11y, and "last-used" must span the whole merged control. |
| 79 | [79-swiftui-layoutpriority-active-width-budget.md](cookbook/79-swiftui-layoutpriority-active-width-budget.md) | HStack truncation is a priority contest not a maxWidth ceiling: `maxWidth` is a flexible `0...max` ceiling and greedy Spacers win, so give the readable element higher `.layoutPriority` (state-driven: active 160/priority 2, inactive 90/1); prefer `maxWidth` over fixed `width:`. |
| 80 | [80-watchconnectivity-phone-watch-state-mirror.md](cookbook/80-watchconnectivity-phone-watch-state-mirror.md) | Mirror full phone→watch state over WatchConnectivity (App Groups DON'T cross devices); use `updateApplicationContext` + eagerly activate `WCSession` at `@main` or cold-launch seed push is dropped. |
| 81 | [81-nonkey-hud-panel-synthetic-paste.md](cookbook/81-nonkey-hud-panel-synthetic-paste.md) | A HUD that synth-pastes into the frontmost app must be NON-KEY (plain `NSPanel` + `FirstMouseHostingView`, `orderFrontRegardless`) or it steals keyboard focus and swallows the ⌘V; clear Return flags, stamp changeCount. |
| 82 | [82-makekey-panel-paste-self-refusal-reactivate.md](cookbook/82-makekey-panel-paste-self-refusal-reactivate.md) | A KEY picker panel that synth-pastes must capture the pre-`makeKey` frontmost app and, only on `wouldPasteIntoSelf`, `target.activate()` + recheck secure input before ⌘V; don't always-reactivate. |
| 83 | [83-calendar-overlap-column-packing.md](cookbook/83-calendar-overlap-column-packing.md) | Lay out overlapping calendar/timeline events: cluster transitively-overlapping runs (cluster end = `max(runningEnd, e.end)`), then first-free-column packing; overlap is HALF-OPEN (`start < end`, free test `>=`). |
| 84 | [84-swiftdata-detached-model-revalidate-guard.md](cookbook/84-swiftdata-detached-model-revalidate-guard.md) | A row closure reading a persisted `@Model` prop after a bulk delete hard-crashes `EXC_BREAKPOINT` (getter traps on detached model); guard `item.modelContext != nil` FIRST before any persisted read. |
| 85 | [85-phase-aware-subprocess-watchdog.md](cookbook/85-phase-aware-subprocess-watchdog.md) | No-progress subprocess watchdog that won't false-alarm during legitimately-silent phases (ANALYZING/FINALIZING): pure `watchdogAssessment` verdict with hard-timeout `.hung` checked FIRST, phase flags set at transitions. |
| 86 | [86-single-flight-async-dedup-actor.md](cookbook/86-single-flight-async-dedup-actor.md) | Single-flight async dedup: a sync `guard cache==nil` is worthless across an `await`; hold `inFlight: [Key: Task<V,Never>]` in an actor, store the Task BEFORE awaiting it, clear the slot AFTER save. |
| 87 | [87-swiftui-caret-token-insertion-nstextfield.md](cookbook/87-swiftui-caret-token-insertion-nstextfield.md) | Insert a token at a SwiftUI text field's caret: wrap `NSTextField` in `NSViewRepresentable` + a weak-held controller; read `currentEditor().selectedRange`, replace via `NSString`, restore focus+caret on next `DispatchQueue.main.async` tick. |
| 88 | [88-detect-fail2ban-ban-vs-auth-failure.md](cookbook/88-detect-fail2ban-ban-vs-auth-failure.md) | Distinguish a fail2ban IP-ban from a real SSH/SFTP auth failure by WHERE the connection dies: `Permission denied`/`Authentications that can continue` = rejected credential; `Connection closed before password:` = pre-auth ban; probe ban-safely with `expect`. |
| 89 | [89-swiftui-native-toolbar-tahoe-glass-optout.md](cookbook/89-swiftui-native-toolbar-tahoe-glass-optout.md) | Native `.toolbar` items grow Liquid Glass capsules on the macOS 26 SDK; opt out via `UIDesignRequiresCompatibility=true` in a hand-managed base Info.plist (`INFOPLIST_KEY_*` fails silently, not on allowlist); use a `.principal` zone for three-zone edge-to-edge spread. |
| 90 | [90-macos-offmain-file-logger-bound-rotate.md](cookbook/90-macos-offmain-file-logger-bound-rotate.md) | Mirror an `@MainActor @Observable` console feed to disk off-main: a `Sendable LogFileWriter` on its own serial `DispatchQueue`, `FileHandle.seekToEnd`+`write` (O(1)), cap RAM array + rotate at byte ceiling, format on producer, `try?`-swallow I/O. |
| 91 | [91-detect-concurrent-cli-sessions-shared-checkout.md](cookbook/91-detect-concurrent-cli-sessions-shared-checkout.md) | Detect two CLI agents sharing one git checkout (shared HEAD → wrong-branch commits): `pgrep -x` → each PID's cwd via `lsof -d cwd -Fn` → `git rev-parse --show-toplevel`, group by toplevel, warn if 2+; worktree-aware, warn-only at SessionStart. |
| 92 | [92-swiftui-greedy-clear-spacer-header-align.md](cookbook/92-swiftui-greedy-clear-spacer-header-align.md) | A width-only `Color.clear`/`Spacer` is greedy in the unconstrained axis and balloons a header row tall; reserve leading inset with `.padding(.leading, sumOfWidths)` and share every column width via one `RowMetrics` enum to align header over rows. |
| 93 | [93-swiftui-pure-sort-feed-offmain-prewarm.md](cookbook/93-swiftui-pure-sort-feed-offmain-prewarm.md) | A sort comparator that resolves its key via file I/O inside `body` hangs the window; keep `sortField` pure (in-memory cache + fallback, zero I/O), prewarm off-main in `Task.detached`, re-sort by bumping a `@Published` token. |
| 94 | [94-quicktime-lossless-atom-date-patch.md](cookbook/94-quicktime-lossless-atom-date-patch.md) | Patch embedded QuickTime/ISO-BMFF `creation_time` (`mvhd`/`tkhd`/`mdhd`) in pure Swift via same-length byte overwrites (fixed-width fields never shift `mdat`/`stco`); QT epoch 1904 (+2082844800), reject v0 overflow, verify with AVAssetWriter byte-diff. |
| 95 | [95-macos-quicklook-thumbnail-hybrid.md](cookbook/95-macos-quicklook-thumbnail-hybrid.md) | Video thumbnails: try `QLThumbnailGenerator` first (out-of-process decode + system cache), FFmpeg fallback only on a throw; `representationTypes: .thumbnail` (`.icon`/`.all` return the generic movie glyph successfully so fallback never fires). |
| 96 | [96-macos-app-indexer-cryptex-symlink.md](cookbook/96-macos-app-indexer-cryptex-symlink.md) | `contentsOfDirectory(at:includingPropertiesForKeys:)` silently drops symlinked Cryptex system apps (Safari) when resolving `.isDirectoryKey`; enumerate with the path API `contentsOfDirectory(atPath:)` to include symlinks like Finder/`ls`. |
| 97 | [97-macos-agent-app-nslog-stderr-capture.md](cookbook/97-macos-agent-app-nslog-stderr-capture.md) | `NSLog` from an `open`-launched app is invisible to `log show process==` (absence misreads as clean); run the bundle binary directly with `nohup "$APP/Contents/MacOS/App" >out 2>err & disown` and grep the stderr file. |
| 98 | [98-swiftui-foreach-id-index-stale-rows.md](cookbook/98-swiftui-foreach-id-index-stale-rows.md) | List renders stale rows after filtering because `.id(index)` on a `ForEach` child overrides `id: \.element.id`, re-keying by position so SwiftUI reuses the cached view tree; key rows on stable `element.id` and `scrollTo(element.id)`. |
| 99 | [99-lftp-netrc-multisite-deploy.md](cookbook/99-lftp-netrc-multisite-deploy.md) | Multi-site `lftp` SFTP deploy with secrets out of git: put USER in the URL (`sftp://USER@HOST`) since ssh ignores `~/.netrc` username, password in chmod-600 `~/.netrc` outside repo; no `--delete`, exclude server-managed files, seed empty `public.json` once. |
| 100 | [100-web-centralized-donation-page.md](cookbook/100-web-centralized-donation-page.md) | One `donate.html?app=<slug>` hub self-personalizes from a JS `APPS` map (paid apps absent → generic); single source of provider URL; never nest a donate anchor inside a card `<a>`. |
| 101 | [101-swiftui-settings-search-highlight-focus.md](cookbook/101-swiftui-settings-search-highlight-focus.md) | Settings-search result→control pulse must fire on BOTH `onAppear` and `onChange` (target mounts with flag set); ⌘F focuses `.searchable` via hidden Button walking to `NSSearchField`; `scrollTo` row-level in a Form. |
| 102 | [102-swiftui-inapp-feedback-spm-shared-endpoint.md](cookbook/102-swiftui-inapp-feedback-spm-shared-endpoint.md) | Drop-in feedback sheet SPM package POSTing to shared `feedback-submit.php`; bridge `@Sendable ()->String?` log hook via `MainActor.assumeIsolated`; `URLSessionTaskDelegate` refusing redirects keeps 302=ok distinct. |
| 103 | [103-web-roster-single-source-of-truth-json.md](cookbook/103-web-roster-single-source-of-truth-json.md) | One `apps.json` roster read by PHP/JS-fetch/server-rendered HTML replaces 5 drifting copies; per-item flags for differing membership; delete static `<option>`s or JS double-fills; verify in headless browser not lint. |
| 104 | [104-swiftui-commands-menu-dividers-donate.md](cookbook/104-swiftui-commands-menu-dividers-donate.md) | `CommandGroup(after:)` inserts NO separator and stacks by declaration order → add explicit `Divider()` (even a divider-only group) beside a package command; Donate menu item takes NO ellipsis (browser hand-off). |
| 105 | [105-web-js-injected-nav-footer-partials.md](cookbook/105-web-js-injected-nav-footer-partials.md) | Single-source JS-injected nav/footer partials: markup in one JS file, pages carry empty placeholders + `data-*` per-page params; reserve `min-height` for CLS; `<noscript>` fallback for legally-required footer links. |
| 106 | [106-xcodebuild-script-phase-self-cycle.md](cookbook/106-xcodebuild-script-phase-self-cycle.md) | `xcodebuild` "Cycle inside <target>" from an in-place re-sign script listing a bundle file as both input AND output → declare ONLY a `$(DERIVED_FILE_DIR)/*.stamp` output; Xcode.app GUI hides it, CLI/CI aborts. |
| 107 | [107-swiftui-disabled-control-hittest-fallthrough.md](cookbook/107-swiftui-disabled-control-hittest-fallthrough.md) | `.disabled(true)` removes a control from hit-testing so taps fall THROUGH to an ancestor `.onTapGesture`; fix by staying enabled and `guard`ing the action inside the closure, conveying disabled via `.opacity`. |
| 108 | [108-spm-citizenship-menu-consolidation-proving-ground.md](cookbook/108-spm-citizenship-menu-consolidation-proving-ground.md) | Consolidate Feedback/Donate/About menu wiring into one shared SPM `CitizenshipCommands`; migrate the source app FIRST as a proving ground; the Feedback↔Tip-Jar divider must be emitted INSIDE the package; verify the live menu via AppleScript. |
| 109 | [109-dynamic-canbecomekey-optin-hud-verbs.md](cookbook/109-dynamic-canbecomekey-optin-hud-verbs.md) | Non-key HUD that also needs bare-key verbs: gate `canBecomeKey` on dynamic STATE (`keyboardModeEngaged`), not a static `true` (a hosted SwiftUI Button promotes it to key anyway); flip true only on explicit engage before `makeKey()`. |
| 110 | [110-macos-settings-pane-deeplink-verify.md](cookbook/110-macos-settings-pane-deeplink-verify.md) | Deep-link a System Settings pane via `x-apple.systempreferences:<bundle-id>`; the id is the pane's ExtensionKit `.appex` `CFBundleIdentifier` (inconsistent, drifts across OS) → verify by reading `/System/Library/ExtensionKit/Extensions/*.appex/Info.plist`. |
| 111 | [111-swiftui-click-to-record-hotkey-field.md](cookbook/111-swiftui-click-to-record-hotkey-field.md) | Hotkey recorder = make the field itself a `Button` (not box + separate Record button that clips); capture via local `NSEvent.addLocalMonitorForEvents(.keyDown)` (no Input Monitoring), `return nil` to swallow, `return event` on Escape, match by Carbon keyCode. |
| 112 | [112-macos-security-scoped-bookmarks.md](cookbook/112-macos-security-scoped-bookmarks.md) | Security-scoped bookmarks to re-open picked files across launches in a SANDBOXED app: persist `Data` from `bookmarkData(.withSecurityScope)`, resolve + `startAccessingSecurityScopedResource`, re-mint on `bookmarkDataIsStale`; no-op outside the sandbox. |
| 113 | [113-macos-appearance-preference-nsapp-not-preferredcolorscheme.md](cookbook/113-macos-appearance-preference-nsapp-not-preferredcolorscheme.md) | Light/Dark/Match-System picker: `.preferredColorScheme(nil)` does NOT clear a forced `NSWindow.appearance` (Match System stays dark) → set `NSApplication.shared.appearance` from `.onChange(initial:true)`, don't keep both owners. |
| 114 | [114-macos-runtime-dock-icon-appearance-swap.md](cookbook/114-macos-runtime-dock-icon-appearance-swap.md) | macOS bundle app icon can't vary by appearance (`actool` drops dark renditions, iOS-only) → swap the running Dock tile at runtime via `NSApp.applicationIconImage`, driven by KVO on `NSApp.effectiveAppearance`; bundle both as `.icns`. |
| 115 | [115-swiftui-polled-status-seed-init-animate-live.md](cookbook/115-swiftui-polled-status-seed-init-animate-live.md) | Polled permission row: seed `@State` in `init` (`State(initialValue:)`) for no first-frame flash, and `withAnimation` only when the polled value actually changes; poll `AXIsProcessTrusted()` via Timer + `didBecomeActive`. |
| 116 | [116-swiftui-bare-key-shortcut-textfield-focus-handoff.md](cookbook/116-swiftui-bare-key-shortcut-textfield-focus-handoff.md) | A bare-letter `.keyboardShortcut([])` is swallowed by a focused `TextField` → clear `@FocusState` on the state transition that reveals the action; a view takes only ONE shortcut, so add a zero-opacity sibling Button for a second binding. |
| 117 | [117-swiftui-imagerenderer-appearance-matrix-test.md](cookbook/117-swiftui-imagerenderer-appearance-matrix-test.md) | Verify a theme actually PAINTS tokens with an `ImageRenderer` snapshot matrix over {light,dark}×presets; headless has ~0.035 green/blue gamma bias so lead with bias-immune checks (luminance polarity, selected-vs-rest differential, WCAG); can't render Representable/lazy views. |
| 118 | [118-swift6-nonsendable-resource-to-actor-sending.md](cookbook/118-swift6-nonsendable-resource-to-actor-sending.md) | Hand a non-`Sendable` resource to a Swift 6 `actor` via `sending` (ownership moves), then observe it ONLY through the actor returning `Sendable` values; a `let` local transfers cleanly where a reachable `var` property keeps the region live and fails. |
| 119 | [119-macos-app-rebrand-xcodegen-engine-name-split.md](cookbook/119-macos-app-rebrand-xcodegen-engine-name-split.md) | Rename an xcodegen macOS app whose name derives from the engine it wraps (YTdl vs `yt-dlp`/`YTDLP`): a case-sensitive `s/YTdl/Magpie/` is the identity boundary that spares engine tokens; `git mv` folders, rewrite `project.yml`, verify built bundle plist; feedback-slug allowlist breaks silently. |
| 120 | [120-swift-crypto-kdf-cryptokit-type-identity.md](cookbook/120-swift-crypto-kdf-cryptokit-type-identity.md) | Add Scrypt/PBKDF2 via swift-crypto: never mix `import CryptoKit` with `import Crypto` (two same-named `SymmetricKey` types), link both `Crypto`+`_CryptoExtras`; a cited PR proves want not ship, confirm the symbol is in a tagged release. |
| 121 | [121-web-clone-retint-single-app-site.md](cookbook/121-web-clone-retint-single-app-site.md) | Clone-and-retint a sibling static app site: clone structure+color but SCRUB donor facts (ARCHS, bundle-id, price, copy); grep both casings; retint in the per-site CSS not `site.css`; never a fake `appcast.xml`; 4 app-side go-live gates. |
| 122 | [122-codable-tolerant-migration-no-wipe.md](cookbook/122-codable-tolerant-migration-no-wipe.md) | Hand-written `init(from:)` so a one-field drift can't wipe a persisted `[Job]`: synthesized decode is all-or-nothing per array; decode-only legacy key, `decodeIfPresent`+default, `try?`→nil on reshaped sub-blobs; `encode` writes new shape only. |
| 123 | [123-draggable-nonactivating-nspanel.md](cookbook/123-draggable-nonactivating-nspanel.md) | Make a non-activating `NSPanel` draggable: `isMovableByWindowBackground`, route all placement through one `place(rect)` recording `lastPlacedOrigin` before `setFrame` so `windowDidMove` ignores self-moves; store top-LEFT anchor; session-only drop memory. |
| 124 | [124-swift-benchmark-under-O-allocation-free-folded-scan.md](cookbook/124-swift-benchmark-under-O-allocation-free-folded-scan.md) | Benchmark hot Swift under `-O` not Debug (~18x penalty from bounds-checks/ARC/no-inline); override `SWIFT_OPTIMIZATION_LEVEL=-O` keeping Debug config so `@testable` works; fold query once and compare against raw mmap bytes, materialize only on match. |
| 125 | [125-swiftui-destination-popup-menu.md](cookbook/125-swiftui-destination-popup-menu.md) | Save-location chooser as a `Menu` (not `Picker`) whose label IS the current choice plus a "Choose…" item opening `NSOpenPanel`; Picker can't host a panel action; `.frame(maxWidth:.infinity,.leading)` on the Menu; render your own checkmark. |
| 126 | [126-closure-struct-dependency-injection.md](cookbook/126-closure-struct-dependency-injection.md) | Dependency injection as a struct of closures (`.live`/`.inert`) for side-effecting syscalls instead of a protocol; tests mutate one field, `.inert` doubles as the safe `#Preview` env; optional async sibling for blocking work. |
| 127 | [127-toctou-identity-verify-before-destructive-op.md](cookbook/127-toctou-identity-verify-before-destructive-op.md) | Close the scan→act TOCTOU gap: capture `(st_dev, st_ino)` via `lstat` at scan time, re-check immediately before trashing, classify matches/mismatch/missingNow/unverifiable (not a bool); block-list protected paths first, identity second. |
| 128 | [128-macos-dock-icon-iconservices-cache-refresh.md](cookbook/128-macos-dock-icon-iconservices-cache-refresh.md) | Generic Dock icon on a fresh build is the IconServices cache, not the build: prove `CFBundleIconName`+`AppIcon.icns` are in the bundle, then `touch`→`lsregister -f`→delete `com.apple.iconservices.store`→`killall Dock iconservicesagent`, no rebuild. |
| 129 | [129-swiftui-focus-preserving-reveal-securefield.md](cookbook/129-swiftui-focus-preserving-reveal-securefield.md) | Reveal/hide password without losing focus or text: keep `SecureField` and `TextField` both alive in a `ZStack` sharing one binding+`@FocusState`, toggle `.opacity`+`.allowsHitTesting`; `if/else` changes structural identity and drops focus. |
| 130 | [130-swiftui-unsaved-edits-selection-switch-prompt.md](cookbook/130-swiftui-unsaved-edits-selection-switch-prompt.md) | Unsaved-changes prompt on `List(selection:)` switch: no `shouldSelect` veto exists, so synchronously write the selection back to A to preserve the `.id(A)` detail draft, stash B in `pendingSwitchID`, dialog commits; teardown paths discard never block. |
| 131 | [131-swiftui-pro-feature-gating-progate.md](cookbook/131-swiftui-pro-feature-gating-progate.md) | Freemium gating off one `@Observable .shared` `isPro`: `.proGated` view-swap to a LockedFeaturePanel (gated view never instantiated) or `ProGate.allow` route-to-upsell for controls; never delete backing data; inject a gate seam so tests don't silently no-op. |
| 132 | [132-strip-php-before-html-parser.md](cookbook/132-strip-php-before-html-parser.md) | Strip `<?php …?>` before an HTML parser or it emits PHP source as data nodes; regex `<\?.*?(?:\?>\\|\Z)` with `re.DOTALL`, non-greedy, replace with a space — anchor to `?>` OR end-of-file since pure-PHP files omit the closing tag. |
| 133 | [133-swiftui-menustyle-button-pill-toolbar.md](cookbook/133-swiftui-menustyle-button-pill-toolbar.md) | Labeled on/off pill for a toolbar `Menu`: `.labelStyle(.titleAndIcon)` to show the name plus `.menuStyle(.button)` so a custom `ButtonStyle` replaces the pull-down bezel (no double background); defaulted amber tint param for high-consequence actions. |
| 134 | [134-ffmpeg-author-verify-timecode-stream-copy.md](cookbook/134-ffmpeg-author-verify-timecode-stream-copy.md) | Author+verify a start timecode on a lossless ffmpeg `-c copy` trim: `-ss` before `-i`, `-timecode HH:MM:SS:FF`; verify via the `tmcd` STREAM tag (format tag is empty even on success); declare downstream duration from the PROBED file since `-c copy` keeps the trailing GOP. |
| 135 | [135-fail-open-proxy-status-codes.md](cookbook/135-fail-open-proxy-status-codes.md) | Fail-open status codes for a key-hiding license relay: partition HTTP codes into VERDICT (200/400/404, only ever the upstream's verbatim reply) vs FAIL-OPEN (everything the proxy itself originates); a self-emitted 400 else silently downgrades a paying user. |
| 136 | [136-swiftpm-metal-shader-embedded-source.md](cookbook/136-swiftpm-metal-shader-embedded-source.md) | Metal shader in a SwiftPM package: `makeDefaultLibrary()` has no main bundle and `makeDefaultLibrary(bundle: .module)` won't compile (no resource bundle from a lone `.metal`); embed the source as a `static let` string and `makeLibrary(source:options:)`, then delete the stray `.metal`. |
| 137 | [137-swift-subprocess-strings-line-cap-deadlock.md](cookbook/137-swift-subprocess-strings-line-cap-deadlock.md) | swift-subprocess `.strings()` deadlocks on a huge newline-free line: default `.maxLineLength(128KB)` stops draining the pipe, child blocks on `write()`; pass `.unbounded` (expose `maxLineLength: Int?`) on both stdout+stderr; small-output tests never catch it. |
| 138 | [138-stale-build-masquerading-as-bug.md](cookbook/138-stale-build-masquerading-as-bug.md) | A bug you can't reproduce for code you already fixed? Check the running binary's build time first: out-of-band apps (hotkey/LSUIElement/login item) aren't replaced by a Debug build+green tests; `ps aux`→`stat -f built`→compare to the fix commit. |
| 139 | [139-ax-drive-swiftui-settings-form-verify.md](cookbook/139-ax-drive-swiftui-settings-form-verify.md) | Drive & verify a SwiftUI Settings Form headlessly via the Accessibility API: focus fields with `kAXFocusedAttribute` and enter text by PASTING (Cmd-V) not `kAXValueAttribute`, assert on the submit button's `kAXEnabledAttribute` (field value lies), match buttons by `AXDescription`; JSON output is the oracle. |
| 140 | [140-xcodegen-folder-reference-vendor-cli-tool.md](cookbook/140-xcodegen-folder-reference-vendor-cli-tool.md) | Vendor an opaque CLI + support tree into an `.app` at `Contents/Resources/` via an xcodegen `type: folder` reference (sibling of `sources:`, copies verbatim, no run-script/sandbox); inject the tool URL so tests find it without `Bundle.main`. |
| 141 | [141-exiftool-geotag-end-to-end-test-no-fixtures.md](cookbook/141-exiftool-geotag-end-to-end-test-no-fixtures.md) | Test ExifTool `-geotag` end-to-end with no binary fixtures: generate JPEG at runtime, defeat the timezone trap with a wide ±16h single-coordinate GPX, and verify GPS from `-json -n` bytes since already-tagged returns exit 2. |
| 142 | [142-xcodegen-test-target-team-id-dyld.md](cookbook/142-xcodegen-test-target-team-id-dyld.md) | dyld "different Team IDs" when an xcodegen `.xctest` loads into its host app: give the test target the SAME `configFiles` xcconfig as the app plus `CODE_SIGN_STYLE: Automatic` so both sign with one `DEVELOPMENT_TEAM`. |
| 143 | [143-observable-mainactor-nsworkspace-mount-monitor.md](cookbook/143-observable-mainactor-nsworkspace-mount-monitor.md) | Live volume mount/unmount monitoring in an `@Observable @MainActor` model: mark the observer-token storage `nonisolated(unsafe)` so nonisolated `deinit` can `removeObserver`, and wrap the `@Sendable` main-queue block in `MainActor.assumeIsolated`. |
| 144 | [144-elicit-verification-verdicts-corrupt-output-and-couldnt-run.md](cookbook/144-elicit-verification-verdicts-corrupt-output-and-couldnt-run.md) | Force each verdict of a verify ladder: a 1-frame `-c copy` yields a deterministic ✗ that isolates structural-anomaly→hash-fail, and `chmod 000` on the source (bookmarks follow inode, so not rename/delete) forces the ⚠ couldn't-run state. |
| 145 | [145-asyncstream-reducer-run-sheet.md](cookbook/145-asyncstream-reducer-run-sheet.md) | Consumer counterpart to #35: fold an engine's `AsyncThrowingStream` into `@Observable` fields via a pure `apply(_:)` reducer on a MainActor Task, shown as a run `.sheet`; two-segment progress bar for a bursty final batch, and honest no-Cancel when cancelling the consumer can't stop the producer. |
| 146 | [146-caffeinate-long-xcodebuild-test-runs-idle-sleep-deadlock.md](cookbook/146-caffeinate-long-xcodebuild-test-runs-idle-sleep-deadlock.md) | A long `xcodebuild test` that "hangs" for hours is the Mac idle-sleeping and SIGKILLing a child `Process` parked in `waitUntilExit()`; wrap in `caffeinate -is` and add `-test-timeouts-enabled` allowances so a real hang aborts by name. |
| 147 | [147-swiftui-present-modal-from-body-not-toolbar-item.md](cookbook/147-swiftui-present-modal-from-body-not-toolbar-item.md) | A `.alert`/`.confirmationDialog`/`.sheet` attached to a `ToolbarItem` soft-locks the window (dims, no chrome) because toolbar items live in titlebar-accessory context; the toolbar button only flips an `@State` flag and the presentation modifier goes on the content body. |
| 148 | [148-verify-web-video-export-webkit-mediarecorder.md](cookbook/148-verify-web-video-export-webkit-mediarecorder.md) | Verify a web `MediaRecorder`/`captureStream` video export headless with Playwright WebKit over HTTP (secure context): probe `isTypeSupported`, capture the `download` event via `waitForEvent`→`saveAs`, and trust magic bytes (`ftyp`/`1a45dfa3`) plus size, not the app's "Saved" status. |
| 149 | [149-macos-hermetic-download-codesign-selfreplace-test.md](cookbook/149-macos-hermetic-download-codesign-selfreplace-test.md) | Test a macOS download→codesign-check→self-replace flow hermetically: `URLSession.download(from:)` on a `file://` URL, ad-hoc `codesign -s -` (TeamID nil==nil passes), `ditto -c -k` to preserve the signature across zip; assert the on-disk `CFBundleShortVersionString` flipped, and stays old on a tamper throw. |
| 150 | [150-sparkle-eddsa-cryptokit-silent-selfupdate.md](cookbook/150-sparkle-eddsa-cryptokit-silent-selfupdate.md) | Verify a Sparkle appcast with plain CryptoKit (no Sparkle.framework): `Curve25519.Signing` `isValidSignature` over `mmap`'d archive bytes in an actor; the verify order is the security property (bad EdDSA = hard abort, then Team-ID codesign match, then move current app to Trash for recoverable undo). |
| 151 | [151-xcodegen-iconcomposer-dot-icon-app-icon.md](cookbook/151-xcodegen-iconcomposer-dot-icon-app-icon.md) | Wire an Icon Composer `.icon` (Xcode 26 Liquid Glass) as the macOS app icon via xcodegen: reference it with `type: file` (else the layers get added as loose resources) and set `ASSETCATALOG_COMPILER_APPICON_NAME` to the `.icon` base name; a missing icon is only an actool warning, so a green build won't catch it. |
| 152 | [152-macos-privileged-subprocess-fail-detection-root-not-fda.md](cookbook/152-macos-privileged-subprocess-fail-detection-root-not-fda.md) | Detect a root-only CLI's privilege failure instead of a false "live": `fs_usage` launches then exits to stderr, so `terminationHandler` classifies `.privilegesRequired` and the store only flips to `.running` on the first parsed event; `fs_usage` needs uid 0 (Full Disk Access is not a substitute). |
| 153 | [153-macos-destructive-action-confirmation-ladder.md](cookbook/153-macos-destructive-action-confirmation-ladder.md) | Gate an irreversible action with a three-rung ladder (one-time `@AppStorage` first-run warning that doubles as its confirm, per-run count-confirm forever, then the OS dialog) presented on the content body not a toolbar item (#147); wiring Stop as cancel-the-consumer is itself the bug — fixed design in #154 — and report every terminal state honestly. |
| 154 | [154-asyncstream-stop-cancel-producer-not-consumer.md](cookbook/154-asyncstream-stop-cancel-producer-not-consumer.md) | Stop a streamed run by cancelling the PRODUCER via a returned `Run` handle, not the consumer Task (which kills the stream mid-wind-down so a destructive engine commits after the "stopped" report and the final `.finished` is lost); flip a visible `.stopping` phase and keep folding events to the genuine terminal event. Companion to #153. |
