# Swift/SwiftUI Patterns Cookbook

**Extracted from working production code across 15+ projects.**
**Last updated: 2026-07-02 (#156).** Change history lives in git — this file is a *router*: grep a keyword or scan the filenames, then read only the matching `cookbook/NNN-*.md`. Never load the whole index.

---

> **MANDATORY STANDARD — READ FIRST**
>
> Every macOS app MUST use the **App Shell Standard** below. This means:
> - `HSplitView` for panes (NOT `NavigationSplitView` — no Tahoe frosted sidebars)
> - `FCPToolbarButtonStyle` for toolbar buttons (NOT default round/capsule buttons)
> - `.windowStyle(.hiddenTitleBar)` + `.preferredColorScheme(.dark)` + `.toolbarRole(.editor)`
> - Custom dark `Theme` struct for consistent colors
>
> **Existing apps not using this pattern should be migrated.** When starting work on
> any macOS app, check whether it follows the App Shell Standard. If it doesn't,
> migrating to this standard is a prerequisite before adding new features.
>
> Reference implementation: `1-macOS/Penumbra/` (pre-Tahoe SDK toolbar)
> Titlebar injection reference: `1-macOS/VAM/` (macOS 26 SDK — no system chrome)

---

## Patterns Index

Each pattern lives in `cookbook/`. Read the relevant file when a pattern is needed.

| # | File | Pattern (grep a keyword here or the file's Tags line; read the file for detail) |
|---|------|---------|
| 0 | [00-app-shell.md](cookbook/00-app-shell.md) | MANDATORY app-shell baseline: entry point, Theme struct, FCPToolbarButtonStyle, HSplitView panes, plus migration checklist for every macOS app. |
| 1 | [01-window-layouts.md](cookbook/01-window-layouts.md) | Window layout variants: NavigationSplitView vs HSplitView, multi-window, autosaved divider positions, NSTableView embedding. |
| 2 | [02-layout-templates.md](cookbook/02-layout-templates.md) | Five reusable layout archetypes: Browser, Editor, Organizer, Dual Viewer, Workspace. |
| 3 | [03-appkit-controls.md](cookbook/03-appkit-controls.md) | SwiftUI wrappers for AppKit controls: NSButton, NSCheckbox, NSPopUpButton, NSSegmentedControl, NSSlider, NSTextField. |
| 4 | [04-swiftui-performance.md](cookbook/04-swiftui-performance.md) | SwiftUI perf: view diffing checkpoints, @ViewBuilder anti-pattern, .equatable(), fixing image-cache flash on redraw. |
| 5 | [05-export-file-dialogs.md](cookbook/05-export-file-dialogs.md) | File dialogs: NSSavePanel/NSOpenPanel, async panels, progress tracking, security-scoped bookmarks, .fileImporter. |
| 6 | [06-app-lifecycle.md](cookbook/06-app-lifecycle.md) | App lifecycle and init order: @main entry, .task init sequencing, scenePhase, Manager.configure(), FolderManager. |
| 7 | [07-timecode-typography.md](cookbook/07-timecode-typography.md) | Timecode displays with SF Pro `.monospacedDigit()` and a weight hierarchy for stable, readable readouts. |
| 8 | [08-keyboard-shortcuts.md](cookbook/08-keyboard-shortcuts.md) | Four keyboard-shortcut tiers: SwiftUI Commands, .onKeyPress, NSEvent monitor, custom manager. |
| 9 | [09-context-menus.md](cookbook/09-context-menus.md) | Context menus: basic, conditional, extracted @ViewBuilder, and NSMenuDelegate for NSTableView rows. |
| 10 | [10-selection-models.md](cookbook/10-selection-models.md) | Selection models: single, multi Set<ID>, grid, NSTableView sync, cross-pane, two-level hierarchical. |
| 11 | [11-drag-drop.md](cookbook/11-drag-drop.md) | Drag and drop: .onDrop typed handlers, concurrent TaskGroup imports, internal reorder, NSTableView/NSView drops. |
| 12 | [12-activity-progress.md](cookbook/12-activity-progress.md) | Progress UI: status bar, inline, determinate+cancel, multi-level, metrics panel, floating, phase readouts. |
| 13 | [13-workspace-switching.md](cookbook/13-workspace-switching.md) | Workspace/view-mode switching: mode toggle, tool picker, sidebar-driven, @AppStorage persistence, nested sub-modes. |
| 14 | [14-subprocess-url.md](cookbook/14-subprocess-url.md) | Subprocess file paths: `URL.path()` pitfall and keeping security-scoped access alive across async pipelines. |
| 15 | [15-native-video-analysis.md](cookbook/15-native-video-analysis.md) | Native video analysis: shot/scene detection via Y-plane histograms and motion scoring via frame differencing. |
| 16 | [16-sparkle-auto-updates.md](cookbook/16-sparkle-auto-updates.md) | Sparkle auto-update integration checklist: INFOPLIST_KEY_ gotcha, empty-appcast fix, minimal updater setup. |
| 17 | [17-thread-safe-rendering.md](cookbook/17-thread-safe-rendering.md) | Thread-safe offscreen rendering: use NSBitmapImageRep inside TaskGroup for concurrent image generation. |
| 18 | [18-pipeline-extraction.md](cookbook/18-pipeline-extraction.md) | Extract shared processing logic into a reusable pipeline with caller-owned I/O. |
| 19 | [19-swift6-concurrency.md](cookbook/19-swift6-concurrency.md) | Swift 6 concurrency: pair @MainActor with @Observable to enforce main-thread mutation at the class level. |
| 20 | [20-actor-reentrancy.md](cookbook/20-actor-reentrancy.md) | When TOCTOU is NOT possible: a synchronous, uninterrupted sequence can't race, so no re-check is needed; contrast with actor await suspension points. |
| 21 | [21-anti-patterns.md](cookbook/21-anti-patterns.md) | Catalog of common Swift/SwiftUI mistakes to avoid. |
| 22 | [22-debounced-cifilter.md](cookbook/22-debounced-cifilter.md) | Live CIFilter preview with debounced updates and a SwiftUI fallback cache to keep the last good image while recomputing. |
| 23 | [23-z-order-overlay.md](cookbook/23-z-order-overlay.md) | Out-of-bounds visual feedback via z-order overlays instead of badges. |
| 24 | [24-web-dev-patterns.md](cookbook/24-web-dev-patterns.md) | Web patterns: Jinja2 data injection, ES-module dependency injection, and a shared state module. |
| 25 | [25-extension-file-splitting.md](cookbook/25-extension-file-splitting.md) | Split large Swift files via extensions; handle access-level fixes and pick a splitting strategy per file type. |
| 26 | [26-launchd-node-service.md](cookbook/26-launchd-node-service.md) | Run a Node service under launchd with KeepAlive and scheduled tasks; install/uninstall and the Apple Silicon PATH gotcha. |
| 27 | [27-timelineview-elapsed.md](cookbook/27-timelineview-elapsed.md) | TimelineView(.periodic) for elapsed/remaining readouts, replacing Timer + objectWillChange. |
| 28 | [28-commandgroup-observation.md](cookbook/28-commandgroup-observation.md) | Wrap a Commands struct with @ObservedObject so menu items update live from @Published state. |
| 29 | [29-disk-space-preflight.md](cookbook/29-disk-space-preflight.md) | Preflight free-space check via URLResourceKey volume APIs, with same-volume detection and named-volume errors. |
| 30 | [30-volume-custom-icons.md](cookbook/30-volume-custom-icons.md) | Set a volume-root icon with a two-step write (`.VolumeIcon.icns` + `com.apple.FinderInfo` xattr + `utimes`) because `NSWorkspace.setIcon` is broken on volume roots since macOS 13.1. |
| 31 | [31-volume-enumeration.md](cookbook/31-volume-enumeration.md) | External-drive heuristic: `volumeIsRemovableKey` is misleading; correct filter is `!isInternal && !isRootFileSystem && !isLikelyDiskImage`. |
| 32 | [32-nsworkspace-asyncstream.md](cookbook/32-nsworkspace-asyncstream.md) | Bridge `NSWorkspace` mount/unmount notifications to `AsyncStream<MountEvent>` in an actor; handle observer ownership and termination cleanup. |
| 33 | [33-managed-developer-id.md](cookbook/33-managed-developer-id.md) | Xcode Archive to Direct Distribution with a server-side managed Developer ID cert; CLI `notarytool` pipeline as appendix. |
| 34 | [34-xcodeproj-clone-rename.md](cookbook/34-xcodeproj-clone-rename.md) | Clone an `.xcodeproj` to reuse toolchain bundling/entitlements/build phases via `cp -R` + sed; covers macOS sed, display names with spaces, xcuserdata gotchas. |
| 35 | [35-asyncstream-bounded-fanout.md](cookbook/35-asyncstream-bounded-fanout.md) | `withTaskGroup` drain-and-refill: stream results from thousands of async ops under a bounded concurrency cap; why naive `for url in files { group.addTask }` is wrong. |
| 36 | [36-fast-preview-heavy-commit.md](cookbook/36-fast-preview-heavy-commit.md) | Split render into a sync in-memory `preview() -> NSImage` (~15ms) vs async `render() -> Data` full pipeline (~300ms); sync `.onChange` for live sliders, no Task/cancel gymnastics. |
| 37 | [37-effective-source-fallback.md](cookbook/37-effective-source-fallback.md) | `pending ?? cached` editor binding lets a saved asset be re-tweaked without re-importing; includes dirty-detection commit gate and self-healing for vanished cache files. |
| 38 | [38-destructive-copy-guard.md](cookbook/38-destructive-copy-guard.md) | Early-return when `sourceURL.standardizedFileURL == destURL.standardizedFileURL` before `removeItem → copyItem`, so `src == dest` doesn't delete the file you're about to read. |
| 39 | [39-design-tokens.md](cookbook/39-design-tokens.md) | App-wide visual tokens: semantic modular type scale, 8pt spacing grid with internal≤external rule, SF Symbol weight/scale, corner radii, and CSS `clamp()` fluid type for web. |
| 40 | [40-spaces-plist-backend.md](cookbook/40-spaces-plist-backend.md) | Public Spaces backend by parsing `com.apple.spaces` plist for real Space UUIDs + current-Space per monitor without private CGS SPI. |
| 41 | [41-web-hero-floating-icons.md](cookbook/41-web-hero-floating-icons.md) | Fill an empty web hero by scattering app icons around the headline: absolute `.float-icon`, rotation, coprime drift animations; use `filter: drop-shadow` not box-shadow; `aria-hidden`/`alt=""`. |
| 42 | [42-web-native-dialog-lightbox.md](cookbook/42-web-native-dialog-lightbox.md) | Image lightbox via a single shared native `<dialog>`; JS swaps `img.src`; free focus trap, ESC-close, `::backdrop` blur; a11y attrs added at runtime, not in HTML. |
| 43 | [43-subprocess-fire-and-collect.md](cookbook/43-subprocess-fire-and-collect.md) | Short-lived subprocess to one stdout blob via `waitUntilExit()` + `readDataToEndOfFile()` in `Task.detached`, avoiding the tail-byte race of `readabilityHandler`; always drain stderr. |
| 44 | [44-inherited-project-dead-code-sweep.md](cookbook/44-inherited-project-dead-code-sweep.md) | Bulk-remove dead Swift from a forked project: `sed -i '' "/file.swift/d" project.pbxproj` drops all 4 refs at once (filename in every comment); `rm` on disk, close Xcode, escape regex metachars. |
| 45 | [45-macos-firmlink-canonical-path.md](cookbook/45-macos-firmlink-canonical-path.md) | `/var` vs `/private/var` URL-equality gotcha: `resolvingSymlinksInPath()` is a no-op on APFS firmlinks but FileManager returns resolved URLs; fix via `URLResourceValues.canonicalPath`. |
| 46 | [46-web-php-download-counter.md](cookbook/46-web-php-download-counter.md) | Backendless download counter: `dl.php?app=` regex-validated, increments `counts.json` under `flock(LOCK_EX)`, then 302s to the real file; GET-only counting defeats prefetch/`curl -I`, bot UA filter. |
| 47 | [47-xcodegen-swiftterm-setup.md](cookbook/47-xcodegen-swiftterm-setup.md) | xcodegen + SwiftTerm: TTF fonts need postBuildScripts + ATSApplicationFontsPath; put UIDesignRequiresCompatibility in info.properties; disambiguate SwiftUI.Color vs SwiftTerm.Color. |
| 48 | [48-swiftterm-output-monitor.md](cookbook/48-swiftterm-output-monitor.md) | SwiftTerm output monitoring: override `dataReceived(slice:)` in a `LocalProcessTerminalView` subclass (delegate interception breaks internals). |
| 49 | [49-web-php-feedback-form-with-admin.md](cookbook/49-web-php-feedback-form-with-admin.md) | PHP flat-file feedback form with public/private PII split (`public.json` vs `.htaccess`-denied log), join-at-read, `textContent` XSS discipline, honeypot + salted-IP rate limit. |
| 50 | [50-detachable-windows.md](cookbook/50-detachable-windows.md) | Detachable windows via `WindowGroup(for: UUID.self)` + a `[UUID: Manager]` `WindowManager`; `detachSession` moves the item and `openWindow(value:)` opens the typed scene. |
| 51 | [51-swiftui-recursive-split-pane.md](cookbook/51-swiftui-recursive-split-pane.md) | Recursive split panes: `indirect enum SplitNode` tree with value-semantic insert/remove, recursing via `HSplitView`/`VSplitView`. |
| 52 | [52-appendingpathcomponent-fs-probe.md](cookbook/52-appendingpathcomponent-fs-probe.md) | Single-arg `appendingPathComponent(_:)` fs-probes to set `hasDirectoryPath`, so a path resolved before vs after the dir exists compares unequal (breaks Set<URL>/caches). |
| 53 | [53-web-grid-masonry-jsspan.md](cookbook/53-web-grid-masonry-jsspan.md) | Masonry with row-first order via CSS Grid + JS `grid-row-end: span N`, span from aspect ratio on img load, unlike `column-count` which fills columns first; resize debounced 150ms. |
| 54 | [54-web-portrait-aware-uniform-grid.md](cookbook/54-web-portrait-aware-uniform-grid.md) | Portrait-aware uniform gallery: default `aspect-ratio: 3/2`, JS tags `.portrait` when naturalHeight>naturalWidth to `grid-row: span 2` + `2/3`, `grid-auto-flow: dense` fills gaps. |
| 55 | [55-web-preview-suffix-deploy.md](cookbook/55-web-preview-suffix-deploy.md) | Staging-less preview on shared hosting: deploy each page as `*-new.html` beside the live one with preview-internal hrefs; ship by renaming + sitewide sed to strip `-new`; same origin shares assets. |
| 56 | [56-web-static-admin-template-roundtrip.md](cookbook/56-web-static-admin-template-roundtrip.md) | Prove `parse(leaf) → render(manifest) == leaf` byte-for-byte before writing any admin mutation endpoint: `{{PLACEHOLDER}}` template, pure `strtr` renderer, regex parser, CLI diff harness. |
| 57 | [57-commandgroup-saveitem-cmdw-override.md](cookbook/57-commandgroup-saveitem-cmdw-override.md) | Override ⌘W in multi-tab SwiftUI via `CommandGroup(replacing: .saveItem)` (only placement beating File > Close Window) with a never-`.disabled` button. |
| 58 | [58-xcodegen-postbuildscripts-sandbox.md](cookbook/58-xcodegen-postbuildscripts-sandbox.md) | xcodegen resource-copy script phase needs both `inputFiles:` (sandbox grant) and `outputFiles:` (incremental skip). |
| 59 | [59-decodable-only-codingkeys.md](cookbook/59-decodable-only-codingkeys.md) | A `Decodable`-only type with a hand-rolled `init(from:)` synthesizes no `CodingKeys` (SE-0166 only generates them alongside a synthesized init/encode), failing "cannot find CodingKeys". |
| 60 | [60-closure-bridged-appkit.md](cookbook/60-closure-bridged-appkit.md) | Trigger AppKit (window close, NSWorkspace deep-link, NSAlert) from a model-layer `@MainActor` ObservableObject by injecting closures instead of holding `weak var window`. |
| 61 | [61-probe-classify-not-catch-all.md](cookbook/61-probe-classify-not-catch-all.md) | TCC/FDA pre-flight without false positives: `enum AccessProbeResult` driven by `fileExists(atPath:isDirectory:)` then typed `CocoaError.fileReadNoPermission`; only `.permissionDenied` gates FDA. |
| 62 | [62-web-prefers-color-scheme-token-theme.md](cookbook/62-web-prefers-color-scheme-token-theme.md) | Web auto dark theme by overriding ~20 design tokens in one `@media (prefers-color-scheme: dark)` block plus `color-scheme: light dark`. |
| 63 | [63-macos-favicon-quicklook-glyph-ico.md](cookbook/63-macos-favicon-quicklook-glyph-ico.md) | macOS favicon gen: ImageMagick silently emits near-blank PNGs for SVGs with clipPath/gradients; render master with `qlmanage -t -s 1024`; author separate glyph-only 16px SVG, pack multi-size `.ico`. |
| 64 | [64-carbon-global-hotkey-toggle.md](cookbook/64-carbon-global-hotkey-toggle.md) | Permission-free system-wide hotkey via Carbon `RegisterEventHotKey` (no Accessibility/Input-Monitoring, catches synthesized keys). |
| 65 | [65-cursor-anchored-nspanel-hud.md](cookbook/65-cursor-anchored-nspanel-hud.md) | Cursor-anchored non-activating `NSPanel` HUD: `[.borderless,.nonactivatingPanel]` + `orderFrontRegardless()`, `acceptsFirstMouse` subclass, clamp to cursor screen `visibleFrame`. |
| 66 | [66-macos-permission-free-disk-stats.md](cookbook/66-macos-permission-free-disk-stats.md) | Permission-free disk stats without `fs_usage` (needs root/FDA): capacity via `statfs("/")`, throughput by summing each `IOBlockStorageDriver` `Bytes (Read/Write)` as per-tick deltas. |
| 67 | [67-swiftui-jitter-free-numeric-readout.md](cookbook/67-swiftui-jitter-free-numeric-readout.md) | Stop a live number from resizing its container: `.monospacedDigit()` equalizes digit width but not count, so stack a hidden `Text(widestValue)` behind it in a `ZStack`. |
| 68 | [68-macos-permission-free-network-throughput.md](cookbook/68-macos-permission-free-network-throughput.md) | Permission-free network throughput via `getifaddrs` (no BPF): cumulative counters live only on `AF_LINK` records (`ifi_ibytes/ifi_obytes`) — skip IP records or double-count. |
| 69 | [69-macos-iokit-power-sources-battery.md](cookbook/69-macos-iokit-power-sources-battery.md) | Permission-free battery via `IOKit.ps`: ownership follows prefix — `IOPSCopy…` uses `.takeRetainedValue()`, `IOPSGetPowerSourceDescription` uses `.takeUnretainedValue()`. |
| 70 | [70-swiftui-data-driven-tile-strip.md](cookbook/70-swiftui-data-driven-tile-strip.md) | Cure SwiftUI "unable to type-check this expression in reasonable time" from N hardcoded subviews by rendering a `ForEach` over a homogeneous `[Descriptor]` array. |
| 71 | [71-lsuielement-self-managed-settings-window.md](cookbook/71-lsuielement-self-managed-settings-window.md) | In `LSUIElement` apps `showSettingsWindow:` silently no-ops. |
| 72 | [72-scoped-carbon-hotkey-esc-dismiss.md](cookbook/72-scoped-carbon-hotkey-esc-dismiss.md) | Permission-free Esc-to-dismiss for a non-key panel: register bare Escape as a Carbon hotkey only while visible; filter by `EventHotKeyID` in the C callback (Carbon dispatches to all handlers). |
| 73 | [73-verify-hud-without-screen-recording.md](cookbook/73-verify-hud-without-screen-recording.md) | Verify a HUD without Screen-Recording: observe show/hide via `CGWindowListCopyWindowInfo`, drive via `CGEventPost`, measure hotkey capture with a listen-only event tap. |
| 74 | [74-macos-permission-free-load-uptime-topproc.md](cookbook/74-macos-permission-free-load-uptime-topproc.md) | Permission-free load/uptime/top-process: `getloadavg` normalized by `activeProcessorCount`, `sysctlbyname("kern.boottime")` not `ProcessInfo.systemUptime` (drops sleep). |
| 75 | [75-macos-top-processes-by-app.md](cookbook/75-macos-top-processes-by-app.md) | Top processes by CPU/mem/disk grouped by app, two engines: in-process `rusage_info_v4` (`ri_user_time` is mach-time not ns, convert via `mach_timebase_info`) vs `/usr/bin/top` (system-inclusive). |
| 76 | [76-macos-appicon-coregraphics-generator.md](cookbook/76-macos-appicon-coregraphics-generator.md) | Generate `AppIcon.appiconset` (all 10 sizes + Contents.json) from a standalone `swift` Core Graphics script: convert `oklch`→sRGB by hand, render per-size not downscaled. |
| 77 | [77-jsx-prototype-to-png-webkit-playwright.md](cookbook/77-jsx-prototype-to-png-webkit-playwright.md) | Render a Babel-in-browser JSX prototype to per-section PNGs without Chrome: a flat-render harness mounts components in normal flow, then Playwright+WebKit screenshots each section. |
| 78 | [78-swiftui-state-aware-menu-pill.md](cookbook/78-swiftui-state-aware-menu-pill.md) | State-aware header pill, both nav target and switcher: body tap navigates when off the surface but opens the picker when already on it, chevron always opens picker. |
| 79 | [79-swiftui-layoutpriority-active-width-budget.md](cookbook/79-swiftui-layoutpriority-active-width-budget.md) | HStack truncation is a priority contest not a maxWidth ceiling: `maxWidth` is a flexible `0...max` ceiling and greedy Spacers win, so give the readable element higher `.layoutPriority`. |
| 80 | [80-watchconnectivity-phone-watch-state-mirror.md](cookbook/80-watchconnectivity-phone-watch-state-mirror.md) | Mirror full phone→watch state over WatchConnectivity (App Groups DON'T cross devices); use `updateApplicationContext` + eagerly activate `WCSession` at `@main` or cold-launch seed push is dropped. |
| 81 | [81-nonkey-hud-panel-synthetic-paste.md](cookbook/81-nonkey-hud-panel-synthetic-paste.md) | A HUD that synth-pastes into the frontmost app must be NON-KEY (plain `NSPanel` + `FirstMouseHostingView`, `orderFrontRegardless`) or it steals keyboard focus and swallows the ⌘V. |
| 82 | [82-makekey-panel-paste-self-refusal-reactivate.md](cookbook/82-makekey-panel-paste-self-refusal-reactivate.md) | A KEY picker panel that synth-pastes must capture the pre-`makeKey` frontmost app and, only on `wouldPasteIntoSelf`, `target.activate()` + recheck secure input before ⌘V; don't always-reactivate. |
| 83 | [83-calendar-overlap-column-packing.md](cookbook/83-calendar-overlap-column-packing.md) | Lay out overlapping calendar/timeline events: cluster transitively-overlapping runs (cluster end = `max(runningEnd, e.end)`), then first-free-column packing. |
| 84 | [84-swiftdata-detached-model-revalidate-guard.md](cookbook/84-swiftdata-detached-model-revalidate-guard.md) | A row closure reading a persisted `@Model` prop after a bulk delete hard-crashes `EXC_BREAKPOINT` (getter traps on detached model); guard `item.modelContext != nil` FIRST before any persisted read. |
| 85 | [85-phase-aware-subprocess-watchdog.md](cookbook/85-phase-aware-subprocess-watchdog.md) | No-progress subprocess watchdog that won't false-alarm during legitimately-silent phases: pure `watchdogAssessment` verdict with hard-timeout `.hung` checked FIRST, phase flags set at transitions. |
| 86 | [86-single-flight-async-dedup-actor.md](cookbook/86-single-flight-async-dedup-actor.md) | Single-flight async dedup: a sync `guard cache==nil` is worthless across an `await`; hold `inFlight: [Key: Task<V,Never>]` in an actor, store the Task BEFORE awaiting it, clear the slot AFTER save. |
| 87 | [87-swiftui-caret-token-insertion-nstextfield.md](cookbook/87-swiftui-caret-token-insertion-nstextfield.md) | Insert a token at a SwiftUI text field's caret: wrap `NSTextField` in `NSViewRepresentable` + a weak-held controller. |
| 88 | [88-detect-fail2ban-ban-vs-auth-failure.md](cookbook/88-detect-fail2ban-ban-vs-auth-failure.md) | Distinguish a fail2ban IP-ban from a real SSH/SFTP auth failure by WHERE the connection dies: `Permission denied` = rejected credential; `Connection closed before password:` = pre-auth ban. |
| 89 | [89-swiftui-native-toolbar-tahoe-glass-optout.md](cookbook/89-swiftui-native-toolbar-tahoe-glass-optout.md) | Native `.toolbar` items grow Liquid Glass capsules on macOS 26 SDK; opt out via `UIDesignRequiresCompatibility=true` in a base Info.plist (see #155 for the allowlist gotcha). |
| 90 | [90-macos-offmain-file-logger-bound-rotate.md](cookbook/90-macos-offmain-file-logger-bound-rotate.md) | Mirror an `@MainActor @Observable` console feed to disk off-main: a `Sendable LogFileWriter` on its own serial `DispatchQueue`, `FileHandle.seekToEnd`+`write`, rotate at byte ceiling. |
| 91 | [91-detect-concurrent-cli-sessions-shared-checkout.md](cookbook/91-detect-concurrent-cli-sessions-shared-checkout.md) | Detect two CLI agents sharing one git checkout (shared HEAD → wrong-branch commits): `pgrep -x` → each PID's cwd via `lsof -d cwd -Fn` → `git rev-parse --show-toplevel`, group by toplevel, warn if 2+. |
| 92 | [92-swiftui-greedy-clear-spacer-header-align.md](cookbook/92-swiftui-greedy-clear-spacer-header-align.md) | A width-only `Color.clear`/`Spacer` is greedy in the unconstrained axis and balloons a header row tall. |
| 93 | [93-swiftui-pure-sort-feed-offmain-prewarm.md](cookbook/93-swiftui-pure-sort-feed-offmain-prewarm.md) | A sort comparator that resolves its key via file I/O inside `body` hangs the window. |
| 94 | [94-quicktime-lossless-atom-date-patch.md](cookbook/94-quicktime-lossless-atom-date-patch.md) | Patch embedded QuickTime/ISO-BMFF `creation_time` (`mvhd`/`tkhd`/`mdhd`) in pure Swift via same-length byte overwrites. |
| 95 | [95-macos-quicklook-thumbnail-hybrid.md](cookbook/95-macos-quicklook-thumbnail-hybrid.md) | Video thumbnails: try `QLThumbnailGenerator` first (out-of-process decode + system cache), FFmpeg fallback only on a throw. |
| 96 | [96-macos-app-indexer-cryptex-symlink.md](cookbook/96-macos-app-indexer-cryptex-symlink.md) | `contentsOfDirectory(at:includingPropertiesForKeys:)` silently drops symlinked Cryptex system apps (Safari) resolving `.isDirectoryKey`. |
| 97 | [97-macos-agent-app-nslog-stderr-capture.md](cookbook/97-macos-agent-app-nslog-stderr-capture.md) | `NSLog` from an `open`-launched app is invisible to `log show process==` (absence misreads as clean). |
| 98 | [98-swiftui-foreach-id-index-stale-rows.md](cookbook/98-swiftui-foreach-id-index-stale-rows.md) | List renders stale rows after filtering because `.id(index)` on a `ForEach` child overrides `id: \.element.id`, re-keying by position so SwiftUI reuses the cached view tree. |
| 99 | [99-lftp-netrc-multisite-deploy.md](cookbook/99-lftp-netrc-multisite-deploy.md) | Multi-site `lftp` SFTP deploy with secrets out of git: put USER in the URL since ssh ignores `~/.netrc` username, password in chmod-600 `~/.netrc` outside repo. |
| 100 | [100-web-centralized-donation-page.md](cookbook/100-web-centralized-donation-page.md) | One `donate.html?app=<slug>` hub self-personalizes from a JS `APPS` map (paid apps absent → generic); single source of provider URL; never nest a donate anchor inside a card `<a>`. |
| 101 | [101-swiftui-settings-search-highlight-focus.md](cookbook/101-swiftui-settings-search-highlight-focus.md) | Settings-search result→control pulse must fire on BOTH `onAppear` and `onChange`; ⌘F focuses `.searchable` via hidden Button walking to `NSSearchField`; `scrollTo` row-level in a Form. |
| 102 | [102-swiftui-inapp-feedback-spm-shared-endpoint.md](cookbook/102-swiftui-inapp-feedback-spm-shared-endpoint.md) | Drop-in feedback sheet SPM package POSTing to shared `feedback-submit.php`; bridge `@Sendable ()->String?` log hook via `MainActor.assumeIsolated`. |
| 103 | [103-web-roster-single-source-of-truth-json.md](cookbook/103-web-roster-single-source-of-truth-json.md) | One `apps.json` roster read by PHP/JS-fetch/server-rendered HTML replaces 5 drifting copies; per-item flags for differing membership; delete static `<option>`s or JS double-fills. |
| 104 | [104-swiftui-commands-menu-dividers-donate.md](cookbook/104-swiftui-commands-menu-dividers-donate.md) | `CommandGroup(after:)` inserts NO separator and stacks by declaration order → add explicit `Divider()` beside a package command; Donate menu item takes NO ellipsis (browser hand-off). |
| 105 | [105-web-js-injected-nav-footer-partials.md](cookbook/105-web-js-injected-nav-footer-partials.md) | Single-source JS-injected nav/footer partials: markup in one JS file, pages carry empty placeholders + `data-*` per-page params; reserve `min-height` for CLS. |
| 106 | [106-xcodebuild-script-phase-self-cycle.md](cookbook/106-xcodebuild-script-phase-self-cycle.md) | `xcodebuild` "Cycle inside <target>" from an in-place re-sign script listing a bundle file as both input AND output → declare ONLY a `$(DERIVED_FILE_DIR)/*.stamp` output. |
| 107 | [107-swiftui-disabled-control-hittest-fallthrough.md](cookbook/107-swiftui-disabled-control-hittest-fallthrough.md) | `.disabled(true)` removes a control from hit-testing so taps fall THROUGH to an ancestor `.onTapGesture`. |
| 108 | [108-spm-citizenship-menu-consolidation-proving-ground.md](cookbook/108-spm-citizenship-menu-consolidation-proving-ground.md) | Consolidate Feedback/Donate/About menu wiring into one shared SPM `CitizenshipCommands`; migrate the source app FIRST as a proving ground. |
| 109 | [109-dynamic-canbecomekey-optin-hud-verbs.md](cookbook/109-dynamic-canbecomekey-optin-hud-verbs.md) | Non-key HUD that also needs bare-key verbs: gate `canBecomeKey` on dynamic STATE (`keyboardModeEngaged`), not a static `true` (a hosted SwiftUI Button promotes it to key anyway). |
| 110 | [110-macos-settings-pane-deeplink-verify.md](cookbook/110-macos-settings-pane-deeplink-verify.md) | Deep-link a System Settings pane via `x-apple.systempreferences:<bundle-id>`. |
| 111 | [111-swiftui-click-to-record-hotkey-field.md](cookbook/111-swiftui-click-to-record-hotkey-field.md) | Hotkey recorder = make the field itself a `Button` (not a box + separate clipped Record button). |
| 112 | [112-macos-security-scoped-bookmarks.md](cookbook/112-macos-security-scoped-bookmarks.md) | Security-scoped bookmarks to re-open picked files across launches in a SANDBOXED app: persist `Data` from `bookmarkData(.withSecurityScope)`, re-mint on `bookmarkDataIsStale`. |
| 113 | [113-macos-appearance-preference-nsapp-not-preferredcolorscheme.md](cookbook/113-macos-appearance-preference-nsapp-not-preferredcolorscheme.md) | Light/Dark/Match-System picker: `.preferredColorScheme(nil)` does NOT clear a forced `NSWindow.appearance` → set `NSApplication.shared.appearance` instead, don't keep both owners. |
| 114 | [114-macos-runtime-dock-icon-appearance-swap.md](cookbook/114-macos-runtime-dock-icon-appearance-swap.md) | macOS bundle app icon can't vary by appearance (`actool` drops dark renditions, iOS-only) → swap the running Dock tile via `NSApp.applicationIconImage`, driven by KVO. |
| 115 | [115-swiftui-polled-status-seed-init-animate-live.md](cookbook/115-swiftui-polled-status-seed-init-animate-live.md) | Polled permission row: seed `@State` in `init` (`State(initialValue:)`) for no first-frame flash, `withAnimation` only when the polled value actually changes. |
| 116 | [116-swiftui-bare-key-shortcut-textfield-focus-handoff.md](cookbook/116-swiftui-bare-key-shortcut-textfield-focus-handoff.md) | A bare-letter `.keyboardShortcut([])` is swallowed by a focused `TextField` → clear `@FocusState` on the transition that reveals the action. |
| 117 | [117-swiftui-imagerenderer-appearance-matrix-test.md](cookbook/117-swiftui-imagerenderer-appearance-matrix-test.md) | Verify a theme actually PAINTS tokens with an `ImageRenderer` snapshot matrix over {light,dark}×presets; headless has ~0.035 gamma bias so lead with bias-immune checks (luminance polarity, WCAG). |
| 118 | [118-swift6-nonsendable-resource-to-actor-sending.md](cookbook/118-swift6-nonsendable-resource-to-actor-sending.md) | Hand a non-`Sendable` resource to a Swift 6 `actor` via `sending` (ownership moves), observe it only through the actor returning `Sendable` values. |
| 119 | [119-macos-app-rebrand-xcodegen-engine-name-split.md](cookbook/119-macos-app-rebrand-xcodegen-engine-name-split.md) | Rename an xcodegen app whose name derives from its wrapped engine (YTdl vs `yt-dlp`): case-sensitive `s/YTdl/Magpie/` spares engine tokens; `git mv` folders, rewrite `project.yml`, verify built plist. |
| 120 | [120-swift-crypto-kdf-cryptokit-type-identity.md](cookbook/120-swift-crypto-kdf-cryptokit-type-identity.md) | Add Scrypt/PBKDF2 via swift-crypto: never mix `import CryptoKit` with `import Crypto` (two same-named `SymmetricKey` types), link both `Crypto`+`_CryptoExtras`. |
| 121 | [121-web-clone-retint-single-app-site.md](cookbook/121-web-clone-retint-single-app-site.md) | Clone-and-retint a sibling static app site: clone structure+color but SCRUB donor facts (ARCHS, bundle-id, price, copy); grep both casings; retint in the per-site CSS not `site.css`. |
| 122 | [122-codable-tolerant-migration-no-wipe.md](cookbook/122-codable-tolerant-migration-no-wipe.md) | Hand-written `init(from:)` so a one-field drift can't wipe a persisted `[Job]`: synthesized decode is all-or-nothing per array. |
| 123 | [123-draggable-nonactivating-nspanel.md](cookbook/123-draggable-nonactivating-nspanel.md) | Make a non-activating `NSPanel` draggable: `isMovableByWindowBackground`, route placement through one `place(rect)` so `windowDidMove` ignores self-moves. |
| 124 | [124-swift-benchmark-under-O-allocation-free-folded-scan.md](cookbook/124-swift-benchmark-under-O-allocation-free-folded-scan.md) | Benchmark hot Swift under `-O` not Debug (~18x penalty from bounds-checks/ARC/no-inline); override `SWIFT_OPTIMIZATION_LEVEL=-O` keeping Debug config so `@testable` works. |
| 125 | [125-swiftui-destination-popup-menu.md](cookbook/125-swiftui-destination-popup-menu.md) | Save-location chooser as a `Menu` (not `Picker`) whose label IS the current choice plus a "Choose…" item opening `NSOpenPanel`; Picker can't host a panel action; render your own checkmark. |
| 126 | [126-closure-struct-dependency-injection.md](cookbook/126-closure-struct-dependency-injection.md) | Dependency injection as a struct of closures (`.live`/`.inert`) instead of a protocol; tests mutate one field, `.inert` doubles as the safe `#Preview` env; optional async sibling for blocking work. |
| 127 | [127-toctou-identity-verify-before-destructive-op.md](cookbook/127-toctou-identity-verify-before-destructive-op.md) | Close the scan→act TOCTOU gap: capture `(st_dev, st_ino)` via `lstat` at scan time, re-check immediately before trashing, classify matches/mismatch/missingNow/unverifiable. |
| 128 | [128-macos-dock-icon-iconservices-cache-refresh.md](cookbook/128-macos-dock-icon-iconservices-cache-refresh.md) | Generic Dock icon on a fresh build is the IconServices cache, not the build: prove `AppIcon.icns` is in the bundle, then `lsregister -f` + delete the iconservices store. |
| 129 | [129-swiftui-focus-preserving-reveal-securefield.md](cookbook/129-swiftui-focus-preserving-reveal-securefield.md) | Reveal/hide password without losing focus or text: keep `SecureField` and `TextField` both alive in a `ZStack` sharing one binding+`@FocusState`, toggle `.opacity`+`.allowsHitTesting`. |
| 130 | [130-swiftui-unsaved-edits-selection-switch-prompt.md](cookbook/130-swiftui-unsaved-edits-selection-switch-prompt.md) | Unsaved-changes prompt on `List(selection:)` switch: no `shouldSelect` veto exists, so synchronously write the selection back to A to preserve the detail draft, stash B in `pendingSwitchID`. |
| 131 | [131-swiftui-pro-feature-gating-progate.md](cookbook/131-swiftui-pro-feature-gating-progate.md) | Freemium gating off one `@Observable .shared` `isPro`: `.proGated` view-swap to a LockedFeaturePanel (gated view never instantiated) or `ProGate.allow` route-to-upsell; never delete backing data. |
| 132 | [132-strip-php-before-html-parser.md](cookbook/132-strip-php-before-html-parser.md) | Strip `<?php …?>` before an HTML parser or it emits PHP source as data nodes; regex `<\?.*?(?:\?>\\\|\Z)` with `re.DOTALL`, non-greedy, replace with a space — anchor to `?>` OR end-of-file since pure-PHP files omit the closing tag. |
| 133 | [133-swiftui-menustyle-button-pill-toolbar.md](cookbook/133-swiftui-menustyle-button-pill-toolbar.md) | Labeled on/off pill for a toolbar `Menu`: `.labelStyle(.titleAndIcon)` shows the name, `.menuStyle(.button)` lets a custom `ButtonStyle` replace the pull-down bezel (no double background). |
| 134 | [134-ffmpeg-author-verify-timecode-stream-copy.md](cookbook/134-ffmpeg-author-verify-timecode-stream-copy.md) | Author+verify a start timecode on a lossless ffmpeg `-c copy` trim: `-ss` before `-i`, `-timecode HH:MM:SS:FF`; verify via the `tmcd` STREAM tag. |
| 135 | [135-fail-open-proxy-status-codes.md](cookbook/135-fail-open-proxy-status-codes.md) | Fail-open status codes for a key-hiding license relay: partition HTTP codes into VERDICT (200/400/404, upstream's verbatim reply) vs FAIL-OPEN (proxy-originated). |
| 136 | [136-swiftpm-metal-shader-embedded-source.md](cookbook/136-swiftpm-metal-shader-embedded-source.md) | Metal shader in a SwiftPM package: `makeDefaultLibrary()` has no main bundle and `makeDefaultLibrary(bundle: .module)` won't compile from a lone `.metal`. |
| 137 | [137-swift-subprocess-strings-line-cap-deadlock.md](cookbook/137-swift-subprocess-strings-line-cap-deadlock.md) | swift-subprocess `.strings()` deadlocks on a huge newline-free line: default `.maxLineLength(128KB)` stops draining the pipe, child blocks on `write()`; pass `.unbounded` on both stdout+stderr. |
| 138 | [138-stale-build-masquerading-as-bug.md](cookbook/138-stale-build-masquerading-as-bug.md) | A bug you can't reproduce for code you already fixed? Check the running binary's build time first: out-of-band apps aren't replaced by a Debug build+green tests. |
| 139 | [139-ax-drive-swiftui-settings-form-verify.md](cookbook/139-ax-drive-swiftui-settings-form-verify.md) | Drive & verify a SwiftUI Settings Form via Accessibility API: focus with `kAXFocusedAttribute`, enter text by PASTING (Cmd-V) not `kAXValueAttribute`, assert submit button's `kAXEnabledAttribute`. |
| 140 | [140-xcodegen-folder-reference-vendor-cli-tool.md](cookbook/140-xcodegen-folder-reference-vendor-cli-tool.md) | Vendor an opaque CLI + support tree into an `.app` at `Contents/Resources/` via an xcodegen `type: folder` reference (copies verbatim, no run-script/sandbox). |
| 141 | [141-exiftool-geotag-end-to-end-test-no-fixtures.md](cookbook/141-exiftool-geotag-end-to-end-test-no-fixtures.md) | Test ExifTool `-geotag` end-to-end with no binary fixtures: generate JPEG at runtime, defeat the timezone trap with a wide ±16h single-coordinate GPX. |
| 142 | [142-xcodegen-test-target-team-id-dyld.md](cookbook/142-xcodegen-test-target-team-id-dyld.md) | dyld "different Team IDs" when an xcodegen `.xctest` loads into its host app: give the test target the SAME xcconfig as the app plus `CODE_SIGN_STYLE: Automatic`. |
| 143 | [143-observable-mainactor-nsworkspace-mount-monitor.md](cookbook/143-observable-mainactor-nsworkspace-mount-monitor.md) | Live volume mount/unmount monitoring in an `@Observable @MainActor` model: mark observer-token storage `nonisolated(unsafe)` so nonisolated `deinit` can `removeObserver`. |
| 144 | [144-elicit-verification-verdicts-corrupt-output-and-couldnt-run.md](cookbook/144-elicit-verification-verdicts-corrupt-output-and-couldnt-run.md) | Force each verdict of a verify ladder: a 1-frame `-c copy` yields a deterministic ✗, and `chmod 000` on the source forces the ⚠ couldn't-run state. |
| 145 | [145-asyncstream-reducer-run-sheet.md](cookbook/145-asyncstream-reducer-run-sheet.md) | Consumer counterpart to #35: fold an `AsyncThrowingStream` into `@Observable` fields via a pure `apply(_:)` reducer on a MainActor Task, shown as a run `.sheet`. |
| 146 | [146-caffeinate-long-xcodebuild-test-runs-idle-sleep-deadlock.md](cookbook/146-caffeinate-long-xcodebuild-test-runs-idle-sleep-deadlock.md) | A long `xcodebuild test` that "hangs" for hours is the Mac idle-sleeping and SIGKILLing a child `Process` parked in `waitUntilExit()`. |
| 147 | [147-swiftui-present-modal-from-body-not-toolbar-item.md](cookbook/147-swiftui-present-modal-from-body-not-toolbar-item.md) | A `.alert`/`.confirmationDialog`/`.sheet` on a `ToolbarItem` soft-locks the window (dims, no chrome) — toolbar items live in titlebar-accessory context. |
| 148 | [148-verify-web-video-export-webkit-mediarecorder.md](cookbook/148-verify-web-video-export-webkit-mediarecorder.md) | Verify a web `MediaRecorder`/`captureStream` export headless with Playwright WebKit: probe `isTypeSupported`, capture `download` via `waitForEvent`, trust magic bytes. |
| 149 | [149-macos-hermetic-download-codesign-selfreplace-test.md](cookbook/149-macos-hermetic-download-codesign-selfreplace-test.md) | Test a macOS download→codesign-check→self-replace flow hermetically: `URLSession.download(from:)` on `file://`, ad-hoc `codesign -s -`, `ditto -c -k` to preserve signature across zip. |
| 150 | [150-sparkle-eddsa-cryptokit-silent-selfupdate.md](cookbook/150-sparkle-eddsa-cryptokit-silent-selfupdate.md) | Verify a Sparkle appcast with plain CryptoKit (no Sparkle.framework): `Curve25519.Signing.isValidSignature` over `mmap`'d bytes in an actor. |
| 151 | [151-xcodegen-iconcomposer-dot-icon-app-icon.md](cookbook/151-xcodegen-iconcomposer-dot-icon-app-icon.md) | Wire an Icon Composer `.icon` (Xcode 26 Liquid Glass) as the app icon via xcodegen: reference with `type: file` (else layers become loose resources), set `ASSETCATALOG_COMPILER_APPICON_NAME`. |
| 152 | [152-macos-privileged-subprocess-fail-detection-root-not-fda.md](cookbook/152-macos-privileged-subprocess-fail-detection-root-not-fda.md) | Detect a root-only CLI's privilege failure instead of a false "live": `fs_usage` launches then exits to stderr, `terminationHandler` classifies `.privilegesRequired`. |
| 153 | [153-macos-destructive-action-confirmation-ladder.md](cookbook/153-macos-destructive-action-confirmation-ladder.md) | Three-rung destructive-action ladder: one-time `@AppStorage` warning that doubles as confirm, per-run count-confirm, then OS dialog; present on content body not a toolbar item (#147). |
| 154 | [154-asyncstream-stop-cancel-producer-not-consumer.md](cookbook/154-asyncstream-stop-cancel-producer-not-consumer.md) | Stop a streamed run by cancelling the PRODUCER via a returned `Run` handle, not the consumer Task (kills the stream mid-wind-down so a destructive engine commits after the "stopped" report). |
| 155 | [155-web-php-card-media-shot-or-icon-fallback.md](cookbook/155-web-php-card-media-shot-or-icon-fallback.md) | Product card media self-upgrades: `is_file()` checks for a hero screenshot, falls back to the icon tile if absent — dropping a screenshot in needs no code change. |
| 156 | [156-xcodegen-infoplist-allowlist.md](cookbook/156-xcodegen-infoplist-allowlist.md) | Canonical home for the `INFOPLIST_KEY_*` allowlist gotcha (non-Apple keys silently drop under `GENERATE_INFOPLIST_FILE`); three xcodegen/xcodeproj fixes. |
| 157 | [157-web-faceted-attribute-filter.md](cookbook/157-web-faceted-attribute-filter.md) | Multi-select tag filter that's OR-within-a-facet but AND-across-facets: group attrs into facets, constrain only touched ones. Fixes flat-OR (removing a pill shrinks results) and flat-AND (same-facet = ∅). |
| 158 | [158-web-last-loaded-root-token-file-wins.md](cookbook/158-web-last-loaded-root-token-file-wins.md) | A shared token file that redefines `:root` and loads LAST silently beats every earlier `:root` (incl. an `@media dark` remap — equal specificity, later wins; media adds none) → wrong accent / dark-on-dark. Override by out-specifying `:root` (`.sheet{--x}`) or delete the loser; never `!important`/reorder. |
| 159 | [159-meta-blind-spot-injection-checklist.md](cookbook/159-meta-blind-spot-injection-checklist.md) | A keyword→prompt library that injects the perspectives Claude structurally forgets (threading, coord origins, sandbox scope, migration paths) into a `/check`/spec/plan — octopus's "council" value with none of the multi-vendor engine. Seeds sourced from THIS repo's own gotcha docs (20_/21_/22_/38_/61_/32_/36_); ~15-line jq matcher, one JSON file, no keys. |
| 160 | [160-swiftui-videoplayer-crash-avplayerview-bridge.md](cookbook/160-swiftui-videoplayer-crash-avplayerview-bridge.md) | SwiftUI `VideoPlayer` aborts on macOS 26 (`EXC_CRASH`/`abort()` via `getSuperclassMetadata` in `_AVKit_SwiftUI`) the first time any video enters the view tree — not file-specific. Fix: bridge AppKit `AVPlayerView` in a tiny `NSViewRepresentable`; it skips the crashing module and adds real transport controls. |
| 161 | [161-remux-passthrough-must-honor-encode-settings.md](cookbook/161-remux-passthrough-must-honor-encode-settings.md) | A remux/passthrough shortcut keyed on codec alone silently copies the stream, discarding the preset's target (asked 2 Mbps, got a 700 Mbps rewrap). Gate on no resolution + no quality/bitrate target. Output chroma the encoder can't emit (4:2:2-10) proves a copy, not a transcode. |
| 162 | [162-lsuielement-dock-app-nonstealing-launch.md](cookbook/162-lsuielement-dock-app-nonstealing-launch.md) | Dock app that doesn't grab focus on launch: mark it LSUIElement (born .accessory) so LaunchServices won't auto-front it, flip to .regular WITHOUT NSApp.activate(), surface via orderFrontRegardless. |
