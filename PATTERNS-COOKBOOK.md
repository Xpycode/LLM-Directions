# Swift/SwiftUI Patterns Cookbook

**Extracted from working production code across 15+ projects.**
**Last updated: 2026-06-13 (added #92–98 — #92 greedy `Color.clear`/`Spacer` header-align, #93 pure sort-feed + off-main prewarm, #94 lossless QuickTime/ISO-BMFF atom date-patch, #95 QuickLook-first thumbnails + FFmpeg fallback, #96 app-indexer misses Cryptex-symlinked system apps, #97 `NSLog` invisible for `open`-launched apps → capture stderr, #98 `ForEach` `.id(index)` overrides element identity → stale rows; + §0.2 to #00 app-shell for a checked-in `.xcodeproj`. (Local cookbook #91–97 renumbered #92–98 to clear origin's #91 session-detector.) Earlier: #91 detect concurrent CLI sessions sharing one git checkout — process-inspection guard `pgrep -x`+`lsof` cwd→worktree-toplevel, warn before the shared-HEAD branch collision; #90 off-main file logger — mirror an in-memory feed to disk, bounded + rotated, sandbox-container-aware; earlier: #89 native titlebar toolbar grows Tahoe Liquid Glass bubbles → `UIDesignRequiresCompatibility` opt-out, #88 fail2ban-ban vs auth-failure during SFTP/SSH deploy, #87 SwiftUI caret token insertion, #86 single-flight async dedup actor, #85 phase-aware subprocess watchdog)**

---

> **MANDATORY STANDARD — READ FIRST**
>
> Every macOS app MUST use the **App Shell Standard** below. This means:
> - `HSplitView` for panes (NOT `NavigationSplitView` — no Tahoe frosted sidebars)
> - `FCPToolbarButtonStyle` for toolbar buttons (NOT default round/capsule buttons)
> - `.windowStyle(.hiddenTitleBar)` + `.preferredColorScheme(.dark)` + `.toolbarRole(.editor)`
> - Custom dark `Theme` struct for consistent colors
>
> **Existing apps not using this pattern should be migrated.** When starting work on
> any macOS app, check whether it follows the App Shell Standard. If it doesn't,
> migrating to this standard is a prerequisite before adding new features.
>
> Reference implementation: `1-macOS/Penumbra/` (pre-Tahoe SDK toolbar)
> Titlebar injection reference: `1-macOS/VAM/` (macOS 26 SDK — no system chrome)

---

## Patterns Index

Each pattern lives in `cookbook/`. Read the relevant file when a pattern is needed.

| # | File | What's Inside |
|---|------|---------------|
| 0 | [00-app-shell.md](cookbook/00-app-shell.md) | **MANDATORY** — Entry point, Theme, FCPToolbarButtonStyle, HSplitView panes, migration checklist |
| 1 | [01-window-layouts.md](cookbook/01-window-layouts.md) | NavigationSplitView, HSplitView variants, multi-window, autosave dividers, NSTableView |
| 2 | [02-layout-templates.md](cookbook/02-layout-templates.md) | 5 archetypes: Browser, Editor, Organizer, Dual Viewer, Workspace |
| 3 | [03-appkit-controls.md](cookbook/03-appkit-controls.md) | NSButton, NSCheckbox, NSPopUpButton, NSSegmentedControl, NSSlider, NSTextField wrappers |
| 4 | [04-swiftui-performance.md](cookbook/04-swiftui-performance.md) | Diffing checkpoints, @ViewBuilder anti-pattern, .equatable(), image cache flash fix |
| 5 | [05-export-file-dialogs.md](cookbook/05-export-file-dialogs.md) | NSSavePanel, NSOpenPanel, async panels, progress tracking, security-scoped bookmarks, .fileImporter |
| 6 | [06-app-lifecycle.md](cookbook/06-app-lifecycle.md) | @main entry, .task init order, scenePhase, Manager.configure(), FolderManager |
| 7 | [07-timecode-typography.md](cookbook/07-timecode-typography.md) | SF Pro .monospacedDigit() for timecode displays, weight hierarchy |
| 8 | [08-keyboard-shortcuts.md](cookbook/08-keyboard-shortcuts.md) | 4 tiers: SwiftUI Commands → .onKeyPress → NSEvent monitor → custom manager |
| 9 | [09-context-menus.md](cookbook/09-context-menus.md) | Basic, conditional, extracted @ViewBuilder, NSMenuDelegate for NSTableView |
| 10 | [10-selection-models.md](cookbook/10-selection-models.md) | Single, multi Set\<ID\>, grid, NSTableView sync, cross-pane, two-level |
| 11 | [11-drag-drop.md](cookbook/11-drag-drop.md) | .onDrop, typed handler, concurrent TaskGroup, internal reorder, NSTableView, NSView |
| 12 | [12-activity-progress.md](cookbook/12-activity-progress.md) | Status bar, inline progress, determinate+cancel, multi-level, metrics panel, floating, phases |
| 13 | [13-workspace-switching.md](cookbook/13-workspace-switching.md) | View mode toggle, tool picker, sidebar-driven, @AppStorage persist, nested sub-modes |
| 14 | [14-subprocess-url.md](cookbook/14-subprocess-url.md) | URL.path() pitfall, security-scoped access across async pipelines |
| 15 | [15-native-video-analysis.md](cookbook/15-native-video-analysis.md) | Shot/scene detection (Y-plane histogram), motion scoring (frame differencing) |
| 16 | [16-sparkle-auto-updates.md](cookbook/16-sparkle-auto-updates.md) | Integration checklist, INFOPLIST_KEY_ gotcha, empty appcast fix, minimal updater |
| 17 | [17-thread-safe-rendering.md](cookbook/17-thread-safe-rendering.md) | NSBitmapImageRep for TaskGroup offscreen rendering |
| 18 | [18-pipeline-extraction.md](cookbook/18-pipeline-extraction.md) | Shared processing logic, caller-owned I/O |
| 19 | [19-swift6-concurrency.md](cookbook/19-swift6-concurrency.md) | @MainActor + @Observable — enforce main-thread mutation at class level |
| 20 | [20-actor-reentrancy.md](cookbook/20-actor-reentrancy.md) | When TOCTOU is NOT possible — synchronous sequences can't race |
| 21 | [21-anti-patterns.md](cookbook/21-anti-patterns.md) | Common mistakes to avoid |
| 22 | [22-debounced-cifilter.md](cookbook/22-debounced-cifilter.md) | Live filter preview with SwiftUI fallback cache |
| 23 | [23-z-order-overlay.md](cookbook/23-z-order-overlay.md) | Out-of-bounds visual feedback without badges |
| 24 | [24-web-dev-patterns.md](cookbook/24-web-dev-patterns.md) | Jinja2 data injection, ES module DI, shared state module |
| 25 | [25-extension-file-splitting.md](cookbook/25-extension-file-splitting.md) | Split large files via extensions, access level fixes, strategy by file type |
| 26 | [26-launchd-node-service.md](cookbook/26-launchd-node-service.md) | KeepAlive server, scheduled tasks, install/uninstall, Apple Silicon PATH gotcha |
| 27 | [27-timelineview-elapsed.md](cookbook/27-timelineview-elapsed.md) | TimelineView(.periodic) for elapsed/remaining readouts, replaces Timer + objectWillChange |
| 28 | [28-commandgroup-observation.md](cookbook/28-commandgroup-observation.md) | Commands struct with @ObservedObject — makes menu items update from @Published state |
| 29 | [29-disk-space-preflight.md](cookbook/29-disk-space-preflight.md) | URLResourceKey volume APIs, preflight check, same-volume detection, named-volume errors |
| 30 | [30-volume-custom-icons.md](cookbook/30-volume-custom-icons.md) | **Two-step write** (`.VolumeIcon.icns` + `com.apple.FinderInfo` xattr + `utimes`) because `NSWorkspace.setIcon` is broken on volume roots since macOS 13.1 |
| 31 | [31-volume-enumeration.md](cookbook/31-volume-enumeration.md) | "External drive" heuristic — why `volumeIsRemovableKey` is misleading; correct filter is `!isInternal && !isRootFileSystem && !isLikelyDiskImage` |
| 32 | [32-nsworkspace-asyncstream.md](cookbook/32-nsworkspace-asyncstream.md) | Bridge `NSWorkspace` mount/unmount notifications to `AsyncStream<MountEvent>` inside an actor; observer ownership + termination cleanup |
| 33 | [33-managed-developer-id.md](cookbook/33-managed-developer-id.md) | Xcode Archive → Direct Distribution with a server-side managed Developer ID cert; CLI `notarytool` pipeline as an appendix |
| 34 | [34-xcodeproj-clone-rename.md](cookbook/34-xcodeproj-clone-rename.md) | Clone an existing `.xcodeproj` when a new app needs the same toolchain bundling / entitlements / build phases — `cp -R` + sed recipe with macOS sed, display-name-with-spaces, and xcuserdata gotchas |
| 35 | [35-asyncstream-bounded-fanout.md](cookbook/35-asyncstream-bounded-fanout.md) | `withTaskGroup` drain-and-refill pattern for streaming results from thousands of async operations with a bounded concurrency cap; why the naive `for url in files { group.addTask }` version is wrong |
| 36 | [36-fast-preview-heavy-commit.md](cookbook/36-fast-preview-heavy-commit.md) | Split render API into `preview(…) throws -> NSImage` (in-memory, sync, ~15 ms) vs `render(…) async throws -> Data` (full pipeline with subprocess, ~300 ms). Synchronous `.onChange` wiring for live slider feedback; no Task/cancel gymnastics. |
| 37 | [37-effective-source-fallback.md](cookbook/37-effective-source-fallback.md) | `pending ?? cached` editor binding so settings can be tweaked on a previously-saved asset without re-importing. Includes dirty-detection for the commit gate and self-healing for disappeared cache files. |
| 38 | [38-destructive-copy-guard.md](cookbook/38-destructive-copy-guard.md) | `sourceURL.standardizedFileURL == destURL.standardizedFileURL` early-return before `removeItem → copyItem` — avoids deleting the file you're about to read when `src == dest`. |
| 39 | [39-design-tokens.md](cookbook/39-design-tokens.md) | **App-wide visual tokens** — typography scale (semantic, modular ratio), 8pt spacing grid with internal≤external rule, SF Symbol weight/scale conventions, corner radii, CSS `clamp()` fluid translation for web projects |
| 40 | [40-spaces-plist-backend.md](cookbook/40-spaces-plist-backend.md) | **Public Spaces backend via `com.apple.spaces`** — parse the documented-format-undocumented-semantics plist to get real Space UUIDs + current-Space per monitor without private CGS SPI. Handles `spans-displays=0`/`1` modes correctly. Includes Swift 6 reader, AsyncStream wiring with 120ms settle delay, v1→v2 migration strategy, and known limits |
| 41 | [41-web-hero-floating-icons.md](cookbook/41-web-hero-floating-icons.md) | **Web landing-hero composition** — fill empty hero space with the product's own app icons scattered at gentle angles around the centered headline. `.hero-stage` wrapper + 3× `.float-icon` (absolute, rotated, drop-shadow stacks, staggered drift animations 7s/8s/9s, hidden under 820px). Why `filter: drop-shadow` not `box-shadow`; why coprime animation periods; accessibility (`alt=""` + `aria-hidden`). |
| 42 | [42-web-native-dialog-lightbox.md](cookbook/42-web-native-dialog-lightbox.md) | **Web image lightbox via native `<dialog>`** — click any thumbnail to enlarge it in a fullscreen overlay (GitHub-style). Single shared `<dialog>` per page, JS swaps `img.src`. Free focus trap + ESC-close + `::backdrop` pseudo-element; backdrop-filter blur; click-anywhere-to-close. `.shot` a11y attrs (`tabindex`/`role`/`aria-label`/keydown) added at runtime, not in HTML. Variations for next/prev nav, captions inside dialog, pinch-zoom alternatives. |
| 43 | [43-subprocess-fire-and-collect.md](cookbook/43-subprocess-fire-and-collect.md) | **Short-lived subprocess → single stdout blob** — `waitUntilExit() + readDataToEndOfFile()` inside `Task.detached` instead of `readabilityHandler`. Avoids the tail-byte race at child-termination, kills 30 lines of handler-cleanup boilerplate, same performance for bounded outputs. Right for `ffprobe`/`exiftool`/`shasum`/`git rev-parse`; wrong for long-running processes with streamed progress (use `readabilityHandler` there). Covers stderr always-drain rule, `Task.detached` rationale, per-call cancellation trade-off. |
| 44 | [44-inherited-project-dead-code-sweep.md](cookbook/44-inherited-project-dead-code-sweep.md) | **Bulk-remove dead Swift code from forked Xcode project** — `sed -i '' "/filename.swift/d" project.pbxproj` per dead file removes all 4 pbxproj references at once (works because Xcode-generated pbxproj puts filename in every `/* comment */`). Combine with disk `rm`, in-place trim for partially-dead files via `sed -n "${N},\$p"`, safety-net unit tests. AvidMXFPeek sweep: 30 files deleted, 5 files trimmed, ~4500 → 1507 LOC in ~20 min. Gotchas: escape regex metachars in filenames, close Xcode first, watch for prefix-name collisions, PBXFileSystemSynchronizedRootGroup exempts contents from pbxproj file refs. |
| 45 | [45-macos-firmlink-canonical-path.md](cookbook/45-macos-firmlink-canonical-path.md) | **macOS firmlink / `/var` vs `/private/var` URL-equality gotcha** — `URL.resolvingSymlinksInPath()` is a **no-op on APFS firmlinks** (Catalina+), but `FileManager`'s enumerator returns firmlink-resolved URLs. Result: hand-built `/var/folders/...` URLs fail `==` against enumerator-returned `/private/var/folders/...` URLs. Fix: `URLResourceValues.canonicalPath` (requires path to exist — canonicalize the parent, then `appendingPathComponent` the leaf). Covers when this bites (test fixtures, Sets of URLs, path-based dedup), what doesn't work (resolvingSymlinksInPath/standardized/NSString variants), diagnostic approach via xcresult assertion diffs. |
| 47 | [47-xcodegen-swiftterm-setup.md](cookbook/47-xcodegen-swiftterm-setup.md) | **xcodegen + SwiftTerm setup gotchas** — binary resources (TTF fonts) not auto-included → postBuildScripts + ATSApplicationFontsPath; UIDesignRequiresCompatibility must be in info.properties not real plist; SwiftUI.Color vs SwiftTerm.Color ambiguity → extension SwiftUI.Color; re-run xcodegen after adding files; SourceKit false positives before first Xcode open |
| 48 | [48-swiftterm-output-monitor.md](cookbook/48-swiftterm-output-monitor.md) | **SwiftTerm output monitoring + PTY input bridge** — override `dataReceived(slice:)` in a `LocalProcessTerminalView` subclass (safe intercept point; delegate interception breaks internals); closure bridge pattern for PTY writes from model layer (`session.sendInput?("y\n")`); rolling 512-char buffer for split-pattern matching across chunks; threading gotchas (dispatch to main before @Observable mutation) |
| 46 | [46-web-php-download-counter.md](cookbook/46-web-php-download-counter.md) | **Web download counter — PHP + flat-file JSON, no backend** — `/dl.php?app=<name>` validates against a regex (path traversal blocked), finds first matching `{dmg,zip,saver,pkg}`, increments `counts.json` under `flock(LOCK_EX)`, then 302s to the real file (Apache serves bytes — Range requests work natively). GET-only counting (HEAD doesn't increment, defeats browser speculative-prefetch and `curl -I`), bot UA filter, counter failure non-fatal. Companion `download-stats.js` reads `counts.json` and prepends "N downloads · " to a static "latest vX.Y · NN MB" line baked into HTML — graceful degradation if JS fails. ~50 lines PHP + 20 lines JS, no DB, no third-party analytics. |
| 49 | [49-web-php-feedback-form-with-admin.md](cookbook/49-web-php-feedback-form-with-admin.md) | **Web feedback form with admin — PHP + flat-file JSON, public/private split, Basic-Auth admin** — in-app "Report a bug / Feature request" form that persists to `feedback/public.json` (web-readable, no PII) + `feedback/private/submissions.log` (`.htaccess` denied, append-only, with email + salted-IP-hash + UA). Join-at-read rather than at-write so operator cat of public.json still shows no email. XSS-safe via `textContent` / `createElement` discipline (no `innerHTML`, no escape function needed). Honeypot + `sha256($ip.$salt)` rate limit (3/h, 10/d). Admin UI at `/admin/` gated by `.htaccess` Basic-Auth + `.htpasswd` — **critical pairing**: Basic-Auth's auto-attached credentials mean CSRF is a real threat, so admin `update.php` enforces same-origin via Origin/Referer check. One-time `_whereami.php` helper discovers the server's absolute path for `AuthUserFile` (delete after use). `deploy.sh` must `--exclude` server-managed files or mirror becomes destructive on second deploy. Composes with #46. |
| 50 | [50-detachable-windows.md](cookbook/50-detachable-windows.md) | **Detachable windows — `WindowGroup(for: UUID.self)` + `WindowManager`** — each detached window gets its own manager looked up by UUID. `WindowManager` maps `[UUID: YourManager]`; `detachSession` moves item out of source manager, creates new manager, stores it, returns the UUID; `openWindow(value: uuid)` opens the typed scene. Includes cross-window drag via `Transferable` + custom UTType + `.dropDestination`. Gotchas: `import CoreTransferable` in non-SwiftUI files; use `removeSession` (not `closeSession`) for moves; typed scene body receives `Binding<UUID?>` — nil-check before lookup. |
| 51 | [51-swiftui-recursive-split-pane.md](cookbook/51-swiftui-recursive-split-pane.md) | **SwiftUI recursive split pane — `indirect enum` tree + recursive View** — `SplitNode` with `leaf` and `split(direction:ratio:)` cases; `inserting/removing` operations return new trees (value semantics). `SplitPaneView` recurses via `HSplitView`/`VSplitView` (AppKit-backed drag-to-resize, free). Critical: `switch` in SwiftUI `body` breaks the result builder — delegate to `@ViewBuilder private func content()`. Active pane gets 1px accent border via `ZStack` overlay with `allowsHitTesting(false)`. Keyboard shortcuts via `Commands` struct + `.commands{}` on `WindowGroup`. |
| 52 | [52-appendingpathcomponent-fs-probe.md](cookbook/52-appendingpathcomponent-fs-probe.md) | **`URL.appendingPathComponent(_:)` fs-probe URL-equality gotcha** — the single-argument overload performs a filesystem probe to set `hasDirectoryPath`. If the same path is resolved twice (once before the directory exists, once after) the two URLs compare **unequal** because `URL ==` walks `hasDirectoryPath`. Bites caches, `prepareOutputDir`-style APIs, `Set<URL>`/`[URL:T]` keyed by computed paths, and diff algorithms. Fix: always pass `isDirectory: true` when appending a directory component (or `.appending(path:, directoryHint: .isDirectory)` on macOS 13+). Standardizing, string-comparing, and firmlink-normalization don't help — only the explicit `isDirectory:` parameter stabilises it. Composes with #45 (firmlink canonical-path); apply both when mixing `FileManager` output with hand-built URLs. |
| 53 | [53-web-grid-masonry-jsspan.md](cookbook/53-web-grid-masonry-jsspan.md) | **Web masonry — CSS Grid + JS-computed `grid-row-end: span N`** — row-first reading order preserved (photos 1→3 along row 1, 4→6 along row 2), unlike CSS `column-count` which fills column 1 entirely before column 2. Technique: `grid-auto-rows: 8px` small unit + per-item JS sets `grid-row-end: span ceil((natH/natW * clientWidth + gap) / (8 + gap))` on img `load`. Resize debounced at 150ms. ~15 lines JS. Pairs with `<dialog>` lightbox (#42). Non-Firefox-gated (native `grid-template-rows: masonry` is still Firefox-only). |
| 54 | [54-web-portrait-aware-uniform-grid.md](cookbook/54-web-portrait-aware-uniform-grid.md) | **Web portrait-aware uniform grid** — mostly-landscape gallery with occasional portraits that shouldn't get cropped or break row flow. `.gallery-link` has `aspect-ratio: 3/2`; JS adds `.portrait` class when `img.naturalHeight > naturalWidth`; `.portrait` gets `grid-row: span 2` + `aspect-ratio: 2/3`; `grid-auto-flow: dense` auto-fills gaps with landscapes stacking beside. Aspect-ratio MUST live on the link (not on `<picture>` — transparent element ignores CSS). Visual outcome: portrait in col 2 spanning rows 2+3, landscapes in cols 1+3 stack above/below. Extensible to panoramics via `.panoramic { grid-column: span 2 }`. |
| 55 | [55-web-preview-suffix-deploy.md](cookbook/55-web-preview-suffix-deploy.md) | **Web preview-via-suffix deploy — staging without staging** — for multi-day rewrites of static sites on shared hosting (Strato, Dreamhost, etc.). Every new page deploys as `*-new.html` alongside the live original; internal hrefs on preview pages use `-new.html` so reviewers stay in preview. Same origin = shared images/fonts/CSS, no duplication. Ship = rename `*-new.html` → `*.html` + sitewide sed to strip `-new` from hrefs. SFTP `put <local> <remote-with-suffix>` keeps local filenames clean. Alternative: `/preview/` subfolder for longer migrations. Pre-deploy grep to catch preview pages accidentally linking to production. |
| 57 | [57-commandgroup-saveitem-cmdw-override.md](cookbook/57-commandgroup-saveitem-cmdw-override.md) | **`⌘W` override in SwiftUI macOS — `CommandGroup(replacing: .saveItem)`** — multi-tab/multi-pane apps need ⌘W to close active pane→tab→spawn-fresh, not the system default close-window. The trap: `CommandMenu` with `.disabled(panes <= 1)` falls through to the system close-window when greyed-out, so ⌘W behaves differently with one vs many panes. Fix: use `CommandGroup(replacing: .saveItem)` (the only placement that wins over `File > Close Window`) with a button that's **never** `.disabled`, and a `closeActive()` that handles every state (close pane → close tab → auto-spawn fresh tab to keep window non-empty). Confirmation via `@Binding<Bool>` + `.confirmationDialog` (not `NSAlert.runModal` — transaction-unsafe). Not for `DocumentGroup` apps (system handling is correct there). Composes with #28, #50. |
| 58 | [58-xcodegen-postbuildscripts-sandbox.md](cookbook/58-xcodegen-postbuildscripts-sandbox.md) | **`xcodegen` postBuildScripts: sandbox-safe + dep-analysis-friendly** — copying resources via a script phase fails two ways out of the box: (1) Xcode warns "will be run during every build because it does not specify any outputs"; (2) sandbox denies `file-read-data` against source paths outside `$TARGET_BUILD_DIR`. Both fixed by **both** `inputFiles:` (sandbox grant) AND `outputFiles:` (mtime-based skip on incremental builds) — one alone is insufficient. Use `$(VAR)` not `${VAR}` in xcodegen path entries (pbxproj syntax, not shell). Globs don't work for sandbox grants — list concrete paths. For >10 files, switch to `inputFileLists` / `outputFileLists` referencing `.xcfilelist` text files. Always `xcodegen generate` after editing `project.yml`. Extends #47. |
| 56 | [56-web-static-admin-template-roundtrip.md](cookbook/56-web-static-admin-template-roundtrip.md) | **Template round-trip invariant as admin-write safety net** — when a static site grows an admin that regenerates public HTML on every mutation, prove `parse(committed_leaf) → render(manifest) == committed_leaf` byte-for-byte *before* writing any mutation endpoint. Anatomy: parametrise one representative page into `{{PLACEHOLDER}}` template; pure-function renderer with `strtr` (one-pass, no recursion footgun) + literal-string concatenation for repeating blocks (heredocs are a whitespace trap); regex parser for reverse direction (brittleness to shape is a feature); CLI-only (`PHP_SAPI !== 'cli'`) harness that parses committed HTML and diffs against render output. Widen coverage across every structural variant you have (masonry+uniform, each parent section, 1-photo and 500-photo galleries). Test becomes permanent regression guard for all future template edits. Validated across 4 leaves × 436 figures zero-diff. Composes with #49 (Basic-Auth scaffold) and #55 (preview-suffix deploy for rolling out admin mutations shadow-first). |
| 59 | [59-decodable-only-codingkeys.md](cookbook/59-decodable-only-codingkeys.md) | **`Decodable`-only types with custom `init(from:)` need explicit `CodingKeys`** — Swift's Codable synthesis (SE-0166) only generates `CodingKeys` when it's also synthesizing one of `init(from:)` / `encode(to:)`. A type that's *only* `Decodable` *and* has a hand-rolled `init(from:)` has nothing left to synthesize, so `decoder.container(keyedBy: CodingKeys.self)` fails with "cannot find 'CodingKeys' in scope". The same shape compiles fine on full `Codable` because the still-synthesized `encode(to:)` carries the keys. Fix: declare `private enum CodingKeys: String, CodingKey { case … }` explicitly. Don't reach for fake `Encodable` — the synthesized `encode(to:)` may bypass your decode-time normalisation. |
| 60 | [60-closure-bridged-appkit.md](cookbook/60-closure-bridged-appkit.md) | **Closure-bridged AppKit from a model-layer `@MainActor` ObservableObject** — when a SwiftUI controller in a non-UI file (`Client.swift`, networking layer) needs to trigger AppKit behaviour (window close, NSWorkspace deep-link, NSAlert), don't hold `weak var window: NSWindow?` — that forces `import AppKit` into your model file and makes every consumer transitively AppKit-dependent. Inject closures (`var dismissAction: (() -> Void)?`, `var openSettingsAction: (() -> Void)?`) from the window controller after `super.init` (weak-capture the window inside to avoid retain cycles). Compile-time enforcement of layer boundaries; trivially stubbable in tests; cross-platform-friendly. Composes with #19, #28. |
| 61 | [61-probe-classify-not-catch-all.md](cookbook/61-probe-classify-not-catch-all.md) | **TCC / FDA pre-flight without false positives — probe-and-classify, not catch-all** — `catch { return false } → fdaBlocked = true` is a false-positive generator: any error (file-not-found, ENOTDIR, ENOENT, transient I/O) gets misread as a permission denial. Real fix: `enum AccessProbeResult { case granted / notFound / notADirectory / permissionDenied / other }` driven by `fileExists(atPath:isDirectory:)` first, then typed `CocoaError where .code == .fileReadNoPermission`, then a catch-all that **logs** the actual NSError domain+code+desc to OSLog. Only `.permissionDenied` triggers the destructive FDA gate; other failures populate `lastError` with a contextual message. Note that arbitrary `~/...` paths are NOT TCC-protected — apps that don't access `~/Documents`/`~/Desktop`/iCloud/etc. **never appear in the FDA list automatically** (gate copy must explain the "+ button" workflow). Companion: #29, #52. |
| 62 | [62-web-prefers-color-scheme-token-theme.md](cookbook/62-web-prefers-color-scheme-token-theme.md) | **Web auto dark theme — remap design tokens, not components** — for a token-based static site (`--surface`/`--ink`/`--accent` at `:root`). Re-theme the whole page in ONE `@media (prefers-color-scheme: dark)` block that overrides ~20 token values; the cascade does the rest, no `.dark .foo` rules. Add `color-scheme: light dark` (else native scrollbars/form widgets stay light). **A token flip only re-themes what uses tokens** — `grep -nE "#[0-9A-Fa-f]{3,6}|rgba?\(" css/*.css | grep -vi "var("` to find hardcoded colours that bypass them (white input-focus bg, hardcoded `:hover`, status chips) and patch each in the same block. Screenshots: `<img>`→`<picture>` with `*-dark.webp` keyed on the media query; **lightbox JS must read `img.currentSrc` not `.src`** (`.src` is always the light fallback). Put the block in the per-app override stylesheet (loaded after the shared base) so a multi-app design system stays undiverged. Pairs with #39, #42, #41. |
| 63 | [63-macos-favicon-quicklook-glyph-ico.md](cookbook/63-macos-favicon-quicklook-glyph-ico.md) | **macOS favicon gen — QuickLook renders what ImageMagick can't + glyph-only 16px** — ImageMagick's SVG support is a delegate to `rsvg-convert`; absent that binary it silently falls back to MSVG and emits a **near-blank PNG** (no error, just ~455 bytes) for any icon with `clipPath`/gradients. Render the master with `qlmanage -t -s 1024 -o DIR icon.svg` (WebKit, same as Safari) instead; keep ImageMagick for downscaling (`-filter Lanczos`) + `.ico` packing. A detailed icon turns to mush at 16px → author a **separate glyph-only SVG** (one bold high-contrast shape, no card/text) and commit it (reproducible). A `.ico` is a multi-image **container**: pack glyph@16 + full@32/48 so each size uses its best source (`magick i16.png i32.png i48.png favicon.ico`). apple-touch-icon must be **flattened on an opaque colour** (`-background … -flatten`) — iOS composites over black, so transparent squircle corners become black notches. Verify: check output byte size after SVG render; `magick "favicon.ico[0]"` to confirm the 16 frame. Pairs with #62, #39. |
| 64 | [64-carbon-global-hotkey-toggle.md](cookbook/64-carbon-global-hotkey-toggle.md) | **Permission-free global hotkey via Carbon `RegisterEventHotKey`** — a system-wide hotkey that fires regardless of focus, with **no Accessibility / Input-Monitoring permission** (unlike `NSEvent` global keyboard monitors), and which even catches *synthesized* key events (BetterMouse / Karabiner mapping a mouse button → combo). The trap: the event callback is a **C function pointer** that can't capture Swift context — pass `self` via the `userData` pointer, and because the callback is *nonisolated* hop `Task { @MainActor in … }` before touching `@MainActor` state. Carbon keycodes ≠ ASCII (`kVK_ANSI_Q` = 12); modifier mask uses Carbon's `cmdKey`/`optionKey`/`controlKey` (cast to `UInt32`), **not** `NSEvent.ModifierFlags`. Give the `EventHotKeyID` a unique four-char `OSType` signature. Rebinding = unregister + register. No `deinit` cleanup (nonisolated under Swift 6 can't touch the non-Sendable Carbon pointers) — tear down via explicit `unregister()` from `applicationWillTerminate`. Conflict with another app's combo fails silently → ship a default + rebind UI. Pairs with #65, #60. |
| 65 | [65-cursor-anchored-nspanel-hud.md](cookbook/65-cursor-anchored-nspanel-hud.md) | **Cursor-anchored non-activating `NSPanel` HUD + permission-free dismiss** — a transient overlay (stats strip, command bar) shown over the focused app **without stealing focus**: `[.borderless, .nonactivatingPanel]` + `orderFrontRegardless()` (not `makeKeyAndOrderFront`), `level = .floating`, `[.canJoinAllSpaces, .fullScreenAuxiliary]`, `backgroundColor = .clear` so SwiftUI draws the rounded shape. First click is swallowed to raise the panel → subclass `NSHostingView` with `acceptsFirstMouse = true`. Anchor at `NSEvent.mouseLocation` (global, bottom-left), **clamp into the cursor screen's `visibleFrame`**, pinning to min-edge if the panel exceeds the screen in a dimension. Dismiss permission-free: hotkey re-press toggles + click-away via a global **mouse** monitor (`addGlobalMonitorForEvents(.leftMouseDown)` needs no permission and only fires for clicks in *other* apps). Esc-to-dismiss via a *scoped Carbon hotkey* is also free — see **#72** (the obvious "global keyboard monitor" route would need Input-Monitoring permission; #72 avoids it). Deliberately **not** the App Shell Standard — don't shell-check it. Lineage: MousePlus `RingWindowController`. Pairs with #64, #57, #60. |
| 66 | [66-macos-permission-free-disk-stats.md](cookbook/66-macos-permission-free-disk-stats.md) | **Permission-free disk stats — `statfs` capacity + IOKit I/O counters (not `fs_usage`)** — live free/used capacity and read/write throughput in a no-permission utility. The trap: `/usr/bin/fs_usage` needs **root / Full Disk Access** and a continuous subprocess — wrong for a glanceable HUD. Instead: capacity via one `statfs("/")` syscall (`f_bavail * f_bsize` = free-to-non-root); throughput by summing each `IOBlockStorageDriver`'s `Statistics` dict `Bytes (Read)`/`Bytes (Write)` (cumulative-since-boot → report the **per-tick delta**, first tick seeds baseline = 0). Cast stat values via `NSNumber.uint64Value` (they're CFNumbers), `IOObjectRelease` **every** service + the iterator (Mach-port leak otherwise), `kIOMainPortDefault` on macOS 12+. `ByteCountFormatter` spells out "Zero KB/s" → set `allowsNonnumericFormatting = false`. Same sampler shape as host CPU/VM stats (`DispatchSourceTimer` + `Sendable` sample + callback). Pairs with #65, #64, #67. |
| 67 | [67-swiftui-jitter-free-numeric-readout.md](cookbook/67-swiftui-jitter-free-numeric-readout.md) | **Jitter-free numeric readout — hidden worst-case template + monospaced digits** — stop a live number (stat, timecode, rate) from resizing its container every time its width changes ("9,2 GB"→"284,76 GB", "7%"→"100%"). Two pieces: `.monospacedDigit()` (equalizes digit *width* but **not** digit *count*) **plus** a reserved field — stack a **hidden** `Text(widestValue)` (e.g. "888,88 GB", "100%") behind the real value in a `ZStack` so the field holds max width regardless of the value. One line, same font (auto-rescales), no `@State`/measurement — simpler than Penumbra `TimecodeView`'s per-glyph `NSAttributedString.size()` framing (use that only for true cross-row digit grids). Composes with content-sized panels (`NSHostingView.fittingSize`): fixed-width fields make the measurement **stable** while still auto-growing when a field is added. Trade-off: worst-case template = some trailing whitespace when the value is short. Pairs with #65, #66, #27, #39. |
| 68 | [68-macos-permission-free-network-throughput.md](cookbook/68-macos-permission-free-network-throughput.md) | **Permission-free network throughput — `getifaddrs` `AF_LINK` byte counters** — live up/down rate in a no-permission utility without packet capture (`BPF` needs elevated rights). `getifaddrs(3)` exposes cumulative-since-boot per-interface counters; the catch: they live **only on the link-layer (`AF_LINK`) record's** `ifa_data` (`if_data.ifi_ibytes`/`ifi_obytes`) — skip the IPv4/IPv6 records or you double-count. Sum the counted interfaces, report the **per-tick delta** (first tick seeds baseline = 0, guard `current >= previous`), `freeifaddrs` via `defer`. Make the interface filter a **pluggable predicate** `shouldCount(name:flags:)` — count-everything (lo0 inflates idle), internet-only (`flags & IFF_LOOPBACK == 0`), or physical-only (`hasPrefix("en")`); test flag *bits* not name strings. Same sampler shape as #66. Pairs with #65, #64, #67, #69, #70. |
| 69 | [69-macos-iokit-power-sources-battery.md](cookbook/69-macos-iokit-power-sources-battery.md) | **Permission-free battery — IOKit Power Sources + the Copy/Get ownership rule** — charge / charging / time-remaining via `IOKit.ps` (same snapshot the menu bar reads, no entitlement). **Not** a delta — each read is absolute state. The trap this entry exists for: `Unmanaged<>` ownership follows the function-name prefix — `IOPSCopy…` (Create Rule) → `.takeRetainedValue()`, `IOPSGetPowerSourceDescription` (Get Rule) → `.takeUnretainedValue()`; mixing them leaks or crashes. `-1` time = "calculating" (→ nil, not 0); pick `kIOPSTimeToEmpty` vs `…TimeToFullCharge` by charging state. Desktop Macs report **no source** → carry `isPresent` and hide the UI (don't show 0%). Two display tricks: **inverted color band** (feed `100 - percent` to a normal hot/calm ramp so low charge = hot) and **state-aware SF Symbol** (`battery.Npercent[.bolt]`). Pairs with #66, #68, #70, #65, #67. |
| 70 | [70-swiftui-data-driven-tile-strip.md](cookbook/70-swiftui-data-driven-tile-strip.md) | **Data-driven SwiftUI tile strip — descriptor array kills the type-checker timeout** — a row of N hardcoded subviews (+ a conditional) eventually trips *"unable to type-check this expression in reasonable time"* because SwiftUI infers one ballooning `TupleView<(A,B,C,…)>`. Fix: render a `ForEach` over a homogeneous `[Descriptor]` so the content type collapses to one `ForEach<…>`. Bonus: a `kind` enum (`CaseIterable, Identifiable`) gives stable identity + display order, and one `store.visibleStats` builder (`compactMap` to filter unavailable items, e.g. battery on desktops #69) is the single chokepoint a future **settings/stat-selection** feature hooks into — views never change. Keep SwiftUI types **out** of the descriptor (apply `Theme.loadColor` in the view); read `visibleStats` **inside** `body` so `@Observable` tracks it; dividers via `if index > 0`; key on `\.element.id` not the array index. Pairs with #66, #68, #69, #67. |
| 71 | [71-lsuielement-self-managed-settings-window.md](cookbook/71-lsuielement-self-managed-settings-window.md) | **Self-managed settings `NSWindow` for `LSUIElement` apps — `showSettingsWindow:` silently no-ops** — in an agent app (no Dock/menu bar), the SwiftUI `Settings` scene + `NSApp.sendAction(showSettingsWindow:)` quietly fails: focus shifts (your `activate()` ran) but **no window appears**, because the responder chain can't reach the scene's window manager without regular activation. Fix: skip the `Settings` scene — build your own `NSWindow(contentViewController: NSHostingController(rootView: SettingsView()))`, set `isReleasedWhenClosed = false`, then `NSApp.activate(ignoringOtherApps:)` + `makeKeyAndOrderFront`. Reduce the App's `Settings` scene to `EmptyView()` so there's one path. Becoming **key** (opposite of the #65 HUD panel's `orderFrontRegardless`) is also what lets an in-app **hotkey recorder** capture keystrokes via a local `keyDown` monitor. Pairs with #65, #64, #60. |
| 72 | [72-scoped-carbon-hotkey-esc-dismiss.md](cookbook/72-scoped-carbon-hotkey-esc-dismiss.md) | **Permission-free Esc-to-dismiss for a non-key panel via a *scoped* Carbon hotkey** — a non-activating HUD (#65) never becomes key, so `keyDown` / `.onKeyPress` never reach it and Esc goes to the focused app. You don't need an Input-Monitoring keyboard monitor (corrects #65's old "Esc is not free" note): register **bare Escape as a Carbon hotkey (#64) only while the panel is visible**, unregister on hide. Two requirements make it correct: (1) a *second* `HotKeyService` instance **must filter by `EventHotKeyID`** in the C callback (`kEventParamDirectObject` → `typeEventHotKeyID`) — Carbon dispatches every press to *all* installed app-level handlers, so without an id check the toggle hotkey also fires the dismiss action (latent bug even with one hotkey). (2) Drive register/unregister from the panel's **own visibility signal** (`onVisibilityChanged` fired in `show`/`hide`, guarded against double-fire), **not** the caller's `togglePanel()` — else a **click-away** dismissal (global mouse monitor) bypasses teardown and leaves Esc captured system-wide forever. Trade-off: bare Esc is swallowed for *all* apps while the panel is up (fine for a transient HUD). Pairs with #64, #65, #71, #73. |
| 73 | [73-verify-hud-without-screen-recording.md](cookbook/73-verify-hud-without-screen-recording.md) | **Verify a HUD / `LSUIElement` app from a sandboxed shell with no Screen-Recording permission** — `screencapture` fails ("could not create image from display") but you can still get pixel-free, deterministic proof. Observe show/hide via `CGWindowListCopyWindowInfo` (owner + geometry need no Screen-Recording; only names are redacted). Drive the app with `CGEventPost` (posting needs **no** permission; Carbon hotkeys catch the synthetic key, like BetterMouse). Measure hotkey **capture** with a listen-only `cgAnnotatedSessionEventTap`: `delivered==0` ⇒ consumed upstream by the hotkey, `delivered>0` ⇒ reached app delivery — the clean discriminator for "captured only while visible / released after hide" (tap needs Accessibility, which a shell often has even when Screen-Recording is denied). Click-away safely by targeting a point inside **no `kCGWindowLayer == 0` window** (same window-list data finds empty desktop), restoring the cursor after. **Key gotcha:** a `RegisterEventHotKey` conflict probe is *useless cross-process* — Carbon doesn't reject cross-process duplicate hotkeys (`eventHotKeyExistsErr` is same-target only); measure consumption, not registration. Pairs with #72, #65, #64. |
| 74 | [74-macos-permission-free-load-uptime-topproc.md](cookbook/74-macos-permission-free-load-uptime-topproc.md) | **Permission-free load average / uptime / top-process — and the libproc same-user gate** — three more absolute-snapshot samplers (sibling of #66/#68/#69). **Load:** `getloadavg(&buf, 3)` (guard `== 3`); it's run-queue depth not %, normalize by `activeProcessorCount` for a color band. **Uptime:** `sysctlbyname("kern.boottime")`, **not** `ProcessInfo.systemUptime` — the latter drops sleep time so laptops read low; boottime matches `uptime(1)`. **Top-process (the reason this entry exists):** verified in XNU `proc_security_policy` — `proc_listallpids`/`proc_pidpath` are `NO_CHECK_SAME_USER` (work for *any* process), but `proc_pid_rusage`/`PROC_PIDTASKINFO` (memory/CPU) are `CHECK_SAME_USER` → **EPERM for foreign UIDs**, no entitlement flips it (only root's `PRIV_GLOBAL_PROC_INFO`). So unprivileged = "top *user* process" (like `top` without sudo); system-wide needs a `sysmond`-style root helper (rejected). Ship **memory** first (`ri_phys_footprint`, matches Activity Monitor; a snapshot → correct on first summon) over delta-based CPU%. Gotchas: `proc_pid_rusage` needs `withMemoryRebound(to: rusage_info_t?.self)` (opaque `void *` param); `proc_name` truncates to 16 chars → use `proc_pidpath().lastPathComponent`; ~600 PIDs/tick ≈ 1–3 ms. **Bonus** `knownStats` migration: a disabled stat and a brand-new stat are indistinguishable if you persist only the enabled set — track a separate "known" set so new kinds default on without resurrecting disabled ones. Pairs with #66, #68, #69, #70, #65, #67. |
| 75 | [75-macos-top-processes-by-app.md](cookbook/75-macos-top-processes-by-app.md) | **Top processes by CPU / memory / disk, grouped by app — two engines (in-process vs `top`)** — sequel to #74 (adds CPU%/disk rates + app grouping; iStat-Menus style, lists in the metric popovers not a standalone tile). **Headline lesson:** foreign-process CPU/mem is *impossible* in-process — XNU same-user-gates ALL libproc calls (`proc_pid_rusage` AND `proc_pidinfo(PROC_PIDTASKINFO)` both EPERM/fail for kernel_task/WindowServer, verified). **Engine A** = in-process `rusage` (user-only, cheap, sandbox-safe, has per-proc disk). **Engine B** = shell out to `/usr/bin/top` (system-inclusive incl. WindowServer/kernel_task — top's Apple-private entitlement rides the binary → no user perm; **needs sandbox OFF**, no per-proc disk column, gate to UI-visible since top costs ~8%, parse the **2nd `-l 2` sample** so top does the delta, filter your own probe by **PID not name**, group via `proc_pidpath` which *does* work cross-user + COMMAND fallback for pathless kernel_task). On an idle Mac the real CPU hogs are system procs only B sees; common move = B-with-A-fallback. **Engine A specifics:** one `rusage_info_v4` pass → 3 rankings: memory snapshot + CPU/disk **two-tick rates** (per-PID `prev…` maps + `prevWall`, pruned each tick). **⚠️ The trap (Engine A):** `ri_user_time`/`ri_system_time` are **mach absolute time units, NOT nanoseconds** (XNU `fill_task_rusage` writes `rm_time_mach` raw; ~24× under-read on Apple Silicon, osquery #7459) — convert via `mach_timebase_info`, wall via `mach_absolute_time()` (`ri_diskio_*`/`ri_phys_footprint` *are* bytes). **Group by app: sum FIRST, rank SECOND** (20 small Chrome helpers summed lead the list; ranking individuals first drops them) → aggregate into `[app: Double]` then sort; restore #74's name-resolve cost with a per-PID `proc_pidpath` cache. `appGroupName(forPath:)` 3 tiers: outermost `.app` → `.xpc`/`.appex` prettify (`com.apple.WebKit.WebContent` → "Web Content") → plain exec climbing past version tokens/generic dirs (`…/claude/versions/2.1.165` → "claude"); unit-test path logic standalone. **XPC services can't be tied to host app** permission-free (parent = launchd; `responsibility_get_pid_responsible_for_pid` is private/App-Store-rejecting, ES `responsible_audit_token` needs entitlement+FDA) → prettify + accept one summed "Web Content" row. CPU% exceeds 100% (per-core); threshold the *group* not the process (or drop it on idle + show 1-decimal %). Pairs with #74, #66/#68/#69, #70, #65, #67, #72 (gates Engine B's lifecycle). |
| 76 | [76-macos-appicon-coregraphics-generator.md](cookbook/76-macos-appicon-coregraphics-generator.md) | **App icon from a design spec — native Core Graphics generator** — turn a vector icon design (gradient squircle + simple shapes, often spec'd in CSS/JSX with `oklch`) into a real `AppIcon.appiconset` (all 10 macOS sizes + `Contents.json`) from a standalone `swift GenerateAppIcon.swift` script — no Sketch/Figma/Icon Composer, `import AppKit`, zero deps, re-runnable, lives in the repo as the icon's source of truth. **Three gotchas:** (1) **oklch → sRGB** — CG and asset catalogs don't speak oklch; convert OKLab→linear→gamma by hand once and hardcode the `rgb()` with the oklch left in a comment (`oklch(0.78 0.13 175)` → `rgb(65,210,179)`). (2) **Render per-size, not downscaled** — a fresh `CGContext` per pixel size with metrics scaled off `size` keeps 1px hairlines/bevels crisp at 16–32px; corner radius = **0.2237 × size** (continuous-corner ratio); **CG y-axis is bottom-up** so a top→bottom CSS gradient runs `start:(x,size)→end:(x,0)`. (3) **Emit both Contents.json** — the 10-entry appiconset one (px = pt×scale: 16,32,32,64,128,256,256,512,512,1024) *and* the catalog-level `Assets.xcassets/Contents.json`, or Xcode ignores the catalog. **Wire-up (xcodegen):** put the catalog under a path already in `resources:`, add `ASSETCATALOG_COMPILER_APPICON_NAME: AppIcon`; verify `actool` logs `Emplaced AppIcon.icns` + `CFBundleIconName` in the built plist. **Note:** `LSUIElement` agent apps show the icon in Finder only (not Dock/Cmd-Tab). Pairs with #77 (render the prototype it came from), #67, #70. |
| 77 | [77-jsx-prototype-to-png-webkit-playwright.md](cookbook/77-jsx-prototype-to-png-webkit-playwright.md) | **Render a Babel-in-browser JSX design prototype to PNGs — no Chrome** — a design handoff ships as React + `@babel/standalone` (`.html` that `<script type="text/babel" src="*.jsx">`-loads CDN modules) and you want static PNGs per section. Two snags: it's a **pan/zoom canvas** (a plain screenshot grabs the zoomed viewport, not artboards at natural size) and there's **no Chrome**. **Fix = two parts.** (A) **Flat-render harness:** a sibling HTML that loads the same modules but mounts the section components in normal document flow — works because design-canvas wrappers usually guard ctx access (`ctx && …`) so they render without the canvas provider; `#root{width:max-content}`, hide `.dc-header` editor chrome, wrap each section in `data-flat-section`, poll until `window.SecX` exists then render and set a `data-render-ready` flag after a double-rAF. (B) **Playwright + WebKit** (Safari's engine — the right renderer on a Mac, and Playwright downloads it): `npm i playwright && npx playwright install webkit`, `newPage({deviceScaleFactor:2})` for retina, `goto(...,{waitUntil:'networkidle'})`, `waitForSelector('body[data-render-ready="1"]')`, then `screenshot({fullPage})` + `element.screenshot()` per `[data-flat-section]` for tight crops. **Gotchas:** `file://` blocks the sibling-jsx fetches → serve with `python3 -m http.server`; wait on the ready flag not a fixed sleep (Babel compile time varies); keep the `integrity=` SRI hashes (security hooks flag bare CDN tags); keep npm/`node_modules` + the ~100MB WebKit build in `/tmp` so they never enter the repo. Pairs with #76, #73 (headless verification mindset). |
| 78 | [78-swiftui-state-aware-menu-pill.md](cookbook/78-swiftui-state-aware-menu-pill.md) | **State-aware menu pill — one tap means "go here" or "switch here" depending on where you are** — a header pill that is *both* a navigation target *and* a switcher (a surface + a menu of alternatives). Naive designs annoy: whole-pill-opens-picker costs two taps to reach the default; body-tap-always-navigates leaves a tiny chevron as the only menu. **Pattern:** make the gesture follow state — when **not** on the surface, a body tap **navigates** onto it in one tap; when **already** on it, a body tap **opens the picker** (re-navigating is a no-op, so reuse the tap); the **chevron always opens the picker**. (Finder idiom: clicking an already-selected filename renames rather than re-selects.) **Two rules:** (1) **one `tapTarget` expression drives label + tap + a11y** so the control never *says* one thing and *does* another, and an inactive pill **previews** where a tap lands ("SHC") instead of a generic category word ("Saved"); (2) after merging two pills into one, **"last-used" must span the whole control** — drop the old `if id != defaultID` guard or an explicit pick of the default won't "stick". Enlarge the chevron hit target with a fixed `18×16` frame kept within line height. Route the keyboard shortcut through the same `tapTarget` so `⌘2` ≡ a click (`⌘Tab` is reserved by macOS). **Setting-gated bugs hide on the default path** — live-test the non-default branch of every pref. Pairs with #79, #08, #10. |
| 79 | [79-swiftui-layoutpriority-active-width-budget.md](cookbook/79-swiftui-layoutpriority-active-width-budget.md) | **Truncation in an HStack is a priority contest, not a maxWidth ceiling — fix it with `layoutPriority`** — a label in an `HStack`-with-`Spacer`s truncates ("Current App"→"Curre…") even though the math says it fits, and bumping `.frame(maxWidth:)` does nothing. **Why:** `maxWidth` is a *ceiling*, not a *reservation* (the frame is flexible `0...max`); the `HStack` grants width by **layout priority** (default `0`, and `Spacer` is greedy), so under any over-budget pressure the flexible text frames are the ones that give. **Fix:** give the element you want readable a higher `.layoutPriority(...)` so it claims its ideal size before lower-priority siblings *and* before the greedy Spacers; make the *less important* element (e.g. an inactive preview reachable another way) yield. Drive a state-based budget — active gets the big ceiling (160) **and** priority `2`, inactive gets `90`/`1`, flipped by one `isActive` flag. Use `maxWidth` not a fixed `width:` (a fixed slot ×2 reserves worst-case for both at once and overflows). **Orthogonal lever:** make the value *shorter and more useful* first — re-label a "Current App" list with the actual frontmost app name ("TextEdit") so it stops being the first to truncate *and* answers the user's real question. **Tell:** label truncates while Spacers absorb slack in the same row ⇒ priority problem, reach for `layoutPriority` before `maxWidth`. Pairs with #78, #67, #70. |
| 81 | [81-nonkey-hud-panel-synthetic-paste.md](cookbook/81-nonkey-hud-panel-synthetic-paste.md) | **A floating HUD that drives synthetic paste into another app — must be NON-KEY** — a control panel (paste-queue emitter, snippet sender, macro HUD) that stays visible *while it pastes into the user's frontmost app*. "Kinda works": the HUD's counter advances but **nothing pastes**, yet `NSWorkspace.frontmostApplication` is correct. **Gotcha 1 (headline): frontmost-app ≠ key-window.** A `.nonactivatingPanel` decouples them — clicking it doesn't *activate* your app (frontmost stays the target ✅) but the panel **can still become key**, stealing **keyboard focus** (exactly how Spotlight types without deactivating Finder), so the synthetic ⌘V (`.cgSessionEventTap`) is delivered to the HUD, not the target ❌. The picker/overlay (#65) survives `canBecomeKey=true` only because it **closes before pasting**; a HUD that stays open must never take key → use a **plain `NSPanel`** (borderless `.nonactivatingPanel` defaults `canBecomeKey=false`) + a **`FirstMouseHostingView`** (`acceptsFirstMouse→true`, set as `contentView`) so buttons still fire while the app is inactive; show with `orderFrontRegardless()`, **never** `makeKeyAndOrderFront`. **Gotcha 2:** a naive synthesized Return sets no flags → inherits `combinedSessionState`'s **Command bleed** from the ⌘V ~60ms earlier → fires as **⌘Return** (no-op); fix `down/up.flags = []`. **Gotcha 3:** ⌘V is async (target reads the pasteboard on *its* run-loop turn) → a rapid next emit overwrites the slot, dropping a paste while a flag-free Return still fires ("fewer lines than line-breaks", run-to-run inconsistent); fix = `isEmitting` re-entrancy lock + settle delays (`returnSettle 0.15`/`lockRelease 0.12`), disable the trigger button while held; refusal takes no lock (instant retry). **Gotcha 4:** a clipboard *manager* pasting *through* the clipboard re-captures its own writes → **stamp `changeCount = pasteboard.changeCount` after every outgoing write** (route all paths through one writer). **Bonus:** `.toggleStyle(.button)` accent only renders in a key/active window → a non-key HUD's mode toggles look permanently OFF; use hard-coded `accentColor` fill, not the system accent. Pairs with #65, #08, #10, #04. |
| 82 | [82-makekey-panel-paste-self-refusal-reactivate.md](cookbook/82-makekey-panel-paste-self-refusal-reactivate.md) | **A KEY picker panel that synth-pastes into another app must reactivate its target — the mirror of #81** — an overlay/launcher that **needs key status** (to host a search field + keyboard nav) writes the pasteboard, closes, and posts ⌘V into "the frontmost app" — but **intermittently nothing pastes**, most visibly into **Finder** (or any app whose window deactivates when your panel appears). **Cause:** `panel.makeKey()` (required for the search field) combined with `orderFrontRegardless()` can leave **your app the frontmost application**, so by paste time `NSWorkspace.frontmostApplication` is **your own bundle id** and the paste service's self-guard **correctly** refuses `wouldPasteIntoSelf` (pasteboard was perfect). Looks app-specific but isn't — text pastes survive when focus happens to stay; Finder makes it reproducible. **Fix (2 parts):** (1) at summon, capture who was frontmost **before** `makeKey()` stole focus → `pasteTargetApp` (a held `NSRunningApplication`); (2) a **fallback ladder** at commit — try the fast plain `paste()` first (zero added latency for the common case), and **only** on `.wouldPasteIntoSelf` call `paste(reactivating: target)`, which `target.activate()`s, waits ~0.12s for it to come front, **re-checks secure input** (app-scoped — Terminal re-enables on regaining focus), then posts ⌘V. **Don't "always reactivate"** — that taxes every paste with `activate()`+settle and re-introduces the post-Sonoma activate-is-a-request behavior the non-activating panel dodges; the ladder pays only when your own `makeKey()` stole frontmost. Same `pasteTargetApp` serves the right-click-menu paste path (which *always* activates you → skip straight to `reactivating:`). **Cautions:** capture an `NSImage`/value for any *UI* use of the target (the live `NSRunningApplication` weak-zeroes); verify in a log-blind agent app by appending decision points (frontmost id, refusal reason, pb types) to `/tmp` — remove before merge. Pairs with #81 (non-key HUD sibling), #65, #08; memories `secure-input-recheck-after-reactivation`, `nsrunningapplication-weak-zeroes`, `hud-panel-must-be-non-key`. |
| 83 | [83-calendar-overlap-column-packing.md](cookbook/83-calendar-overlap-column-packing.md) | **Side-by-side layout for overlapping calendar/timeline events — cluster, then first-free-column** — a day/week time-grid (or Gantt / resource timeline) must place overlapping events in parallel lanes without collisions, *and* keep events that have no neighbours at full width. Naïve "one lane per event" wastes space; "shrink to the day's max concurrency" lets one 8 AM clash narrow your 3 PM solo meeting. **Shape = two passes keyed on the same rule.** (1) **Cluster** sorted events into runs of *transitively* overlapping events; cluster end = `max(runningEnd, e.end)` **not** `= e.end` so a long event swallowing a short one stays open for a later overlapper (nesting-safe, transitive). Width is **local to the cluster** → a lone later event is a cluster of one → full width for free ("trailing expansion" is not a special case). (2) **assignColumns** within a cluster = first-free-column: `columnEnds.firstIndex(where: { e.start >= $0 })` reclaims the leftmost freed lane (greedy reuse keeps the cluster as narrow as true max-concurrency — the A-B-C chain where C drops back into A's vacated lane 0 instead of forcing a third), else append a lane. Caller derives `columnCount = columns.max()+1` **per cluster** so all cards share one denominator and tile to a clean edge; view is pure arithmetic `x = column/columnCount × dayWidth`. **The one decision both passes share: overlap is HALF-OPEN** — `a.start < b.end`, and the free test is `>=` not `>`. Touching events (`a.end == b.start`) do **not** overlap → separate clusters, can reuse a lane; `<=`/`>` spuriously shares width on every back-to-back pair. **Decoupling that makes it testable:** the kernel positions an `EventLike` **protocol** (no `import EventKit`, no SwiftUI) → full suite (no-overlap, two-overlap, back-to-back, transitive, nested, three-mutual, lone-beside-cluster, all-placed-once) runs headless in ms with a tiny `TestEvent`, no simulator/permission/window. Deterministic `(start,end,id)` sort. Pairs with #21 (time-grid y-axis geometry this splits the x of), #04. |
| 84 | [84-swiftdata-detached-model-revalidate-guard.md](cookbook/84-swiftdata-detached-model-revalidate-guard.md) | **A row-level closure that re-runs on a deleted `@Model` must guard `modelContext != nil`** — a SwiftUI list of SwiftData `@Model` rows where a per-row closure (`.task(id:)`, `.onReceive`, `onAppear` async, a `@ViewBuilder` branch) reads a **persisted property** (`item.imageData`, `item.fileURL`, `item.tags`). Works most of the time, then **hard-crashes `EXC_BREAKPOINT`** after a **Clear All / cap-trim / bulk delete** — often triggered by an *unrelated* action (copying a new item, reopening the panel). Crash report's faulting frame is **`<Model>.<prop>.getter → SwiftData → _assertionFailure`** with one of *your* row closures just below; nothing in your logic looks wrong because the **getter itself traps**. **Cause = view-identity lifecycle outlives `@Model` lifecycle:** `context.delete`+save sets the model's `modelContext` to **nil** (detached), but SwiftUI hasn't re-diffed yet so the deleted rows **still exist in the view tree**; a closure firing in that window — especially a **broadcast notification every row subscribes to** (`.onReceive(...overlayDidShow)`), which widens "unlucky" to "every reopen after a delete" — reads a persisted property on the detached model and SwiftData's getter `precondition`s it's alive. It isn't. **Looks like the new feature's fault** because the closure is usually recently-added (a staleness re-check / live badge): the feature that re-reads a row on every reopen re-reads a *deleted* row on every reopen; the triggering capture/skip logic is a red herring. **Fix = one early `guard item.modelContext != nil else { return }` before ANY persisted read** (SwiftData's canonical "deleted" signal) — must be **first**, since a `case .image where item.imageData == nil` *pattern* evaluates the getter before any case body. Apply it to every row-level persisted reader (context menus reading `item.tags`, `@ViewBuilder` switches on persisted state). **Not** `try?` (it's a `precondition` trap, nothing to catch), **not** snapshotting (defeats the live re-validate), **not** unsubscribing (no clean pre-removal hook); the real cure is **`@Query`** (hands the view only live results) but that's a structural migration — guard now, migrate later. Pull the crash without Xcode: newest `~/Library/Logs/DiagnosticReports/*.ips`, find the triggered thread, look for `<Model>.<prop>.getter` under a SwiftData frame. Pairs with the SwiftData `@ViewBuilder` footgun family. |
| 85 | [85-phase-aware-subprocess-watchdog.md](cookbook/85-phase-aware-subprocess-watchdog.md) | **A no-progress watchdog that doesn't false-alarm during legitimately-silent phases** — you run a long subprocess (ffmpeg/ffprobe export, batch encode, big upload) and want to warn when it's *genuinely* stuck, but the naive `if now - lastProgress > 30 { showStuckDialog() }` **false-positives** during phases that emit no progress on purpose: **ANALYZING** (pre-encode ffprobe keyframe scan reads the whole file before ffmpeg spawns, 1–2 min on a slow volume), **FINALIZING** (after `progress=end` the muxer flushes the `moov` atom while the bar already shows 100%). Each silent phase has to be taught separately (Penumbra shipped this bug twice). **Pattern = a pure, `nonisolated static` `watchdogAssessment(secondsSinceActivity, isAnalyzing, isFinalizing, encodeStall, hardTimeout) → Verdict` with a strict PRECEDENCE LADDER:** the hard-timeout `.hung` backstop is checked **first**, above every phase, so a genuinely-wedged scan/flush (process stuck in uninterruptible `U`-state I/O) is *still* reclaimed by your self-heal path; below it the calm phases (`.analyzing`, `.finalizing`) suppress the alert; only mid-work no-activity past `encodeStall` returns `.advisoryStall` (the ONLY verdict that alarms the user); else `.active`. **Phase = dumb per-job flags set at the transitions**, never inferred: `didStartEncoding` (false → ANALYZING; set true at subprocess spawn) + `sawProgressEnd` (set true on the `progress=end` line) → `isAnalyzing = currentJob != nil && !didStartEncoding && !sawProgressEnd`, `isFinalizing = sawProgressEnd && currentJob != nil`. **The subtle bug:** the job is marked `.exporting` (so `currentJob != nil`) BEFORE the pre-encode scan runs and before the subprocess spawns; a watchdog that only resets its clock "when no job is active" won't reset in that window → 30s false alarm. `didStartEncoding` is what says "the encode clock hasn't legitimately started — stay calm." Pure verdict = whole false-positive regression suite runs in **ms** (no subprocess, no volume); a new silent phase = one `if isX { return .x }` + one flag slotted into the ladder, and because `hung` outranks the phases a new calm phase can never suppress genuine-hang recovery. UI: show the phase name + an **indeterminate** `ProgressView()`, not a bar frozen at 0%/100%. Trade of ranking `hung` first: a *legitimately* >`hardTimeout` scan is falsely abandoned → size the timeout vs real worst-case, keep tunable. Pairs with a `.hung` self-heal (SIGTERM→SIGKILL→resume continuation with non-retryable `.abandoned`→advance queue, orphan `U`-state PID), #43 (subprocess fire-and-collect), #20 (actor-reentrancy/TOCTOU). |
| 86 | [86-single-flight-async-dedup-actor.md](cookbook/86-single-flight-async-dedup-actor.md) | **Single-flight async dedup in an actor — kill a check-then-act-across-`await` race** — several callers each want the same expensive async result for one key (load keyframes for a clip / fetch a URL / generate a thumbnail) and the obvious `guard cache == nil else { return }` then `let v = await scan(); cache = v` runs the scan N times: **a synchronous guard is worthless once an `await` separates the check from the write** — every caller passes the guard before any assigns (Penumbra: one clip got **3 concurrent ffprobe scans**, batch pre-load + per-clip export + the retry all racing the nil-guard, each ~3× slower on a slow volume, each hitting the 120s cap → partial `Saved 535/594/594`). This is the live-TOCTOU counterpart to #20 (*synchronous sequences can't race*): add an `await` between check and act and the race is back. **Pattern = hold an `inFlight: [Key: Task<V, Never>]` ledger in an actor** (often one you already have — the disk-cache actor): (1) fast-path the durable cache; (2) `if let existing = inFlight[key] { return await existing.value }` join a running scan; (3) else `let task = Task { await scan() }`, **store `inFlight[key] = task` BEFORE `await task.value`** so a caller entering during the scan joins instead of starting a second, then persist and clear the slot. **Two orderings ARE the pattern:** insert-before-await (insert after → empty slot for the whole scan → duplicate) and **clear-slot-after-save** (hold the slot through the sub-ms disk write so latecomers join the completed Task; clear before → a caller sees empty slot + cache miss → fresh duplicate). **Why an actor not a `Bool`/`NSLock`:** a `Bool` flag can only make late callers *bail* (skip the work — here export without keyframe snapping), never *await the result* — you need to hand them the in-flight `Task` (a dead `isFetchingKeyframes` bool was exactly this mistake); a lock doesn't span the `await`. Caller collapses to a thin wrapper; the dumb guard stays only as a cheap fast-out. `scan` must be `@Sendable` (it's stored in a Task); capture the key explicitly `{ [url] in … }`; `Task<_, Never>` when the scan encodes failure as nil; only the originator clears the slot (joiners take the early return). Testable headless: point at a non-existent key so cache read/write are no-ops, then assert **exactly one** scan for 50 racing callers (dedup-too-weak) **and** that a later call scans again (dedup-too-strong / slot never released) — a sleep in `scan` holds the window open so joiners provably arrive mid-scan. Pairs with #20, #19, #43, #85. |
| 87 | [87-swiftui-caret-token-insertion-nstextfield.md](cookbook/87-swiftui-caret-token-insertion-nstextfield.md) | **Insert a token at the caret of a SwiftUI text field — wrap `NSTextField`, restore caret across the binding bounce** — a token/pattern editor (rename pattern, snippet/template/mention field) has buttons that should drop `{date}`/`${var}`/`:emoji:` **at the cursor**, replacing any selection, leaving focus + caret after the token. SwiftUI `TextField` exposes **neither the selection range nor the field editor**, so a button can only `text += token` (wrong place, caret destroyed). **Fix = `NSViewRepresentable` over `NSTextField` + a small `ObservableObject` controller holding a `weak var textField`.** The field's **field editor** (`currentEditor()`, an `NSText`) carries `selectedRange` = the caret. `controller.insert(token, currentText, set:)` reads `sel` from the **live field editor** (not a lagging `@State` range; nil-when-unfocused → append at end), builds `ns.replacingCharacters(in: sel, with: token)` via **`NSString`** (UTF-16 ranges, emoji-safe), pushes it through the SwiftUI `Binding` (`set(newText)`), **then restores focus + caret on the NEXT `DispatchQueue.main.async` tick** — because writing the binding makes SwiftUI call `updateNSView` → re-set `stringValue` → park caret at end; you can't pre-empt that synchronously (AppKit analogue of the JS `requestAnimationFrame(() => setSelectionRange(...))`). **Three load-bearing details:** (1) `updateNSView` **must** guard `if v.stringValue != text` or every keystroke's binding update re-assigns mid-edit and fights the user (caret jumps, chars drop); (2) **`makeFirstResponder` THEN set `selectedRange`** — selecting before the field is key window silently no-ops; (3) **controller object, not a `@Binding` selection** — the buttons live in SwiftUI, the caret in AppKit; a `weak`-held `ObservableObject` is the clean seam (no Coordinator plumbing leaks to the parent, no retain cycle). `controlTextDidChange` delegate pushes user typing back to the binding. Field editor is **shared per window** → non-nil only while first responder, always handle the unfocused/append case. Source: Conjoyn `Views/RenamePopover.swift`. Pairs with #39 (design-token reskin), native-control-in-popover. |
| 88 | [88-detect-fail2ban-ban-vs-auth-failure.md](cookbook/88-detect-fail2ban-ban-vs-auth-failure.md) | **Tell a fail2ban IP-ban from a real auth failure during an SFTP/SSH deploy — and don't make it worse** — an automated upload to shared hosting (Strato/IONOS/OVH via `lftp`/`sshpass`/`rsync -e ssh`) keeps "rejecting the password," but the password may be **perfectly correct** and the host has silently **IP-banned the machine** after a few failed attempts; retrying a correct password against the ban *extends* it and convinces you the credential is broken. **The whole diagnosis is in WHERE the connection dies:** `Authentications that can continue: …` + `Permission denied (publickey,password)` = server **received and rejected** the credential (auth problem — wrong pw/user, key not authorized, method disabled); **`Connection closed by <ip> port 22` BEFORE any password prompt** = firewall/fail2ban **dropped you pre-auth**, credential never evaluated (ban). The **progression** is the tell — early `Permission denied` flipping to a pre-auth `Connection closed` on the *same* command **is** the ban engaging. **Probe ban-safely with `expect`:** drive the native `sftp`/`ssh` client and branch on the prompt — if it closes before `password:` you bail **without sending credentials**, so the probe adds **zero** failed-auth and can't lengthen the ban (`CLOSED_PRE_AUTH`=wait; `AUTH_DENIED`=credential really wrong; `CONNECTED_OK`=in). **Verify the password independently** from the working GUI client's store: `security find-internet-password -s host -a user -w` (run it **yourself** — an agent scanning the keychain is correctly blocked as credential harvesting), then hash-compare without printing. **Recovery:** stop ALL attempts incl. the GUI client (same IP, same ban), wait the bantime (10–30 min typical, don't poll), then **one** attempt. **Pitfalls:** special chars break password passing — `&`/`#`/`%` wreck lftp's parser (use `user 'name' 'pass'`), and **zsh doesn't word-split unquoted vars** (a header var reaches `curl` mangled); feed via `chmod 600` file (`sshpass -f`), never `argv` or an unquoted var. `curl`'s default UA trips `curl|wget` bot filters (pass `-A "Mozilla/…"`). A `known_hosts` entry does **not** prove a prior successful login (host key is saved at KEX, before auth). Pairs with #46/#49 (what you're deploying), #29 (Strato hosting). |
| 89 | [89-swiftui-native-toolbar-tahoe-glass-optout.md](cookbook/89-swiftui-native-toolbar-tahoe-glass-optout.md) | **Native titlebar toolbar grows Liquid Glass "bubbles" on the macOS 26 SDK → opt out with `UIDesignRequiresCompatibility`** — migrate a macOS app's top bar from a **custom `HStack` titlebar** (`.hiddenTitleBar` + a 52pt view + traffic lights overlaying a hand-tuned inset) to a **native `.toolbar` + `.toolbarRole(.editor)`** (App Shell Standard) and suddenly **every toolbar item sits in its own translucent rounded capsule**; the custom bar never had them and sibling apps (Penumbra/CropBatch) don't either. **Cause = Tahoe Liquid Glass**, which the OS auto-enrolls *native* toolbar items into **but only for apps linked against the macOS 26 SDK** — the old custom `HStack` never touched the native toolbar machinery (no glass ever), and the sibling apps look flat because they're **pre-Tahoe-SDK builds** (the "other apps don't do it" tell = SDK skew, not code). Items that already carry their own background (custom path-well, `FCPToolbarButtonStyle` pill) get an ugly **double background**: system glass capsule behind your chrome. **Fix = `UIDesignRequiresCompatibility = true` in Info.plist** — app-wide, Info.plist-only by design (glass is an SDK-linked system opt-in, no clean per-item public opt-out); the sanctioned "stay on the old look" escape hatch, correct when the app's aesthetic is fully bespoke (custom `Theme`/buttons/forced-dark — glass would clash everywhere). **The time-wasting trap:** with `GENERATE_INFOPLIST_FILE=YES` the obvious `INFOPLIST_KEY_UIDesignRequiresCompatibility: YES` **fails SILENTLY** — `INFOPLIST_KEY_*` only injects an Apple allowlist and this key isn't on it (verify: `PlistBuddy -c "Print :UIDesignRequiresCompatibility"` → "Does Not Exist"). **Working fix:** point `INFOPLIST_FILE` at a **minimal hand-managed base plist holding ONLY that one key** and keep `GENERATE_INFOPLIST_FILE=YES` — Xcode seeds from the base and **merges the generated + allowlisted keys on top** (verify both the compat key AND `CFBundleDisplayName` land). xcodegen: set `INFOPLIST_FILE` as a plain build setting so xcodegen leaves the file alone — do **not** use its `info:{path,properties}` block (it owns/overwrites the plist, see #47 gotcha #2) unless you also drop GENERATE + move all `INFOPLIST_KEY_*` into `properties`. **The migration deletes code:** drop the custom `TitleBar` HStack (wordmark/tagline + the `Spacer().frame(width:64)` traffic-light inset — AppKit owns that under `.toolbarRole(.editor)`) and the `NSViewRepresentable` WindowConfigurator (`titlebarAppearsTransparent`/`titleVisibility=.hidden`/`isMovableByWindowBackground` — native toolbar gives the drag region for free; trade-off: can no longer drag from empty content, native is drag-from-toolbar only); add `.toolbar { ToolbarItemGroup(.principal){…}; ToolbarItemGroup(.primaryAction){…} } .toolbarRole(.editor)` (attaches to the window toolbar even without NavigationStack). Cleanup tell: the old bar's `Theme.titlebar` color token is now orphaned — grep + delete. If you instead **want** the Tahoe look, do the opposite: drop your custom item backgrounds and let the glass capsule *be* the background. Source: Conjoyn `ContentView.swift`/`project.yml`. Pairs with #47, #00 (App Shell Standard), #71. |
| 90 | [90-macos-offmain-file-logger-bound-rotate.md](cookbook/90-macos-offmain-file-logger-bound-rotate.md) | **Off-main file logger — mirror an in-memory `@Observable` console feed to disk, bounded + rotated, sandbox-container-aware** — an `@MainActor @Observable` logger driving a live Debug Console is **in-memory only** (logs vanish on quit → a bug report tells you nothing) and the newest-first array grows **unbounded** (RAM leak over a long session). The `/minimums` *diagnostic logging to disk* baseline. **Naive fix is wrong twice:** `String(contentsOf:) + write(to:)` inside `@MainActor log()` does **file I/O on the main thread** (jank when logging from an export/waveform hot path) AND **rewrites the whole file every call** (O(n²)). **Pattern:** keep the `@MainActor` logger for the UI feed; hand each **already-formatted** line to a separate **non-isolated `Sendable` `LogFileWriter`** whose only job is I/O on its **own private serial `DispatchQueue`** — off-main AND ordered (the queue *is* the lock, no `NSLock`/actor hop). Append with **`FileHandle.seekToEnd` + `write(contentsOf:)`** (O(1)/line), fall back to `Data.write(.atomic)` on first write / post-rotation. **Two bounds, two lifetimes:** cap the in-memory array (~500) for **RAM**; rotate the file at a byte ceiling (5 MB → one `diagnostic.log.1`) for **disk**. **Format on the producer** (main actor) and pass a plain `String` so the writer never shares a `DateFormatter`/`ISO8601DateFormatter` across threads — ISO-8601 on disk (date-stamped/sortable/greppable), `HH:mm:ss` on screen. **Swallow I/O errors (`try?`)** — a *diagnostic* logger must never crash/stall its host (the rare place `try?` is correct, not lazy). **`Sendable` is honest** (not `@unchecked`) only because every stored prop is a `let` of a Sendable type (`URL`/`DispatchQueue`/`UInt64`). **Sandbox gotcha:** `.applicationSupportDirectory` is redirected to `~/Library/Containers/<bundleid>/Data/…` — **writable with NO entitlement** (unlike `network.client` for Sparkle), and users **can't navigate there**, so ship a **Help-menu "Reveal Diagnostic Log in Finder"** (`NSWorkspace.activateFileViewerSelecting` — *selects*, doesn't open → no Console.app launch) via a `nonisolated diagnosticLogURL`. Public API (`log`/`clearLogs`/`messages`) unchanged → existing call sites + the console view need zero edits; `clearLogs()` clears the in-memory feed only (disk is the durable record). Source: Penumbra `Utils/Logger.swift` + `App/PenumbraApp.swift`. Distinct from #16 (Sparkle auto-update — the *other* macOS ship baseline); pairs with #85 (subprocess watchdog), #75 (permission-free stats). |
| 91 | [91-detect-concurrent-cli-sessions-shared-checkout.md](cookbook/91-detect-concurrent-cli-sessions-shared-checkout.md) | **Detect concurrent CLI sessions sharing one git checkout — warn before the shared-HEAD collision** — two long-lived CLI agents (Claude Code sessions, editor terminals) open in the **same git working dir** share **one HEAD**: a `git checkout`/`switch` in one silently moves the branch for **all** of them, so a commit lands on the wrong branch (the theme-editor incident — same-machine cousin of the cross-Mac duplicate-commit class). **Git can't answer "are two sessions here?"** — it has no session concept, and the naive trigger *"is there a checkout?"* fires 100% of the time (every repo always has a HEAD → pure noise). The real signal is an **OS** question: *is a second live session's `cwd` inside this same working tree right now?* **Pattern = process inspection, not a lock file:** `pgrep -x <agentbin>` (exact name — `pgrep -f` over-matches the GUI app's helper procs / your own grep / any path substring) → each PID's `cwd` via `lsof -a -d cwd -p PID -Fn` (the `n`-prefixed field) → `git -C "$cwd" rev-parse --show-toplevel` → **group by toplevel, warn if 2+ share one.** **Three load-bearing choices:** (1) **exact-name `pgrep`**; (2) **group by worktree toplevel → worktree-aware**: two sessions in *different* worktrees resolve to *different* toplevels and DON'T trip it (separate HEADs = the safe config you're steering toward; grouping by the shared `.git` common-dir would false-positive on the cure); (3) **count ≥ 2, no self-exclusion** — at `SessionStart` your own PID is already listed, so the opening 2nd session sees count 2 and warns, a lone session sees 1 and stays silent. **Why not a PID lockfile:** it goes stale on crash/`kill -9` (blocks or false-warns forever) and a lock *inside the repo* rides Syncthing to the other Mac and lies about the host; process inspection is **stateless, self-correcting, touches neither repo nor git** (cost: one `pgrep` + a few `lsof`, <1s — fine for a hook). **Warn-only, never block** (match "remind, don't mutate" hook style); fire at `SessionStart` (flag the 2nd session *before* any checkout) and expose on-demand via a `/status`-type command. If the start script is itself a **symlink**, resolve the `readlink` chain to find the sibling detector (`dirname "$0"` points at the symlink dir, not the repo). Pair with a one-command `git worktree add ../<repo>-<name> -b <branch>` helper — the warning is wasted if the cure has friction. **Worktrees are single-machine/short-lived** (their `.git` file holds an absolute `gitdir:` → breaks if synced to another Mac; let *commits* carry work across machines). Generalizes: swap the binary name to guard any REPL/TUI/agent where two instances can share a checkout. Source: Directions `hooks/session-guard.sh` + `/worktree` + `37_multi-mac-discipline.md` Rule 5. Pairs with #85, #88. |
| 92 | [92-swiftui-greedy-clear-spacer-header-align.md](cookbook/92-swiftui-greedy-clear-spacer-header-align.md) | **Greedy `Color.clear`/`Spacer` balloons a row → pad-based inset to align a Finder-style header over custom list columns** — adding a clickable column-header row above a custom SwiftUI list (checkbox · disclosure · thumbnail · flexible title · fixed meta columns) goes wrong two ways. (1) **The header bar balloons vertically**: you reserved the left slots with `Color.clear.frame(width: 14)` so the first label lines up with the row title, and the bar fills half the pane with labels floating in a tall band. **Cause: `Color.clear` and `Spacer` are greedy in any axis you DON'T constrain** — width-only `.frame` leaves height unbounded → the view expands to the parent's full height, and in an `HStack` the tallest child sets the row height, so one unbounded clear drags the whole bar tall. **Fix: reserve the leading inset with `.padding(.leading, sumOfLeadingWidths)`, never greedy fillers** (or give `Color.clear` a finite height). (2) **A header word wraps** ("DURATION"→"DURATI/ON") when wider than its narrow numeric column → `.lineLimit(1)` + widen that one shared column (data stays right-aligned inside). **Alignment mechanism: share EVERY column width between header and rows via one constants enum** (`RowMetrics`) so they can't drift — both end with the same fixed-width columns + spacing + trailing padding, so their right edges coincide regardless of the flexible middle; the left labels align because the header's leading padding equals the row's leading element stack. Only **fixed-width** row columns can be aligned to — a `maxWidth:.infinity` title has no fixed position, so group those labels left and pad to where the title *starts* (a sub-line field like a date that isn't its own column goes in the left cluster too). Mixing two spacings in one `HStack` is impossible (one `spacing:`) → nest `HStack`s. **General tell: any `VStack`/`HStack` element mysteriously too tall/wide → hunt for a `Color.clear`/`Spacer` with an unconstrained axis.** Source: Conjoyn `Views/RecordingsList.swift`. Pairs with #00 (App Shell Standard), #70 (data-driven tile strip), #93 (the sort behind these same columns). |
| 93 | [93-swiftui-pure-sort-feed-offmain-prewarm.md](cookbook/93-swiftui-pure-sort-feed-offmain-prewarm.md) | **List-sort beachballs → keep the computed view-feed pure, prewarm the expensive key off-main, re-sort via a `@Published` token** — a list sorted by a computed property the view reads in `body` (`visible.sorted { resolvedDate($0) < resolvedDate($1) }`) **hangs the window ~1s** because the key isn't stored, it's *resolved* (read a `.SRT` cue + a filesystem stat) per row — dozens of **synchronous file opens off slow media, on the main thread, inside view-body evaluation**. Looks like the thumbnails (they're loading at the same time) but those run on an `actor` with async extraction and never touch main; **the I/O hiding in the sort comparator is the real blocker.** **Pattern: split responsibilities.** (1) **Pure sort** — `sortField` reads only an in-memory cache with an already-in-memory fallback (`cache[id] ?? embeddedValue`), **zero I/O**, so a sort can't freeze regardless of cache state. (2) **Off-main prewarm** after the scan — `Task.detached(priority:.utility)` resolves everything, passing only `Sendable` values built on the main actor first (`[(UUID, Clip?)]` + a `Date?`, never the non-`Sendable` row aggregate), then `await MainActor.run` to publish into the cache and **bump a private `@Published Int`** — which fires `objectWillChange` and re-renders every observer regardless of which property it reads, so the list re-sorts into corrected order once warm (the cache itself stays NON-`@Published` so filling it doesn't re-render per entry). **Gotchas:** `dict[key] = nil` **deletes** the key → use `updateValue(nil, forKey:)` to store a resolved-to-nil; `cache[id]` on a `[UUID:Date?]` is `Date??` so `?? fallback` treats only the *outer* absent as a miss (preserves a cached `.some(nil)`); **decorate-sort-undecorate** (map→sort pairs→drop) computes the key O(n) not O(n log n); keep a *synchronous* resolve for user-triggered small paths (rename preview over *selected* rows) — same cache, two access patterns. **Rule: a SwiftUI computed view-feed runs on the main thread inside `body` — no file/network/blocking work there, ever; push it to an async prewarm.** Source: Conjoyn `ConversionViewModel.swift` (`filteredGroups`/`prewarmStartDates`). Pairs with #92 (the header over these columns), #86 (single-flight dedup), #37 (effective-source fallback). |
| 94 | [94-quicktime-lossless-atom-date-patch.md](cookbook/94-quicktime-lossless-atom-date-patch.md) | **Lossless in-place QuickTime/ISO-BMFF date patch in pure Swift (no ffmpeg) — proven by AVAssetWriter + byte-diff** — change a video's **embedded** encode date (`mvhd`/`tkhd`/`mdhd` `creation_time`) on MP4/M4V/MOV without the ffmpeg/ffprobe dependency every reference app shells out to, and without `AVAssetExportSession`/`AVAssetWriter` (which write a *new* file → need enclosing-**directory** sandbox access, reset the FS creation date, may drop the file-grant on the new inode). **Key realization:** those date fields are **fixed-width** integers → overwriting them never changes a box size, never shifts `mdat`, never invalidates `stco`/`co64` → the edit is a few **same-length byte overwrites at known offsets**: lossless, instant, no re-encode, sandbox-correct (mutate the granted file via `FileHandle(forUpdating:)`). **Two epochs:** QT counts seconds since **1904-01-01 UTC**, Unix from 1970, gap = **2,082,844,800 s**; return **signed `Int64`** so a pre-1904 date is a negative sentinel you reject, not an 18-quintillion unsigned wrap. **Version-0 fields are uint32 → 2040-02-06 ceiling; you CANNOT widen a box to version-1 in place (changes size, shifts mdat) → reject overflow, never truncate** (atomic all-or-nothing beats leaving mvhd/tkhd/mdhd disagreeing — the #1 "didn't really work" bug). **Read through a `ByteSource` abstraction, never the whole file** (gigabyte `mdat`, and `moov` is often AFTER it) — one parser over in-memory `[UInt8]` (tests) or a seeking `FileHandle` (production, header-only reads); box scanner handles `size==1` largesize, `size==0` EOF, `uuid` (+16 usertype), with a **corruption firewall** (`boxExceedsContainer`/`invalidBoxSize` throw before any read/write past real bounds). **Field offsets:** content start `C=offset+headerSize`, `version` at `C+0`; v0 creation `[C+4…7]`/mod `[C+8…11]` uint32, v1 creation `[C+4…11]`/mod `[C+12…19]` uint64 (all BE). **Refuse `cmov` (zlib-compressed header), missing `moov`, unknown version — BEFORE touching bytes.** **Separate pure compute (`[BytePatch]` overwrite-only ⇒ can't resize) from apply** (same patch list drives memory + disk; `seek`+`write(contentsOf:)`+`synchronize()`). **Refusal-before-write ordering:** open read-only → locate → **close** → compute (may throw) → **only then** open `forUpdating` (a crash/refusal mid-op can't leave a half-written file). **Verify losslessness with NO ffprobe:** generate a real movie with **`AVAssetWriter`** (writes `moov`-after-`mdat`, the adversarial layout), patch it, assert `after.count==before.count` AND **only the patched offsets differ** (stronger than ffprobe's single-field readout — catches any stray `mdat`/box-size write), then re-parse from disk to read the target back. **Note** this patches the **binary** fields; the `udta`/`com.apple.quicktime.creationdate` ISO-8601 **string** is the only variable-length case → overwrite only if it exists AND new string is same byte length (Apple's is fixed 24 bytes), else skip+warn; filesystem Created/Modified is a third layer (`FileManager.setAttributes` + read-back verify, since it silently ignores rejected changes). Source: VEDC `VEDCFeature/Engine/` + `Dates/`, TDD/38 tests. Pairs with #52 (appendingPathComponent FS-probe), #38 (destructive-copy guard). |
| 95 | [95-macos-quicklook-thumbnail-hybrid.md](cookbook/95-macos-quicklook-thumbnail-hybrid.md) | **QuickLook-first video thumbnails with an FFmpeg fallback (hybrid) — offload poster-frame decode out-of-process + system cache, keep the subprocess path as a fallback only** — a clips/recordings list drew each row's poster by shelling out to **FFmpeg per clip** (`-ss -i -frames:v 1 -f image2pipe`); every visible row **spawns a heavyweight subprocess in your process** (so you hand-roll a 3-slot throttle semaphore + `Task.isCancelled`/SIGTERM kill-poll + once-only `ContinuationGuard`), **competes for the same disk-read budget** as the scan/metadata/SRT work firing right after a scan (visible stall on a ~95 MB/s UHS-I card), and **re-decodes from scratch on every scan** (no cache). Quiet over-extraction to look for: a ported `getThumbnails` extracts **both first and last frame** while the row only renders `first ?? last` → the last frame is a second subprocess per clip for a frame shown only if the first *fails*. **Fix = ask `QLThumbnailGenerator` first, FFmpeg fallback only on a miss.** QuickLook decodes **out-of-process** in the system Thumbnails agent (`com.apple.quicklook.ThumbnailsAgent`) so the decode load *leaves your app* (that — not "fewer processes" — is what eases contention), and is **system-cached keyed on file+mtime+size** so a re-scan returns **instantly** (the user-visible "definitely faster"); it self-manages concurrency/cancellation so the common path needs **none** of the FFmpeg machinery. **`import QuickLookThumbnailing` auto-links** (no `project.yml`/xcodegen/`.framework` change — confirm with a real build, SourceKit lies "Cannot find type"/"No such module" until the whole-module index loads). `QLThumbnailGenerator.Request(fileAt:size:scale:representationTypes:)` → `try await .shared.generateBestRepresentation(for:)` → `.nsImage`. **Three load-bearing details:** (1) **`representationTypes: .thumbnail` is a correctness trap, not a preference** — `.icon`/`.all` are *allowed to hand back the generic movie-file icon* when no content thumbnail exists, and that returns **successfully**, so your `?? FFmpeg` fallback **never fires** and every row shows the same filmstrip glyph; use `.thumbnail` only and let the throw be the miss. (2) **catch → `nil` is the fallback hinge, not error-swallowing** — QL throws for unreadable/unsupported files; the catch is the control flow that routes that clip to FFmpeg, which is what makes it *hybrid*. (3) **Demote the FFmpeg machinery, don't delete it** — keep the semaphore/kill-poll/`ContinuationGuard`, but now they throttle *only* the rare QL miss → safety-net robustness (no blank tiles ever) while the common path bypasses all of it (going *pure* QL lets you delete it, at the cost of a permanent placeholder on any file QL can't read). **Also drop the unused last frame** (keep the nil `last` field so the struct/call sites don't churn — roughly halves per-clip work). **size/scale = the deal with the daemon:** match the tile generously, ~320×180 @ scale 2 for a ~67×38 pt `.aspectRatio(.fill)` Retina tile (same `maxWidth:320` the FFmpeg path used) — too small → soft crop, too large → near-source-res posters cached for a thumbnail strip. **Caveats:** QL picks its *own* representative poster (≠ exact frame 0 — usually a feature, avoids black leader; matches Finder), so flag the behavior change; a *sandboxed* app needs the file in its granted scope (non-sandboxed reads card paths freely). **Test flip:** an assertion that *both* frames are non-nil is now obsolete → assert `first` non-nil (QL **or** fallback, don't care which) + `last` **nil**; the "missing source → empty but still cached" test is unchanged (QL throws → FFmpeg nil → both nil, result cached). Source: Conjoyn `Services/ThumbnailManager.swift`. Pairs with #86 (single-flight dedup ledger this manager already uses), #43 (subprocess fire-and-collect = the fallback), #93 (keep the view-feed pure / off-main), #75 (permission-free work). |
| 96 | [96-macos-app-indexer-cryptex-symlink.md](cookbook/96-macos-app-indexer-cryptex-symlink.md) | **macOS app indexer silently misses Safari (and Cryptex system apps) — `contentsOfDirectory(at:)` drops symlinks, use the path variant** — you scan `/Applications` etc. for `.app` bundles; the index looks complete (96 apps) but **Safari never appears** (the matcher/engine are provably correct — the candidate set just lacks Safari). Cause: on modern macOS / macOS 26 Tahoe, system apps live on the read-only **Cryptex** volume and are surfaced into `/Applications` via a **symlink** (`/Applications/Safari.app -> ../System/Cryptexes/App/System/Applications/Safari.app`), and the **URL-based** `FileManager.contentsOfDirectory(at:includingPropertiesForKeys:[.isDirectoryKey],options:[.skipsHiddenFiles,.skipsSubdirectoryDescendants])` **silently omits that symlink** (returns 47 where `ls` shows 48) — resolving `.isDirectoryKey` across the Cryptex mount for a symlink whose own `isDirectory` is *false* drops the entry. The symlink is perfectly readable: `Bundle(url:)` resolves it → `CFBundleIdentifier=com.apple.Safari` with a full `infoDictionary`; only the **enumeration** is broken. **Fix: enumerate with the path API** `contentsOfDirectory(atPath: root.path)` (returns names like `ls`, **includes symlinks**) → `.filter { !$0.hasPrefix(".") && $0.lowercased().hasSuffix(".app") }.map { root.appendingPathComponent($0) }`; downstream `Bundle(url:)`/`NSWorkspace.icon(forFile:)`/`openApplication(at:)`/`resourceValues(.localizedNameKey)` all resolve the symlink with no extra work, so no need to follow it yourself. **Tell = a count mismatch:** `ls -1 /Applications | grep -c '\.app$'` (48) vs the URL enumerator's count (47); `find /Applications -maxdepth 1 -type l -name '*.app'` names the culprit. Trade-off: the path API can't batch-prefetch resource keys (fine — an indexer reads `Bundle`/`resourceValues` per candidate anyway) and has no `.skipsHiddenFiles` (replicate with `!name.hasPrefix(".")`); still dedup by bundle id. **Rule: to enumerate a macOS dir the way Finder sees it (symlinks included), use `contentsOfDirectory(atPath:)`; reach for the URL variant only for batched key prefetch on symlink-free dirs.** Source: LaunchAway `Index/AppIndexer.swift`. Pairs with #74/#75 (permission-free enumeration), #52 (appendingPathComponent FS-probe), #97 (the diagnosis technique), #00 (App Shell Standard). |
| 97 | [97-macos-agent-app-nslog-stderr-capture.md](cookbook/97-macos-agent-app-nslog-stderr-capture.md) | **Debugging a launched macOS app: `NSLog` is invisible to `log show` for an `open`-launched app → run the binary directly and capture stderr** — you trace a running app (esp. an `LSUIElement` agent with no console UI) with `NSLog("DIAG…")`, then `log show --predicate 'process == "App"' --last 2m --info --debug` returns **zero lines** — not "no failures," *no output at all* — and you misread that absence as "the bad branch never ran," chasing ghosts. Cause: `open /path/App.app` launches via **LaunchServices** (reparented under `launchd`); `NSLog`/stderr does **not** reliably surface under a `process ==` unified-log predicate for a dev build — and the danger is that **absence looks like a clean result** (an `|| echo "ok"` guard or empty grep reads as success while the probe is disconnected). **Fix: launch the executable inside the bundle directly and redirect stderr to a file you control** — `BIN="$APP/Contents/MacOS/App"; killall App 2>/dev/null; nohup "$BIN" > /tmp/out.log 2> /tmp/err.log & disown; sleep 2; grep DIAG /tmp/err.log`. `nohup … & disown` keeps the app alive after the launching shell exits (a bare `… &` in a one-shot script gets orphaned/killed on return) while stderr stays attached; it's still a real GUI app (same Mach-O/bundle/`LSUIElement` — summon its hotkey, type, click) but its log is now a plain `grep`-able file between interactions (a `log stream | grep > file &` pipeline is fragile — the stream orphans and "completes" instantly). **Enables layered diagnosis** (each layer rules out a stratum, every probe removed after): (1) a **real-dependency unit test** proves the backend correct in isolation (→ bug is in view/binding/runtime, not model); (2) a **`body`-level `NSLog`** (`let _ = NSLog("render…"); return ZStack{…}`) separates "data didn't change" from "view didn't re-render" from "rendered stale"; (3) a **targeted reachability probe** localizes an OS quirk. `print` → stdout, `NSLog` → stderr, so redirect both (`> out 2> err`). Caveat: this is a **dev-loop** technique — for release/daemon durability use `OSLog`/`Logger` + a shipped off-main file logger (#90); gate commits on `grep -rn DIAG Sources/`. Source: LaunchAway debugging session (all `DIAG` probes removed). Pairs with #96 + #98 (the two bugs this surfaced/localized), #90 (durable shipped logger), #73 (verify-without-screen-recording). |
| 98 | [98-swiftui-foreach-id-index-stale-rows.md](cookbook/98-swiftui-foreach-id-index-stale-rows.md) | **SwiftUI list shows stale rows after the data filters → an `.id(index)` modifier silently overrode `ForEach`'s element identity** — a search/results list updates its data correctly each keystroke (you can log `results` narrowing 66→17→1) but **the screen keeps the old rows**: type "saf", the engine holds `[Kagi for Safari]` (1 item), yet the panel still renders "Accessibility" (whatever was first unfiltered). Looks like a broken `@Observable` binding; isn't — `body` *is* re-running. Cause: `ForEach(Array(results.enumerated()), id: \.element.id) { index, target in Row(target).id(index) … }` — the `.id(index)` (added so `ScrollViewReader` could `scrollTo(index)`) **overrides** the `ForEach`'s `id: \.element.id`, re-keying rows by **position**; when the list collapses 116→1, the row at index 0 keeps identity `.id(0)`, so SwiftUI **reuses the cached old view tree** instead of building a fresh row for the new `target`. Extra-confusing: `.onChange(of: results)` fires correctly (Observation tracks the value independent of row identity) so you "see" the data change and wrongly trust the binding, while the rendered `ForEach` is frozen on index identities. **Fix: key everything on the stable element id** — `.id(target.id)` on the row, and scroll to the *selected target's id* not the index (`onChange(of: selectedIndex){ guard results.indices.contains($0) ; proxy.scrollTo(results[$0].id, anchor:.center) }`); the element's own id already serves `ScrollViewReader`, so the `.id(index)` was never needed. **Rules: never identify list rows by array index when the array can filter/reorder/insert/delete; `.id(x)` on a `ForEach` child overrides the `ForEach` `id:` key — if you must attach one (ScrollViewReader/transitions/forced teardown) pass the SAME stable identity; `ScrollViewReader` needs no index ids (`scrollTo(element.id)` just works).** Tell: data observably changes but the rendered list is stale → suspect a positional `.id()`/`ForEach(0..<n)` before the model or binding (localized via a `body`-level `NSLog`, #97). Source: LaunchAway `Views/LauncherView.swift` (`scrollingResults`). Pairs with #70 (data-driven tile/result strip), #97 (the diagnosis), #00 (App Shell Standard). |
| 99 | [99-lftp-netrc-multisite-deploy.md](cookbook/99-lftp-netrc-multisite-deploy.md) | **One-SFTP-user multi-site `lftp` deploy with `~/.netrc` — no secrets in git** — deploy several sites from one monorepo to shared hosting (Strato/IONOS/OVH) over **SFTP** with **one credential** and **zero secret in any committed file**. **Headline gotcha:** `lftp`'s sftp backend shells out to external `ssh` (`ssh -a -x -s HOST sftp`), and **`ssh` takes the username only from the URL, never from `~/.netrc`** → `lftp sftp://HOST` (no user) connects as your *local* user → server drops you → `Connection closed by <ip> port 22` (mimics a fail2ban ban, see #88, but it's just a missing username). **Fix = user in the URL, password in netrc:** `lftp sftp://USER@HOST` makes ssh get `-l USER`, and lftp then matches the *password* from `~/.netrc` by `(machine, login)` — nothing on the command line, nothing in the script (debug tell: `lftp -d` shows the connect program; missing `-l` = the bug). **Split the credential by sensitivity:** password → `~/.netrc` (chmod 600, lives in `$HOME` **outside** the repo → never in the Syncthing tree or git, recreated per-Mac); host+user → gitignored `.env` (identifiers, not secrets → convenient to sync). Write `~/.netrc` with a **single-quoted heredoc** (so `$`/`?`/`^` in the password aren't shell-expanded) under `umask 077`; verify by **length** (`awk '/password/{print length($2)}'`), never by printing. **Structure:** shared `_deploy-common.sh` (sourced — centralizes auth + a `deploy_mirror <src> <remote>` fn) + **thin per-site `deploy.sh`** that self-locates via `BASH_SOURCE` and calls it + a `deploy-all.sh` loop. **Safety rails:** `DRY_RUN=1` → `--dry-run` AND skips local mutations (a preview touches nothing); **no `--delete`** (mirror only adds/updates → never prunes server-written state); **exclude server-managed files** (`feedback/public.json`, `submissions.log`) so deploys don't clobber live data — but that means a **first deploy to an empty server omits them → seed the empty `public.json` once with a one-off `put`** (access-control `.htaccess` files are NOT excluded, so they ride up and create `private/`); **normalize 644/755** before upload (macOS's stray 0600 → Apache 403). **Un-ignoring the now-sanitized `deploy.sh`:** old `.gitignore` blanket-ignored `deploy.sh` *because it held a password*; once sanitized, commit them — but **overwrite the plaintext first, then un-ignore, then PROVE clean before committing** (`git grep --cached` for the password/user; confirm `.env` not staged), and watch for a *sibling* site's own `deploy.sh`+`.deploy.env` un-ignoring at the same time. **Pitfalls:** multi-line `lftp -e "…"` parses unreliably (use one `;`-separated line); `~/.netrc` is per-Mac/not-synced (fresh Mac has `.env` but no password → document recreation in `.env.example`). Source: App-Websites monorepo (apps.lucesumbrarum.com + Penumbra + Aloft → one Strato user). Pairs with #88 (ban vs auth failure), #46/#49 (the PHP backends → the server-managed files you must exclude), #29 (Strato hosting). |
| 80 | [80-watchconnectivity-phone-watch-state-mirror.md](cookbook/80-watchconnectivity-phone-watch-state-mirror.md) | **Phone ↔ Watch full-state mirror over WatchConnectivity — App Groups DON'T cross devices** — the watch app/complication reads an App Group `UserDefaults`, but `group.X` on iPhone and on Watch are **separate sandboxes that never sync**; the only bridge is WatchConnectivity. Bites silently when the connectivity layer was built as a *control channel* (watch → phone "do the thing") and nobody ever sent the **data** back → watch reads a store only its own writes populated (empty on fresh install). A **complication** exposes it instantly (pure consumer, no other data source). **Fix = mirror full state phone→watch.** (1) **Hook the push at the persistence choke point**, not an existing "on save" callback something else owns (here `CloudSyncManager` claims `onGroupsSaved`; a 2nd `=` clobbers it — last-write-wins) → add a separate `onGroupsPersisted` so local **and** iCloud-received changes both propagate. (2) **`updateApplicationContext`** (latest-state-wins, delivers when reachable even if watch was asleep — right transport for full-state; `transferUserInfo`=ordered queue, `sendMessage`=live foreground only) + **push on session activation** to re-seed on plain launch; payload is plist-only so encode model → JSON `Data`. (3) **⚠️ Eagerly activate the WCSession at `@main`** (hold the singleton in the watch `App` struct) — `didReceiveApplicationContext` only fires if the delegate was set **before** the context arrived; a lazy singleton touched only in a toggle handler **drops the first (seed) push on cold launch** ("works after I interact, not on launch"); also drain `receivedApplicationContext` on activation. (4) **Merge rule** = phone-authoritative (`return incoming`, simple/consistent, can revert a just-made watch edit in the ~1s round-trip) vs per-item `max(lastModified)` (safer when a revert = oversleep); keep it one swappable function. **No loop back:** the receive→save path must NOT re-trigger a watch→phone sync ping. Same App Group in **every** target's entitlements (incl. the watch widget ext) or `getWidgetData()` reads an empty suite. Complication freshness == last push, so `reloadAllTimelines()` in the receive path. **Tell:** watch shows empty on device but works in isolation, or a `…ConnectivityManager` has only send methods + no `didReceive*`. Pairs with #06, #29. |

---

## Quick Reference Table

| Pattern | Source Project | Use Case |
|---------|---------------|----------|
| **App Shell Standard** | **Penumbra** | **MANDATORY — base for all macOS apps** |
| FCPToolbarButtonStyle | Penumbra | Flat 4px toolbar buttons, replaces round |
| PaneToggleButton | Penumbra | Toolbar toggle with FCPToolbarButtonStyle |
| Theme struct | Penumbra | Dark color palette (0.10/0.15 grays) |
| .hiddenTitleBar + .dark | Penumbra | No system chrome, forced dark mode |
| .toolbarRole(.editor) | Penumbra | Editor toolbar, no nav chrome |
| HSplitView + @AppStorage | Penumbra | Togglable panes with persisted visibility |
| InfoStripView | Penumbra | Contextual bar below toolbar |
| Separate view structs | swiftdifferently.com | Performance (diffing checkpoints) |
| .equatable() modifier | swiftdifferently.com | Views with closures |
| debugRender() extension | swiftdifferently.com | Visualize re-renders |
| NavigationSplitView | Directions | Sidebar navigation |
| HSplitView (simple) | TextScanner | 2-pane layouts |
| HSplitView (complex) | Phosphor | Preview + timeline |
| HSplitView (3-section) | AppUpdater | Sidebar with header/footer |
| Multi-window + Menu Bar | WindowMind | Background utilities |
| Autosave dividers | Penumbra, VCR | Remember pane sizes |
| NSTableView in SwiftUI | VCR | Column headers, cell reuse, native table |
| AppKitButton | Convention | Native NSButton, replaces SwiftUI Button |
| AppKitCheckbox | Convention | Native checkbox toggle |
| AppKitPopup | Convention | Native NSPopUpButton dropdown |
| AppKitSegmented | Convention | Native segmented control |
| AppKitSlider | Convention | Native NSSlider |
| AppKitTextField | Convention | Native NSTextField input |
| AppKitToolbarButtonStyle | Penumbra | Native look in SwiftUI .toolbar |
| NSSavePanel + progress | Phosphor | File export |
| NSOpenPanel (folder) | Directions | Folder selection |
| Security-scoped bookmarks | Directions | Persistent folder access |
| .fileImporter + drag/drop | CropBatch | Image picking |
| @main + .task init | MusicClient | Service initialization |
| Scene phase handling | Group Alarms | iOS lifecycle |
| Manager.configure() | MusicClient | Dependency injection |
| FolderManager | MusicServer | Bookmark restoration |
| **Layout Template A: Browser** | **FCP, Penumbra** | **Sidebar + grid + inspector** |
| **Layout Template B: Editor** | **FCP, Phosphor** | **Viewer + timeline + sidebar** |
| **Layout Template C: Organizer** | **AppUpdater** | **Source list + full detail** |
| **Layout Template D: Dual Viewer** | **FCP compare** | **Side-by-side / overlay / wipe** |
| **Layout Template E: Workspace** | **FCP tabs** | **Tab-switched distinct layouts** |
| KB Tier 1: SwiftUI Commands | VideoScout, Penumbra | Menu-bar shortcuts (Cmd+key) |
| KB Tier 2: .onKeyPress | QuickMotion, VideoScout | View-level JKL, arrows, space |
| KB Tier 3: NSEvent local monitor | Penumbra, VideoWallpaper | App-wide single-key, consume events |
| KB Tier 4: KeyboardShortcutManager | Penumbra | User-customizable, recordable |
| Context menu: basic | ClipSmart | Simple action list on rows |
| Context menu: conditional | VAM | State-driven items |
| Context menu: extracted + submenus | VideoWallpaper, FileManagement | Reusable, nested menus |
| Context menu: NSMenuDelegate | VCR | NSTableView row menus |
| Selection: single `@Binding` | VideoScout | `List(selection:)` + `.tag()` |
| Selection: multi `Set<ID>` | Penumbra | `Table(selection:)`, batch ops |
| Selection: grid + keyboard nav | VideoScout | `LazyVGrid` + arrow keys |
| Selection: NSTableView sync | VCR | `isUpdatingSelection` loop guard |
| Selection: cross-pane observable | Penumbra | `@Observable` shared model |
| Selection: two-level | VAM | Sidebar category + item binding |
| Drop: basic `.onDrop` | CropBatch | File drop zone + highlight |
| Drop: typed handler utility | QuickMotion | Reusable `VideoDropHandler` |
| Drop: concurrent TaskGroup | Penumbra | Bulk multi-file import |
| Drop: internal reordering | Phosphor | `.draggable` + `.dropDestination` |
| Drop: NSTableView | VCR | `registerForDraggedTypes` + delegate |
| Drop: AppKit NSView subclass | TimeCodeEditor | `NSDraggingDestination` override |
| Progress: status bar | VCR | `.safeAreaInset` bottom bar |
| Progress: inline in bar | Penumbra, VAM | Spinner + text when busy |
| Progress: determinate + cancel | Phosphor, CutSnaps | Export bar + % + cancel |
| Progress: multi-level | VideoScout | Overall + per-item bars |
| Progress: metrics panel | P2toMXF | Elapsed / ETA / speed chips |
| Progress: floating overlay | VideoScout | Slide-up `.bottomTrailing` |
| Progress: phase indicator | VOLTLAS, VCR | Color-coded stage icons |
| Progress: footer swap | P2toMXF | Normal actions → progress+stop |
| Workspace: view mode toggle | Penumbra | Grid/list/single via toolbar |
| Workspace: tool mode picker | CropBatch | `.segmented` picker, controls swap |
| Workspace: sidebar-driven | VOLTLAS | `@ViewBuilder switch` on enum |
| Workspace: @AppStorage persist | VideoScout | Mode survives relaunch |
| Workspace: nested sub-modes | VOLTLAS | Outer phase + inner variant |
| TC font: SF Pro .monospacedDigit() | Penumbra | Timecode without slashed zeros (FCP-style) |
| Jinja2 data injection | PDF2Calendar | Server→client data passing |
| ES Module DI | PDF2Calendar | Avoid circular imports in JS modules |
| Shared State Module | PDF2Calendar | Centralized state for vanilla JS apps |
| launchd KeepAlive server | X-STATUS | Node.js server auto-start + auto-restart |
| launchd scheduled task | X-STATUS | Daily data collection (cron replacement) |
| Install/uninstall scripts | X-STATUS | Idempotent launchd agent management |
| **Volume custom icons (two-step write)** | **Sigil** | **`.VolumeIcon.icns` + FinderInfo xattr** |
| **VolumeEnumerator + external heuristic** | **Sigil** | **`!isInternal && !DMG` filter** |
| **NSWorkspace → AsyncStream bridge** | **Sigil** | **Mount/unmount events via structured concurrency** |
| **Managed Developer ID GUI workflow** | **Sigil** | **Archive → Direct Distribution, no local cert** |
