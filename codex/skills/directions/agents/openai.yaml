interface:
  display_name: "Directions"
  short_description: "Shared Claude and Codex project workflows"
  default_prompt: "Run status for this project using the shared Directions workflow."

policy:
  allow_implicit_invocation: true
