# Update Directions

Pull latest Directions from GitHub to the current project's docs folder.

```bash
cd docs && git pull origin main
```

If there are local changes, warn the user before pulling.

After update, summarize what changed (check git log).
