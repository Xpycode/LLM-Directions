# Session History

## Active Project
LLM-Directions - Documentation system for AI-assisted development

## Current Status
→ See [PROJECT_STATE.md](../PROJECT_STATE.md)

## Sessions

| Date | Focus | Outcome | Log |
|------|-------|---------|-----|
| 2026-06-14 (b) | First `AppCitizenshipKit` integration (Conjoyn) + harden the package + cookbook #108 + Directions | **Proving-ground migration.** Chose **Conjoyn** (the app the patterns were extracted from, #102/#104) over DiskVerdict — it already hand-wires Feedback+Donate+Help, so this is a *migration*, deliberately used as a **differential test** of the new package. Replaced `FeedbackCommands` + the hand-rolled `DonateCommands` struct with one `CitizenshipCommands(citizenship)`; `import FeedbackKit`→`import AppCitizenshipKit`; dropped the now-transitive direct FeedbackKit dep; added the link-rich About. **The migration exposed 3 package defects, each fixed UP in the package + re-tagged (not patched per-app):** (1) an **ellipsis the package's own #104 forbids** (`"Support <App>…"`, a browser hand-off) → dropped; (2) a **missing Feedback↔donate divider** — *load-bearing finding:* a separator between two items a **nested `Commands` emits internally** must be emitted by the package (the host's declaration-order #104 handle doesn't reach inside) → package emits a leading `Divider()`; (3) a **naming collision** — "Support"=help-desk (clashed w/ the web Support page, same ambiguity #105 fixed) + "Donate"=charity → **tip-jar framing: "Leave a Tip"/"Tip Jar"**, baked into the package so all apps inherit it. Published `Xpycode/AppCitizenshipKit` **0.1.1** (ellipsis+divider) + **0.1.2** (stale doc comments) — `swift build`+4/4 tests each; Conjoyn flipped local `path:`→`url/from:0.1.2`, **builds under Swift 6 strict concurrency** against the tag. **Verified the LIVE menu** via AppleScript (Commands aren't unit-testable): Help = *Conjoyn Help · — · Send Feedback… · — · Leave a Tip*; App = *About Conjoyn · Check for Updates… · —* (ACK `replacing:.appInfo` coexists w/ Sparkle `after:.appInfo`). **"Check again" ×2 paid off:** caught Conjoyn already had the surfaces (task → migration), then caught **5 stale "Support Conjoyn" comments** post-rename (3 already pushed) → grep the OLD term *incl. comments* after a rename. **Cross-Mac:** fetch-before-add found `ahead+behind` w/ **two #106s** (unpushed local disabled-control vs origin's pushed xcodebuild) → Rule 1 reconcile: published keeps #106, **renumbered local → #107** (+ fixed its mis-stamped "# 100" H1); a concurrent `/cookbook` writer (`52bcb07`, QuickScreenShot 64/65 gotchas) landed mid-session w/o collision. Pushed Conjoyn (3 commits) + master (#106→#107 renumber, **#108**, Directions). Signed build still needs the FDMSRXXN73 cert (multi-Mac gap, pre-existing). | [log](2026-06-14-b.md) |
| 2026-06-14 | Portfolio-wide "app citizenship" audit (Help/Feedback/Donate/About/Updates/etc.) + build `AppCitizenshipKit` | **(1) Audit (7 parallel agents over 30 real macOS apps):** scored each on 9 cross-cutting features. Headline gaps — **Donate 0/30**, Feedback 2/30 (Conjoyn, DiskVerdict), custom About 1/30 (ClipSmart), real Help 3/30, Onboarding 3/30, editable shortcuts 6/30, Sparkle-wired Updates 6/30, **AppIcon broken/empty 19/30** (separate shipping blocker); Settings 15/30 (only reliably-built feature). Best templates: ClipSmart + Penumbra (5/9). Flagged 2 defects (AutoRedact empty appiconset; AvidMXFPeek links Sparkle but no code uses it). **(2) Infra reality-check (corrected assumptions):** FeedbackKit = drop-in SPM, published `Xpycode/FeedbackKit` 0.1.2, consumed by URL; HelpMenu (appHELP) = not even a git repo, deliberately **vendored** per-app; ShortcutKit (zPackages) = local-path only, no remote; **AppUpdater is NOT a reusable updater** (standalone MacUpdater-clone app; self-update = Sparkle directly); **PaymentOptions = docs/design only** (paid-licensing R&D, not donations — donate is the web `donate.html?app=<slug>` link, cookbook #100, no Swift helper existed). **(3) Built `AppCitizenshipKit`** at `zPackages/AppCitizenshipKit` (sibling to FeedbackKit). **Design = Option B:** wrap only what's both missing AND cleanly packageable → Feedback (re-export FeedbackKit via URL) + Donate (new, the 0/30 gap, first reusable Swift donate helper) + About (native panel + Website/Support/Privacy links); left Help (vendored) & ShortcutKit (local) alone to keep one clean URL dep. API: `CitizenshipConfig(appID:appName:…)` (slug doubles for feedback allow-list + donate `?app=`; lucesumbrarum URLs default) → one-line `CitizenshipCommands(config)` + standalone `DonateLink` + `CitizenshipAbout.show`. `swift build` clean, `swift test` 4/4. README + per-app rollout checklist. Not yet: git/GitHub publish, first-app integration, `/minimums` update, AppIcon sweep. | [log](2026-06-14.md) |
| 2026-06-13 (b) | Resolve cross-project status view + multi-Mac sync architecture + build `/arrive`+`/depart` | **(1) Cross-project status view (parked since 2026-03-24):** chose Option B — a disposable, gitignored `dashboard.py`→`dashboard.html` (top-down scan of all 47 `PROJECT_STATE.md` + git, search/filter/stale-dim) over the committed `project.json` generator+aggregator+triggers (same shape that sank prior web-dashboards; violates the HTML-effectiveness "markdown stays in git" rule). First run doubled as a fleet health check: 6 drifted PROJECT_STATE, only zPackages blocked; fixed 2 parser bugs. ADR + condensed-log in `decisions.md`. **(2) Sync architecture:** diagnosed the two-copies setup (`/Users` 6.5 GB `.git`-stripped vs `/Volumes/sim` 29 GB full git); corrected a false "44 projects unbacked" alarm (real copy: ~33/46 gitted, ~15+ on GitHub). Evaluated a Syncthing/`.git` research brief — agreed with its diagnosis + "respect git, dumb-courier the syncer" principle, but it optimizes for a no-cloud constraint the user lacks for code → recommended **private GitHub for code, Syncthing for assets, neither for regenerable junk**; rejected whole-`.git`-sync (no remote = no fallback). Proposed a tier-based folder layout (A/B/C) over the `01_/02_/03_/04_/docs` convention (codifying into `13_` deferred). **(3) Built `/arrive`+`/depart`** (`commands/arrive.md`,`depart.md`): `/arrive`=read-only fetch→plain behind/ahead + other-Mac's last commit + where-you-left-off; `/depart`=session-close hygiene + commit (stamped `Handoff-from: <Mac>` trailer) + push. **Decision: machine identity in git's commit trailer, not a tracked `.md`** (immutable, conflict-free; rejected the timestamp-in-md footgun). Mac label from `~/.claude/this-mac` (outside Syncthing) w/ `LocalHostName` fallback. Wired into `/directions` + global menus; copied live to `~/.claude/commands/`; trailer round-trip verified (git 2.50). Other Mac gets them via `/update-directions`. First real `/depart` run pushed this session. | [log](2026-06-13-b.md) |
| 2026-06-13 | Reconcile a behind-10 + dirty tree (erased-Mac Syncthing phantom) + harden hook/doctrine | Session-start pre-flight flagged `behind 10` with a dirty tree (9 modified + 12 untracked). Diagnosed as a **Rule 1a dual-carry phantom**, not a duplicate-commit: the *other* Mac had been erased-and-restored, Syncthing (excludes `.git`) carried the current files here onto a stale `.git` (`d139ff3`, 10 behind tip `5255d19`), so every "local change" was already on origin. **Proved byte-identity** file-by-file (`git diff --quiet origin/main` on tracked; tree-presence + content match on untracked) and **coordinated with the reset Mac's open session** (read-only diagnostic prompt → it was clean at `5255d19`, no unique work). Reconciled losslessly in one move: **`git reset --hard origin/main`** (the 12 untracked files were in the target commit → checked out clean; `git clean -nd` empty after). Then hardened: **(1)** `hooks/session-start.sh` now computes the `behind+dirty` verdict by hashing each dirty path (`git hash-object`) against origin's blob (`git rev-parse origin/main:<f>`, ≤200 files) → prints a *confirmed* "byte-identical phantom, lossless to reset --hard" vs a per-file caution (was a guess); `bash -n` clean, oracle verified. **(2)** Rule 1a in `37_multi-mac-discipline.md` gained the one-move `reset --hard` recovery, the hash-object oracle, the **`diff -q`-in-a-pipe SIGPIPE/pipefail gotcha** (three checks disagreed; hash/`git diff --quiet` were right), the computed-hook note, source incident, and a cheatsheet-row update. | [log](2026-06-13.md) |
| 2026-06-11 | Same-folder Claude session collision guard + `/worktree` | Two Claude sessions in one folder share a single git checkout (one HEAD) — a `checkout` in either moves the branch for **both**, so a commit lands on the wrong branch (the theme-editor incident). Sharpened the user's `/status` "checkout check" idea: the real signal isn't "a checkout exists" (always true) but "is a **2nd live `claude` session in this folder**?" — which git can't answer but the OS can. **(1)** New `hooks/session-guard.sh` — warn-only detector: `pgrep -x claude` → `lsof` cwd → `git rev-parse --show-toplevel`, warns when 2+ sessions share one **worktree toplevel**. Worktree-aware (different worktrees don't false-positive); pure local process inspection, no lock files, never touches git. Caught a **real live collision** (two sessions in Penumbra on `feature/theme-editor`). **(2)** Wired into `session-start.sh` (resolves its own symlink to find the sibling; flags a 2nd session before any checkout) and `/status` (stable path + master-repo fallback); `install.sh` symlinks it per-Mac. **(3)** New `/worktree` command — confirmed helper that sets up `../<repo>-<name>` with its own HEAD for the parallel session, with the single-Mac/short-lived caution. **(4)** Doctrine: added **Rule 5** to `37_multi-mac-discipline.md` (same-Mac axis, orthogonal to the cross-Mac Rules 1–4) + triggers + 2 cheatsheet rows + closing-note edit. Commits `da7fb6d` (feature) + `1670755` (doc), both ff-merged to `main` and **pushed**; branches deleted, tree clean. | [log](2026-06-11.md) |
| 2026-06-10 | Statusline cleanup + build the model-switch reminder system | Three statusline/UX asks. **(1)** Removed the broken `⏱ 7d … (%)` weekly-limit readout — ripped out the `ccusage` background-refresh job, lock/cache plumbing, `usage_seg`, and orphan `fmt_tok`/`fmt_left` helpers; deleted stale cache files. **(2)** Color-coded model names by tier (Fable=red 203 / Opus=orange 208 / Sonnet=green 71 / Haiku=white 255) — passive cost cue; first edit refused because `~/.claude/statusline.sh` is a **symlink** into the repo → edited the real `claude-statusline/statusline.sh`. **(3)** Built the enforcement of the 2026-05-31-b standing preference: a **keyword hook + statusline handshake**. New `hooks/model-advisor.sh` (`UserPromptSubmit`) keyword-matches the prompt to a tier (`EXEC_RE`/`PLAN_RE`/`TRIVIAL_RE`/`HARD_RE`), and on a real mismatch shows a one-line `systemMessage` nudge (debounced, bidirectional). Statusline writes the live model to `~/.claude/.current-model-<sid>` and reads back a `~/.claude/.model-suggestion-<sid>` to render a persistent ` ⮕ sonnet?` hint that auto-clears on switch. Registered the hook in `settings.json`; session-start now prunes stale state files. Verified end-to-end (7 hook scenarios + handshake), `bash -n` clean. Chose hook over CLAUDE.md-only for **deterministic** reminders. Researched lineup/pricing via `claude-api` skill. Reconciled the 5th cross-Mac duplicate (dropped a byte-identical dup #86 commit), committed + **pushed `93c3632`**; in sync. User switching Macs — machine-local hook re-setup noted in PROJECT_STATE Resume. | [log](sessions/2026-06-10.md) |
| 2026-06-08 | Ship the copy-vs-drift fix + reconcile cross-Mac divergence | Executed the parked RESUME plan. **(1)** Added a ~45-row **Directions Index** (topic→master-doc, from live `TRIGGERS:`) to live `~/.claude/CLAUDE.md` + tracked mirror `CLAUDE-GLOBAL-TEMPLATE.md`. **(2)** `/setup` now scaffolds **only project-specific files** — universal `00–61` docs read on demand, not copied; **sentinel `00_base.md` → `PROJECT_STATE.md`** across `commands/setup.md`, template, live config. **(3)** Found the template & live config had themselves drifted (live proof of the flaw) → re-aligned. Also reconciled `main` divergence: origin had duplicate cookbook #80/#81 from another Mac, local a clean superset → `-s ours` merge, pushed `effe3f2`. `46_main-menu.md` was already committed (59ccfae). RESUME.md retired. **Follow-up (same day):** this Mac had done the *same* fix in parallel (uncommitted); `fetch` showed origin ahead-9 with the work already shipped from the other Mac (4th cross-Mac collision). 9/10 files byte-identical → `reset --hard origin/main` dropped the dup commits; the one real gap was `commands/update-directions.md` (other Mac left it on the old copy-model) → fixed + pushed `0897333`. Added a **session-start `git fetch` pre-flight** to live + template global config to stop the recurrence, pushed `499b314`. | [log](sessions/2026-06-08.md) |
| 2026-06-07 | Main-menu house-style + Directions copy-vs-drift fix (spilled from zPackages) | Drafted `46_main-menu.md` (main menu = complete command inventory). Diagnosed copy-vs-drift flaw → proposed Index-in-global-CLAUDE.md + `/setup` change. **Fix shipped 2026-06-08.** | [log](sessions/2026-06-07.md) |
| 2026-06-06 | Evaluate "Unreasonable Effectiveness of HTML" for a cross-project status dashboard | Read Thariq's demo site + official Claude blog + ChatPRD interview + 20 example artifacts (no benchmarks; author = self-described HTML maximalist). Thesis: human-facing output → self-contained `.html` beats markdown (density, >100-line readability, shareable links, two-way interaction, joy). **Decision rule:** HTML for human-facing/comparisons/dashboards; **markdown stays for short outputs, chained agents, anything in a git repo, anything iterated weekly.** Surveyed implementing skills — `dogum/html-artifacts` (faithful essay port), **`voidful/claude-html-report-skill`** (reports/→GitHub Pages w/ manifest+landing page, most relevant), `ghoulvspol/html-effectiveness-skill`, `careerhackeralex/visualize`. **Key diagnosis:** past web-dashboard attempts failed because they were *web apps* (server/build to maintain) — the HTML-artifact pattern has no app/server/build; agent reads filesystem→emits one static html. HTML helps by *deleting* the website idea. Cautions: keep dashboard a disposable/gitignored snapshot (per essay's own git carve-out), don't HTML-ify cookbook/Directions docs, usefulness gated by PROJECT_STATE.md quality. Rec: cheap one-off `dashboard.html` first; install voidful skill only if reused. Connects to 2026-03-24 generator design — reconcile before building. | [log](2026-06-06.md) |
| 2026-05-31 (b) | Standing preference: proactive model switching | Reviewed the current Claude Code model lineup (**Opus 4.8** `claude-opus-4-8` — hard reasoning/architecture/debugging/planning; **Sonnet 4.6** `claude-sonnet-4-6` — well-specified day-to-day coding; **Haiku 4.5** `claude-haiku-4-5` — mechanical edits/searches/doc updates; plus **Default**, **Opus Plan Mode** = Opus-plans→Sonnet-executes, **`/fast`** = faster Opus not a downgrade, and per-subagent overrides). Established the switch heuristic — reasoning-bound→Opus, spec-bound→Sonnet, search/grind→Haiku, mixed→Opus Plan Mode — to stop burning Opus on everything. Saved `feedback_proactive-model-switching.md` (+ MEMORY.md pointer): Claude flags the cheaper tier at the START of a task and, at plan↔execute boundaries, reminds the user to verify the active model via `/model` (Claude **can't** see the live model — env context is only a session-start snapshot). | [log](2026-05-31-b.md) |
| 2026-05-31 | Make performer-voice "mood lightener" system global | Imported user's `VOICES.md` (21 comedian/character voices) and promoted it from project-scoped to **global**: wrote `~/.claude/VOICES.md` (full roster + rules) and added a "Performer Voices (Mood Lightener)" section to `~/.claude/CLAUDE.md` so it loads in every project. Established a **TARS-style dial** (default 40%; user says "go to 80% voices" / "drop to 20%") and a **🎭 blockquote marker** (`🎭 *(VoiceName, NN%)*`) so the user can visually tell when Claude is joking — plain text = serious. Hard rule: never voice genuinely bad news (data loss / security / destructive ops). Updated project memory `feedback_voice-level-dial.md` to point at the global home. Test-heavy sprint explicitly deferred to a future session. | [log](2026-05-31.md) |
| 2026-05-28 | Apple developer credentials audit + correction | Discovered team IDs were swapped in `~/.claude/apple-developer.md` (FDMSRXXN73 = paid, not free — confirmed via Membership Details + Certificates portal screenshots). Rebuilt credentials file with full cert inventory (10 certs), complete App ID list, and "New Project Checklist" section with renewal deadline 2026-09-27. Verified all 4 published apps (syncthingStatus, VideoWallpaper, Group Alarms, ProjectProgressTracker) already use correct team — code was right, docs were wrong. | [log](2026-05-28.md) |
| 2026-05-16 | SFTPmount Wave 0 Step 3 attempt → pivot to 26.5 re-validation → canonized `37_multi-mac-discipline.md` | Pre-flight on M1 Max revealed Step 3 blocked-on-M4-Pro: no `FSKitExp.app`, no `~/scratch/sftpmount-spikes/`, no `pluginkit`/`systemextensionsctl` registration of our extension, no `fskitd` log entries on 2026-05-09. **Hard evidence: spike was on M4 Pro as journal header states.** Pivoted to three parallel read-only checks on macOS 26.5: **(A)** all 6 rev-3 corrections re-validated against current `com.apple.fskit.ftp.appex` Info.plist + entitlements — 5 confirmed, 1 needs update (`LSMinimumSystemVersion`: 26.4 → 26.5); also surfaced 6 additional Info.plist keys to mirror Apple's stock pattern + entitlement decision to drop `com.apple.security.network.server` for SFTP. **(B)** log archaeology (`log show --last 7d`) confirmed no spike traces on this Mac. **(C)** FSKit framework surface on 26.5 SDK (21 public headers + swiftinterface) validated rev-2's `@main UnaryFileSystemExtension` approach exactly. Wrote findings into `01_Project/spike-r2-fskitd/NOTES.md` (143L → 207L) with a 5-item rev-3 punch list ready for next M4 Pro session. SFTPmount commit `f146c66` pushed. **Late session: canonized `37_multi-mac-discipline.md`** (279L) — three cross-Mac recurrences in two weeks (`.claude/settings.local.json` union-merge 2026-05-14, SFTPmount duplicate spike commit 2026-05-15, M1-vs-M4 spike-context discovery today) crossed the "needs a doc" threshold. Structure mirrors `39_libsql-turso-sync.md`: mental model + 4 rules (`git fetch` first / verify machine state on actual machine / union-merge accumulating files / pivot-when-blocked) + 30-second pre-flight + cheatsheet. Inbound refs wired in `00_base.md` + `01_quick-reference.md`. Master commit `b372d58` pushed. **30s discipline range now contiguous 30–39.** **3 commits on `origin/main`** (`1618ebf` session+state, `8bf78ce` allowlist, `b372d58` slot-37 doc) | [log](2026-05-16.md) |
| 2026-05-15 | Audit punch-list cleanup: AutoRedact orphans + consumer-index backfill + script regex fix | **AutoRedact** (not git-tracked): split inline 4-session file `2026-04-05.md` into per-session files `.md`/`-b.md`/`-c.md`/`-d.md` (43/57/44/34 lines). Backup retained `.pre-split-backup`. **Consumer indexes:** added rows in MousePlus (`2026-05-01-a`, local), SFTPmount (`2026-05-09-spike`, pushed as `1eaf000`), KinoBerlin (`2026-01-16-tmdb-ratings-plan` with `(artifact)` label, local-only repo, `089137f`). **Group Alarms** false-positive — entries were already indexed via `(./<file>.md)` bullet-list links the audit regex didn't accept. **Script patch** (`5c1ca10`): `sync-session-index.sh` regex now accepts both `(<file>.md)` and `(./<file>.md)` link forms; verified `✓ in sync` across 11 projects (4 backfilled + 6 regression-test + master self-audit). **SFTPmount duplicate-commit recovery:** another Mac had pushed near-identical spike work; `git reset --hard origin/main` dropped my duplicate, re-added the unique `_index.md` row. **3 commits on `origin/main`** (`5c1ca10`, `246fbd4` allowlist chore, plus SFTPmount's `1eaf000` on a different remote). Audit punch list now 1 item remaining (`/session-close` dogfooding — can't be done retroactively) | [log](2026-05-15.md) |
| 2026-05-13 → -14 | Cross-project Directions audit: six quick-wins + master backfill + 10-consumer sweep + full sync-conflict cleanup + M2 complete | 4 parallel Explore agents scanned 30 projects / 434 logs; web cross-check (AGENTS.md standard, Kiro SDD, 2026 CLAUDE.md guides). **Q2:** rename `55_→36_`. **Q5+Q5b:** archived `/0-DIRECTIONS/` monolith + parallel `docs/`; umbrella dir 30 → 6 entries. **Q4:** `28_xcode-signing-and-sourcekit.md`. **Q1:** `sync-session-index.sh` + `/check-index`. **Q3:** `/session-close` 6-step checklist. **Master backfill:** 9 missing rows. **Consumer cleanup:** 29 bootstrap-contamination files removed across 4 consumers. **Sync-conflict cleanup:** all 12 conflicts under `/ProgrammingProjects/` resolved. **Syncthing prevention:** `.stignore` ignores `.git`, `.DS_Store`, `.claude/settings.local.json`, signing xcconfigs, build/cache dirs. **M2 complete:** `29_web-strato-hosting.md` (230L, 4 web projects); `39_libsql-turso-sync.md` (198L, LEARNING libSQL CDC); `38_ios-swiftui-state.md` (247L, 5 iOS state patterns from Group Alarms). 20s gotchas contiguous 20–29; 30s discipline now 30–36 + 38–39. **M1 complete** (`b5b1d19`): consolidated `50_progressive-context.md` + `52_context-management.md` + `58_context-engineering.md` (1042L of overlap) into canonical three-part `52_context-management.md` (Architecture + Runtime + Information Design); 50 and 58 become breadcrumb stubs; inbound refs updated in 3 files. Audit punch list **fully closed**. **13 commits on `origin/main`** | [audit-day](2026-05-13.md) · [exec-day](2026-05-14.md) |
| 2026-05-10 | Admin: commit pending settings, push | Committed `f098483` (Claude Code permission allowlist additions from 2026-05-01 cleanup), pushed `6e42766..f098483` to `origin/main` | [log](2026-05-10.md) |
| 2026-05-01 | Flatten master structure to fix consumer-bootstrap friction | Moved `docs/cookbook/` → `cookbook/` (62 entries, links rewritten); consolidated drifted `sessions/` folders; replaced `git clone … docs` bootstrap with rsync-with-excludes (no embedded `.git`, no tool-cache bloat); shipped `mcp-templates/`; added cookbook entries 40–61; 2 commits `8b495e5` + `57f2e9a` pushed to `origin/main`; patched BatchTextExtractorPlus inline | [log](2026-05-01.md) |
| 2026-04-28 | Diagnose & prevent codebase-memory-mcp CPU runaway | Killed PID 22328 (837% CPU, 21 GB RAM), freed 5.6 GB by deleting 3 umbrella DBs; wrote PreToolUse guard hook (concrete + templated); shipped `27_mcp-gotchas.md`, `hooks/mcp-guards/`, ecosystem entry, `CLAUDE-GLOBAL-TEMPLATE.md` MCP Hygiene subsection — committed `bf99e2f`, unpushed | [log](2026-04-28.md) |
| 2026-04-21 | Add design-tokens cookbook + correct Penumbra reference model | Created `39-design-tokens.md` (typography/spacing/icons/radii grounded in live Penumbra); added canonical-path + SwiftUI-not-AppKit note to `00-app-shell.md`; promoted Sigil's Theme extensions to sanctioned policy; restructured memory into per-file index | [log](2026-04-21.md) |
| 2026-04-19 | Persist Apple identity + signing conventions | Saved Team ID FDMSRXXN73 + canonical bundle pattern `com.lucesumbrarum.<AppName>` to memory; scanned 8 macOS pbxprojs; added `14_project-identity.md` | [log](2026-04-19.md) |
| 2026-04-14 | Rename root folder XcodeProjects → CodingProjects | Scoped refs, wrote idempotent rename script with backup+rollback; awaiting execution | [log](2026-04-14.md) |
| 2026-04-03 | Install Stream Deck MCP integration for Claude Code | Installed official `@elgato/mcp-server` v0.1.1 globally; native MCP support shipped in Stream Deck 7.4 (April 2026); chose official over 4 community alternatives (verygoodplugins, jxxh204, sohumsuthar, AgentDeck); verified `elgato: Connected` in Claude Code MCP config at both project and user scope | [log](2026-04-03.md) |
| 2026-03-31 | Evaluate Phantom (ghostwright/phantom) autonomous co-worker agent | Not adopting now — flagged as a Mac mini project for a dedicated future session. Key fit: background delegation + persistent cross-project memory via Qdrant-backed MCP (Tailscale + Ollama + Slack channel); less useful for active coding work | [log](2026-03-31.md) |
| 2026-03-24 | Design per-project static website generator for Directions | Bottom-up architecture: each project generates `docs/site/index.html` + `project.json` (data contract); master aggregator at `__DIRECTIONS/site/` discovers all `project.json` files; auto-triggers on `/setup` and `/log`. Complements existing ProjectOverview (top-down). Open questions on schema + LOC/git activity | [log](2026-03-24.md) |
| 2026-03-10 | Install codebase-memory-mcp + persist Apple developer credentials | Installed CMM v0.4.6 to `~/.local/bin/`; auto-registered with 7 editors (Claude Code, Codex CLI, Cursor, Windsurf, Gemini CLI, VS Code, Zed); 4 skills installed (exploring, tracing, quality, reference); complemented Serena (Serena = editing, CMM = exploration/tracing). Created `~/.claude/apple-developer.md` with Team ID FDMSRXXN73 + bundle prefix `com.lucesumbrarum` + signing identity | [log](2026-03-10.md) |
| 2026-02-27 | Wire folder structure into setup flow | Updated template, base, setup command; synced live CLAUDE.md | [log](2026-02-27.md) |
| 2026-02-21 | Improve Claude Code approval-prompt UX | Added rule to global `~/.claude/CLAUDE.md` Communication Style: "always explain the command's purpose in your message before executing it" — workaround until native Claude Code support for command-explanation prompts | [log](2026-02-21.md) |
| 2026-02-18 | Install XcodePreviews globally | Cloned, installed `/preview` command, updated ecosystem docs, tested on Group Alarms | [log](2026-02-18.md) |
| 2026-02-12 | Install codemoot (Claude Code ↔ Codex CLI bridge for cross-model review) | Built from source at `/Users/sim/Tools/codemoot` (npm `@codemoot/cli` had a `workspace:*` protocol bug); `pnpm link --global`; `codemoot doctor` passes in __DIRECTIONS + Penumbra; added global CLAUDE.md note instructing Claude Code to run `codemoot` via Bash, not the built-in `/code-review` skill | [log](2026-02-12.md) |
| 2026-02-11 | Multi-Mac skills sync + deploy pending Directions updates | Pushed pending commit (skills.sh integration); gitignored `.claude/skills/` and `.agents/` (managed externally by `npx skills`); created `scripts/install-skills.sh` for multi-Mac deployment; kept `settings.local.json` in repo as setup reference | [log](2026-02-11.md) |
| 2026-02-10 | Research pointfreeco/sqlite-data + audit projects with persistence | Reviewed sqlite-data (GRDB-backed, SwiftData-like API `@Table`/`@FetchAll`/`@FetchOne`, CloudKit sync, ~6× faster than GRDB+Codable); audited 1-macOS + 2-iOS — found 6 projects with persistence (MusicServer/GRDB best migration candidate; Nexus/CardGamesTutorials/LangDisp/PhotoInventory on SwiftData; Meeting Recording on Core Data); no adoption yet — bookmarked | [log](2026-02-10.md) |
| 2026-02-02 | Deploy cookbook + sync commands | Vestige patterns stored, 7 commands added to global CLAUDE.md | [log](2026-02-02.md) |
| 2026-01-29 | Installed Vestige memory MCP server | MCP configured for Claude | [log](2026-01-29.md) |
| 2026-01-24 | LLM failure modes reference | Added 53_llm-failure-modes.md from WFGY analysis | [log](2026-01-24.md) |
| 2026-01-23 | System improvement analysis | Simplified onboarding, consolidated docs, added testing guide | [log](2026-01-23.md) |
| 2025-02-07 | Install agent skills marketplace (skills.sh) globally | Installed **261 skills** across SwiftUI/Swift (avdlee), UI/UX (vercel-labs, anthropics, nextlevelbuilder, wshobson, giuseppe-trisciuoglio), web (React/Tailwind/shadcn), workflow patterns (obra/superpowers — TDD, systematic-debugging, verification, planning, git-worktrees), docs generation (PDF/DOCX/XLSX/PPTX), architecture, AWS/DevOps; established `npx skills` as the deployment/update mechanism. (*Filename year is 2025 — may be a typo for 2026; content fits the broader 2026 ecosystem timeline.*) | [log](2025-02-07.md) |

---

## Session Log Template

When starting a new session, create a file: `sessions/YYYY-MM-DD-[a|b|c].md`

```markdown
# Session: [Date] [a/b/c]

## Goal
[What we're trying to accomplish]

## Context
- Previous session: [link or summary]
- Current phase: [discovery|planning|implementation|polish|shipping]

## Progress

### Completed
- [x] [What got done]

### In Progress
- [ ] [What's being worked on]

### Discovered
- [New things learned]

### Decisions Made
- [Decision] → logged in decisions.md

### Blockers
- [Anything blocking progress]

## Next Session
- [What to do next]

## Notes
[Anything else worth remembering]
```

---
*One log per session. Link from here.*
